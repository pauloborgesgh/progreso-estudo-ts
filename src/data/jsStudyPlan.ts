import type { Semana } from '../types/study';

export const jsStudyPlan: Semana[] = [
  {
    semana: 1,
    titulo: 'Fundamentos da Linguagem',
    descricao: 'Conheça a história do JavaScript e domine os tipos e operadores fundamentais',
    instrucoes: 'Esta semana estabelece a base de tudo que você vai construir. Comece entendendo o que é JavaScript e como executá-lo, depois explore variáveis, tipos e operadores. Pratique bastante a declaração de variáveis com let e const, e familiarize-se com o console do navegador. Os exercícios de string e number são importantes para criar uma base sólida. Ao final, o mini-projeto vai integrar tudo que você aprendeu em uma calculadora simples.',
    dias: [
      {
        dia: 1,
        titulo: 'Introdução ao JavaScript',
        teoria: 'JavaScript é a linguagem de programação mais utilizada na web, criada por Brendan Eich em 1995. Ela permite criar desde interações simples em páginas até aplicações completas (full-stack) com Node.js.',
        topicos: [
          { titulo: 'O que é JavaScript e sua história', descricao: 'JavaScript foi criada em apenas 10 dias por Brendan Eich na Netscape, e desde então evoluiu para se tornar a linguagem mais onipresente da história da computação. Hoje ela roda em navegadores, servidores (Node.js), dispositivos móveis e até IoT. Diferente de linguagens como Java ou C#, JavaScript é dinâmica e baseada em protótipos. O nome "JavaScript" foi uma estratégia de marketing para aproveitar a popularidade do Java da Sun Microsystems.', exemplo: 'console.log("Olá, mundo!"); // Primeiro programa em JS' },
          { titulo: 'Configuração do ambiente', descricao: 'Para começar, você só precisa de um navegador moderno (Chrome, Firefox ou Edge) e um editor de código como VS Code. O console do navegador (F12 > Console) permite testar código imediatamente sem configurar nada. Para projetos maiores, instale o Node.js (nodejs.org) que inclui o npm para gerenciar pacotes. Com o Node.js, você pode executar arquivos .js diretamente no terminal com node arquivo.js.', exemplo: 'node meu-arquivo.js // Executa JS no terminal' },
          { titulo: 'Console.log e debugging', descricao: 'console.log() é a ferramenta mais básica e essencial do JavaScript — ela imprime valores no console do navegador ou terminal. Você pode passar múltiplos valores separados por vírgula: console.log("nome:", nome). O console também oferece console.error() para erros, console.table() para arrays/objetos, e console.time() para medir performance. Para debug mais avançado, use a palavra-chave debugger ou os breakpoints do DevTools.', exemplo: 'console.log("nome:", "Ana", "idade:", 25);\nconsole.table([{nome:"Ana"},{nome:"Bruno"}]);' },
          { titulo: 'Sintaxe básica e comentários', descricao: 'JavaScript tem sintaxe similar a C e Java: statements terminam com ; (opcional mas recomendado), blocos usam { }, e variáveis não precisam de tipo declarado. Comentários de uma linha usam // e de múltiplas linhas /* */. JavaScript é case-sensitive (nome é diferente de Nome). A indentação não é obrigatória mas é essencial para legibilidade. Statements podem ocupar múltiplas linhas e o ponto-e-vírgula é inserido automaticamente (ASI) em muitos casos.', exemplo: '// Comentário de linha\n/* Comentário\n  de bloco */\nlet nome = "Ana"; // case-sensitive' }
        ],
        exercicio: 'Abra o console do navegador, declare variáveis com seu nome e idade, e exiba uma saudação usando console.log.'
      },
      {
        dia: 2,
        titulo: 'Variáveis e Tipos Primitivos',
        teoria: 'JavaScript tem 7 tipos primitivos: string, number, boolean, null, undefined, symbol e bigint. Variáveis podem ser declaradas com var, let ou const, cada uma com regras diferentes de escopo e reatribuição.',
        topicos: [
          { titulo: 'var, let e const', descricao: 'var tem escopo de função e sofre hoisting (elevação), sendo a forma antiga de declarar variáveis. let tem escopo de bloco ({}) e não pode ser redeclarada no mesmo escopo. const também tem escopo de bloco mas não permite reatribuição — o valor é constante. A regra moderna é: use const por padrão, let quando precisar reatribuir, e evite var completamente. const não significa imutável — objetos const podem ter propriedades alteradas.', exemplo: 'const nome = "Ana";\nlet idade = 25;\nidade = 26; // ok\n// nome = "João" // erro! const não reatribui' },
          { titulo: 'Tipo string', descricao: 'Strings representam texto e podem ser declaradas com aspas simples (\'), duplas (") ou crases (template literals: `texto ${expressao}`). Template literals permitem interpolação de variáveis e strings multilinha sem precisar de \n. Strings são imutáveis — métodos como toUpperCase() retornam uma nova string sem modificar a original. O operador + concatena strings, mas template literals são mais legíveis para valores dinâmicos.', exemplo: 'const nome = "Ana";\n`Olá, ${nome}!` // "Olá, Ana!"\n"JavaScript".length // 10' },
          { titulo: 'Tipo number', descricao: 'JavaScript não distingue entre inteiros e decimais — tudo é number (formato IEEE 754 de 64 bits). Valores especiais incluem Infinity, -Infinity e NaN (Not a Number). NaN é curioso: typeof NaN retorna "number", mas NaN === NaN é false (use Number.isNaN() para verificar). Operações matemáticas usam +, -, *, /, % (módulo) e ** (exponenciação). Para cálculos precisos com decimais, cuidado com arredondamentos — 0.1 + 0.2 não é exatamente 0.3.', exemplo: 'typeof 42 // "number"\n0.1 + 0.2 // 0.30000000000000004\nNaN === NaN // false (use Number.isNaN())' },
          { titulo: 'Boolean, null e undefined', descricao: 'Boolean tem apenas dois valores: true e false, usados em condicionais. null representa ausência intencional de valor — é algo que você atribui. undefined significa que uma variável foi declarada mas não inicializada, ou uma propriedade que não existe. null é um object (typeof null retorna "object", um erro histórico). undefined é o próprio tipo. Em condicionais, ambos são falsy, mas têm significados diferentes: null é "vazio de propósito", undefined é "não definido".', exemplo: 'let x; // undefined\nlet y = null; // nulo intencional\ntypeof null // "object" (erro histórico)' }
        ],
        exercicio: 'Declare variáveis com const e let para nome, idade, altura e uma lista de hobbies. Use typeof para verificar os tipos.',
        codigoInicial: `// Declare suas variáveis aqui\nconst nome = "SeuNome";\nlet idade = 25;\n\n// Verifique os tipos\nconsole.log(typeof nome);\nconsole.log(typeof idade);`,
        codigoSolucao: `const nome = "Maria Silva";\nlet idade = 25;\nconst altura = 1.68;\nconst hobbies = ["ler", "correr", "codar"];\n\nconsole.log(typeof nome);     // "string"\nconsole.log(typeof idade);    // "number"\nconsole.log(typeof altura);   // "number"\nconsole.log(typeof hobbies);  // "object" (arrays são objetos)\nconsole.log(typeof null);     // "object" (erro histórico conhecido)\nconsole.log(typeof undefined);// "undefined"`
      },
      {
        dia: 3,
        titulo: 'Operadores',
        teoria: 'Operadores realizam operações em valores (operandos). JavaScript tem operadores aritméticos, de comparação, lógicos, de atribuição e mais.',
        topicos: [
          { titulo: 'Operadores aritméticos', descricao: 'Os operadores aritméticos são: + (adição), - (subtração), * (multiplicação), / (divisão), % (módulo/resto) e ** (exponenciação). O operador + também concatena strings: 1 + 2 = 3, mas "1" + 2 = "12". Para converter string para número use Number() ou o unário +: +"123" = 123. O módulo (%) é útil para verificar paridade (x % 2 === 0) e ciclos. A precedência segue a matemática padrão: * e / antes de + e -, use parênteses para agrupar.', exemplo: '10 + 5 // 15\n"1" + 2 // "12"\n10 % 3 // 1\n2 ** 3 // 8' },
          { titulo: 'Operadores de comparação', descricao: 'Comparação de igualdade: == (com coerção de tipo) e === (estrito, sem coerção). A regra de ouro: sempre use === a menos que tenha um motivo específico para usar ==. Exemplo: 1 == "1" é true, mas 1 === "1" é false. Outros: != (desigualdade com coerção), !== (desigualdade estrita), >, <, >=, <=. A coerção de tipo do == pode causar bugs sutis — por isso === é recomendado. Para comparar objetos, == e === verificam referência, não conteúdo.', exemplo: '1 === "1" // false\n1 == "1" // true (coerção)\n10 > 5 // true\n"a" < "b" // true (ordem lexicográfica)' },
          { titulo: 'Operadores lógicos', descricao: 'Os operadores lógicos && (AND), || (OR) e ! (NOT) funcionam com curto-circuito. && retorna o primeiro valor falsy ou o último valor: 0 && "texto" retorna 0. || retorna o primeiro truthy ou o último: "" || "padrão" retorna "padrão". ! nega o valor: !true = false, !0 = true (0 é falsy). O curto-circuito é muito usado para valores padrão: const nome = usuario.nome || "Visitante". && é usado para renderização condicional em React.', exemplo: 'true && false // false\n"" || "padrão" // "padrão"\n!true // false\n!!"texto" // true (conversão para boolean)' },
          { titulo: 'Operador ternário e precedência', descricao: 'O operador ternário (condição ? valorSeVerdadeiro : valorSeFalso) é uma forma concisa de if/else. Exemplo: const status = idade >= 18 ? "adulto" : "menor". Ternários aninhados são possíveis mas prejudicam a legibilidade — prefira if/else ou switch para múltiplas condições. A precedência de operadores determina a ordem de avaliação: ! tem maior precedência, seguido por comparações, &&, e por último ||. Use parênteses para evitar ambiguidades.', exemplo: 'const status = idade >= 18 ? "adulto" : "menor";\n!true && false || true // true (precedência: ! > && > ||)' }
        ],
        exercicio: 'Crie expressões que testem todos os operadores: some dois números, compare strings, use lógicos para verificar se um número está entre 10 e 20.',
        codigoInicial: `const a = 15;\nconst b = "15";\nconst c = 20;\n\n// Teste comparações\nconsole.log("a == b:", a == b);\nconsole.log("a === b:", a === b);\n\n// Use operadores lógicos\nconsole.log("a entre 10 e 20:", a > 10 && a < 20);\n\n// Ternário\nconsole.log(a >= 18 ? "Maior" : "Menor");`,
        codigoSolucao: `const a = 15;\nconst b = "15";\nconst c = 20;\n\nconsole.log("a == b:", a == b);       // true (coerção)\nconsole.log("a === b:", a === b);     // false (tipos diferentes)\nconsole.log("a != b:", a != b);       // false\nconsole.log("a !== b:", a !== b);     // true\n\nconsole.log("a entre 10 e 20:", a > 10 && a < 20); // true\nconsole.log("a é 15 ou 20:", a === 15 || a === 20); // true\nconsole.log("não é 15:", !(a === 15));              // false\n\nconsole.log(a >= 18 ? "Maior" : "Menor"); // "Menor"`
      },
      {
        dia: 4,
        titulo: 'Strings em Detalhe',
        teoria: 'Strings em JavaScript têm diversos métodos para manipulação: split, join, slice, trim, replace, toUpperCase, toLowerCase, charAt e mais.',
        topicos: [
          { titulo: 'length e acesso a caracteres', descricao: 'A propriedade length retorna o número de caracteres da string: "Hello".length = 5. Para acessar um caractere específico, use colchetes: "Hello"[1] = "e" (índices começam em 0). O método charAt(posição) faz o mesmo: "Hello".charAt(1) = "e". Índices negativos retornam undefined em strings (diferente de arrays). Strings são iteráveis com for...of, que percorre cada caractere incluindo emojis (que podem ter 2 caracteres UTF-16).', exemplo: '"JavaScript"[0] // "J"\n"Hello".length // 5' },
          { titulo: 'toUpperCase e toLowerCase', descricao: 'toUpperCase() converte todos os caracteres para maiúsculas e toLowerCase() para minúsculas. Esses métodos são essenciais para normalização de texto em buscas e comparações. Exemplo: "João".toLowerCase() retorna "joão". Lembre-se que strings são imutáveis — os métodos retornam uma nova string. Para comparação case-insensitive: str1.toLowerCase() === str2.toLowerCase(). Eles respeitam caracteres acentuados e especiais.', exemplo: '"Java".toUpperCase() // "JAVA"\n"Java".toLowerCase() // "java"' },
          { titulo: 'split e join', descricao: 'split(separador) divide uma string em um array de substrings. Exemplo: "maçã,banana,uva".split(",") retorna ["maçã", "banana", "uva"]. split("") separa por caractere. split() sem argumento retorna um array com a string inteira. join(separador) é o inverso: junta um array em uma string. Exemplo: ["a", "b", "c"].join("-") retorna "a-b-c". A combinação split + join é poderosa para transformar textos.', exemplo: '"a,b,c".split(",") // ["a","b","c"]\n["a","b"].join("-") // "a-b"' },
          { titulo: 'slice, trim e replace', descricao: 'slice(início, fim) extrai uma parte da string sem modificar a original. Aceita índices negativos: "JavaScript".slice(-6) = "Script". trim() remove espaços em branco do início e fim — essencial para limpar inputs de formulários. replace(busca, substituição) substitui a primeira ocorrência: "azul azul".replace("azul", "verde") = "verde azul". Para substituir todas, use replaceAll() ou regex com global: replace(/azul/g, "verde").', exemplo: '"JavaScript".slice(0, 4) // "Java"\n"  texto ".trim() // "texto"\n"azul azul".replaceAll("azul", "verde")' },
          { titulo: 'includes, startsWith e concatenação', descricao: 'includes(substring) verifica se a string contém a substring: "JavaScript".includes("Script") = true. startsWith() e endsWith() verificam início e fim. Concatenação pode ser feita com +, concat(), ou template literals. Template literals com ${} são a forma mais moderna e legível: `Olá, ${nome}!`. Eles também permitem strings multilinha sem \n. Para repetir strings: "abc".repeat(3) = "abcabcabc".', exemplo: '"JavaScript".includes("Script") // true\n"abc".repeat(3) // "abcabcabc"' }
        ],
        exercicio: 'Receba um nome completo, extraia o primeiro nome e sobrenome com split, exiba em maiúsculas e substitua espaços por traços.',
        codigoInicial: `const nomeCompleto = "Maria da Silva Santos";\n\n// Use split para separar\nconst partes = nomeCompleto.split(" ");\nconsole.log(partes);\n\n// Extraia primeiro nome\nconst primeiroNome = partes[0];\nconsole.log("Primeiro nome:", primeiroNome.toUpperCase());`,
        codigoSolucao: `const nomeCompleto = "Maria da Silva Santos";\n\nconst partes = nomeCompleto.split(" ");\nconsole.log("Partes:", partes);\n\nconst primeiroNome = partes[0];\nconst ultimoNome = partes[partes.length - 1];\nconsole.log("Primeiro nome:", primeiroNome.toUpperCase());\nconsole.log("Último nome:", ultimoNome.toLowerCase());\n\nconst nomeFormatado = nomeCompleto.replace(/\\s/g, "-");\nconsole.log("Com traços:", nomeFormatado);\n\nconsole.log("Total de caracteres:", nomeCompleto.length);\nconsole.log("Contém 'Silva':", nomeCompleto.includes("Silva"));\nconsole.log("Começa com 'Maria':", nomeCompleto.startsWith("Maria"));`
      },
      {
        dia: 5,
        titulo: 'Numbers e Math',
        teoria: 'JavaScript tem o objeto Math com métodos para operações matemáticas como Math.round, Math.floor, Math.ceil, Math.random, Math.max e Math.min.',
        topicos: [
          { titulo: 'Number e métodos estáticos', descricao: 'O objeto Number fornece métodos úteis: Number.isInteger() verifica se é inteiro, Number.isNaN() verifica se é NaN (diferente de isNaN global que faz coerção). Number.parseInt() e Number.parseFloat() convertem strings para números. toFixed(n) formata um número com n casas decimais e retorna string: (3.14159).toFixed(2) = "3.14". toPrecision() controla o total de dígitos significativos.', exemplo: 'Number.isNaN("abc") // false (sem coerção)\nisNaN("abc") // true (coerção!)\n(3.14159).toFixed(2) // "3.14"' },
          { titulo: 'Math.floor, Math.ceil e Math.round', descricao: 'Math.floor() arredonda para baixo (piso): Math.floor(4.9) = 4. Math.ceil() arredonda para cima (teto): Math.ceil(4.1) = 5. Math.round() arredonda para o inteiro mais próximo: Math.round(4.5) = 5, Math.round(4.4) = 4. Use floor para índices de arrays, ceil para paginação, round para médias. Para arredondar para decimais específicos: Math.round(valor * 100) / 100.', exemplo: 'Math.floor(4.9) // 4\nMath.ceil(4.1) // 5\nMath.round(4.5) // 5\nMath.round(4.4) // 4' },
          { titulo: 'Math.random', descricao: 'Math.random() retorna um número pseudo-aleatório entre 0 (inclusivo) e 1 (exclusivo). Para gerar números em intervalos: Math.random() * max (0 a max). Para inteiros: Math.floor(Math.random() * (max - min + 1)) + min. Exemplo para 1-6 (dado): Math.floor(Math.random() * 6) + 1. Math.random() não é criptograficamente segura — para isso use crypto.getRandomValues().', exemplo: 'Math.random() // 0.723... (entre 0 e 1)\nMath.floor(Math.random() * 6) + 1 // dado 1-6' },
          { titulo: 'Math.max, Math.min e Math.abs', descricao: 'Math.max(n1, n2, n3...) retorna o maior valor, Math.min() o menor. Ambos ignoram NaN e undefined. Para usar com arrays: Math.max(...array) ou Math.max.apply(null, array). Math.abs() retorna o valor absoluto (remove sinal negativo). Math.pow(base, expoente) é equivalente a **. Math.sqrt() calcula raiz quadrada. Math.trunc() remove a parte decimal sem arredondar: Math.trunc(4.9) = 4 (diferente de floor com negativos).', exemplo: 'Math.max(1, 5, 3) // 5\nMath.abs(-10) // 10\nMath.pow(2, 3) // 8\nMath.sqrt(9) // 3' }
        ],
        exercicio: 'Crie um gerador de números aleatórios para um jogo de dado (1-6) e uma função que arredonda valores monetários para 2 casas decimais.',
        codigoInicial: `// Dado virtual\nfunction rolarDado() {\n  // Retorne número entre 1 e 6\n}\n\nconsole.log("Dado:", rolarDado());\n\n// Formatar preço\nfunction formatarPreco(valor) {\n  // Retorne com 2 casas decimais\n}\n\nconsole.log("Preço:", formatarPreco(49.9));`,
        codigoSolucao: `function rolarDado() {\n  return Math.floor(Math.random() * 6) + 1;\n}\n\nconsole.log("Dado:", rolarDado());\n\nfunction formatarPreco(valor) {\n  return "R$ " + valor.toFixed(2);\n}\n\nconsole.log("Preço:", formatarPreco(49.9)); // "R$ 49.90"\n\n// Extra: números aleatórios entre min e max\nfunction aleatorioEntre(min, max) {\n  return Math.floor(Math.random() * (max - min + 1)) + min;\n}\n\nconsole.log("Entre 10 e 20:", aleatorioEntre(10, 20));`,
      }
    ],
    miniProjeto: {
      titulo: 'Manipulador de Texto',
      descricao: 'Crie funções para manipular strings usando split, join, trim, toUpperCase e replace',
      requisitos: [
        'Função que conta palavras em uma frase (split + length)',
        'Função que transforma para slug (replace + split + join)',
        'Função que extrai iniciais de um nome',
        'Função que valida email (includes + trim)',
        'Função que mascara CPF com replace'
      ]
    }
  },
  {
    semana: 2,
    titulo: 'Controle de Fluxo',
    descricao: 'Domine condicionais, switches e loops para controlar o fluxo do seu código',
    instrucoes: 'Esta semana é sobre tomada de decisões e repetição. Comece com if/else para entender o fluxo condicional, depois explore switch para múltiplas opções. Os loops são fundamentais para processar listas de dados. Pratique os diferentes tipos de loop (for, while, for...of) para entender quando usar cada um. O mini-projeto da semana vai integrar condicionais e loops em uma aplicação prática de calculadora.',
    dias: [
      {
        dia: 1,
        titulo: 'Condicionais if/else',
        teoria: 'Condicionais permitem executar código diferente baseado em condições booleanas. if executa um bloco se a condição for verdadeira, else if testa condições adicionais, e else captura todos os outros casos.',
        topicos: [
          { titulo: 'Estrutura if/else if/else', descricao: 'A estrutura básica é: if (condição) { /* código */ } else if (outraCondição) { /* código */ } else { /* fallback */ }. A condição é convertida para boolean — valores falsy (false, 0, "", null, undefined, NaN) são tratados como false. Apenas o primeiro bloco verdadeiro é executado. else if e else são opcionais. Para múltiplas condições independentes, use if separados.', exemplo: 'if (nota >= 7) { console.log("Aprovado"); } else { console.log("Reprovado"); }' },
          { titulo: 'Truthy e Falsy', descricao: 'JavaScript converte qualquer valor para boolean em contextos condicionais. Valores falsy: false, 0, "" (string vazia), null, undefined, NaN. Todos os outros são truthy: objetos vazios ({}) são truthy, arrays vazios ([]) são truthy, "false" (string) é truthy. Isso é importante: if ("false") executa o bloco! Para verificar se um array está vazio, use array.length > 0, não if(array) que é sempre true.', exemplo: '!!"texto" // true\n!!0 // false' },
          { titulo: 'Condicionais aninhadas', descricao: 'É possível colocar if dentro de if para condições mais complexas. Exemplo: if (usuario.logado) { if (usuario.admin) { /* painel admin */ } }. Cuidado com muitos níveis de aninhamento — eles reduzem a legibilidade. Use operadores lógicos (&&, ||) para simplificar: if (usuario.logado && usuario.admin). Outra alternativa é early return: if (!usuario.logado) return;.', exemplo: 'if (logado && admin) { painelAdmin(); }' },
          { titulo: 'Boas práticas com if/else', descricao: 'Evite else desnecessário: if (condicao) { return x; } return y; é mais limpo que if/else com return. Prefira switch para múltiplos valores fixos. Use operador ternário para condicionais simples de uma linha. Mantenha blocos pequenos — se um bloco if for muito grande, extraia para uma função. Sempre use {} mesmo para uma linha — isso previne bugs ao adicionar código depois.', exemplo: 'if (idade < 18) return "Menor"; return "Adulto";' }
        ],
        exercicio: 'Crie uma função que recebe uma nota de 0 a 10 e retorna o conceito: A (9-10), B (7-8), C (5-6), D (3-4), E (0-2).',
        codigoInicial: `function notaParaConceito(nota) {\n  // Implemente aqui\n}\n\nconsole.log(notaParaConceito(9.5));\nconsole.log(notaParaConceito(7));\nconsole.log(notaParaConceito(3));`,
        codigoSolucao: `function notaParaConceito(nota) {\n  if (nota >= 9) return "A";\n  if (nota >= 7) return "B";\n  if (nota >= 5) return "C";\n  if (nota >= 3) return "D";\n  return "E";\n}\n\nconsole.log(notaParaConceito(9.5)); // "A"\nconsole.log(notaParaConceito(7));   // "B"\nconsole.log(notaParaConceito(3));   // "D"\nconsole.log(notaParaConceito(1));   // "E"`
      },
      {
        dia: 2,
        titulo: 'Switch e Operador Ternário',
        teoria: 'Switch permite testar uma variável contra múltiplos valores de forma mais organizada que múltiplos if/else. O ternário é uma forma concisa de if/else para expressões simples.',
        topicos: [
          { titulo: 'Estrutura switch/case', descricao: 'switch(expressão) { case valor1: /* código */ break; case valor2: /* código */ break; default: /* fallback */ }. A expressão é comparada com cada case usando ===. break interrompe a execução — sem ele, o fluxo "cai" para o próximo case (fall-through). default é opcional e executa se nenhum case corresponder. Switch é ideal para menus, estados de máquina e despacho de ações.', exemplo: 'switch (dia) { case 1: console.log("Dom"); break; default: break; }' },
          { titulo: 'Fall-through intencional', descricao: 'O fall-through (execução contínua entre cases) pode ser útil intencionalmente. Exemplo: case 1: case 2: console.log("Início"); break; — ambos os cases executam o mesmo código. Outro uso: case "verao": case "primavera": console.log("Quente"); break;. Documente fall-through intencional com comentários para evitar confusão. Alternativa moderna: agrupar cases vazios como acima.', exemplo: 'case 1: case 2: console.log("Inicio"); break;' },
          { titulo: 'Operador ternário', descricao: 'O ternário (condição ? valorSeTrue : valorSeFalse) é uma expressão, não um statement — ele RETORNA um valor. Exemplo: const maioridade = idade >= 18 ? "Adulto" : "Menor". Ternários podem ser encadeados: a > b ? "a" : b > c ? "b" : "c" — mas isso prejudica legibilidade. Use ternário apenas para casos simples de uma linha. Para lógica mais complexa, prefira if/else ou switch.', exemplo: 'const status = idade >= 18 ? "Adulto" : "Menor";' },
          { titulo: 'Quando usar switch vs if', descricao: 'Use switch quando testar uma única variável contra múltiplos valores fixos (números, strings). Use if/else para condições com operadores lógicos, intervalos (nota > 7), ou expressões complexas. Switch é mais verboso mas mais legível para 3+ cases. Em código moderno, objetos de mapeamento (lookup) são uma alternativa: const cores = { 1: "vermelho", 2: "azul" }; cores[valor] || "padrão".', exemplo: 'const cores = { 1: "vermelho", 2: "azul" }; cores[valor] || "padrao";' }
        ],
        exercicio: 'Crie uma função que recebe o dia da semana (1-7) e retorna se é dia útil ou fim de semana. Use switch com fall-through.',
        codigoInicial: `function tipoDia(dia) {\n  // Use switch com fall-through\n}\n\nconsole.log(tipoDia(2)); // "Dia útil"\nconsole.log(tipoDia(7)); // "Fim de semana"\nconsole.log(tipoDia(0)); // "Dia inválido"`,
        codigoSolucao: `function tipoDia(dia) {\n  switch (dia) {\n    case 1:\n    case 7:\n      return "Fim de semana";\n    case 2:\n    case 3:\n    case 4:\n    case 5:\n    case 6:\n      return "Dia útil";\n    default:\n      return "Dia inválido";\n  }\n}\n\nconsole.log(tipoDia(2)); // "Dia útil"\nconsole.log(tipoDia(7)); // "Fim de semana"\nconsole.log(tipoDia(0)); // "Dia inválido"\n\n// Versão com ternário:\nconst diaUtil = dia >= 2 && dia <= 6 ? "Dia útil" : "Fim de semana";`
      },
      {
        dia: 3,
        titulo: 'Loops: for, while, do-while',
        teoria: 'Loops executam um bloco de código repetidamente enquanto uma condição for verdadeira. for, while e do-while são as estruturas clássicas de repetição.',
        topicos: [
          { titulo: 'Loop for clássico', descricao: 'for (inicialização; condição; incremento) { /* bloco */ } é o loop mais usado. Exemplo: for (let i = 0; i < 5; i++) { console.log(i); } imprime 0 a 4. A inicialização executa uma vez, depois a condição é testada antes de cada iteração, e o incremento executa após cada bloco. Qualquer parte do for pode ser vazia: for (;;) é um loop infinito. Use let para o contador — var vaza do bloco.', exemplo: 'for (let i = 0; i < 5; i++) { console.log(i); }' },
          { titulo: 'Loop while', descricao: 'while (condição) { /* bloco */ } testa a condição ANTES de cada iteração. Se a condição for false no início, o bloco nunca executa. É ideal quando você não sabe quantas iterações serão necessárias: while (sorteio !== 6) { sorteio = rolarDado(); }. Risco de loop infinito se a condição nunca ficar false — sempre atualize a variável da condição dentro do bloco.', exemplo: 'while (sorteio !== 6) { sorteio = rolarDado(); }' },
          { titulo: 'Loop do-while', descricao: 'do { /* bloco */ } while (condição); executa o bloco PELO MENOS UMA VEZ antes de testar a condição. É útil quando você precisa que algo aconteça antes da verificação: do { resposta = prompt("Digite um número:"); } while (isNaN(resposta)). A diferença para while é a garantia de execução mínima. Menos comum que for e while, mas essencial em casos específicos.', exemplo: 'do { resp = prompt("Num:"); } while (isNaN(resp));' },
          { titulo: 'Break e continue', descricao: 'break interrompe completamente o loop: for (let i = 0; i < 10; i++) { if (i === 5) break; } — para no 5. continue pula para a próxima iteração: for (let i = 0; i < 10; i++) { if (i % 2 === 0) continue; console.log(i); } — imprime só ímpares. Em loops aninhados, break/continue afetam apenas o loop mais interno. Use labels (outer: for...) para controlar loops externos, mas isso é raro.', exemplo: 'for (let i = 0; i < 10; i++) { if (i === 5) break; if (i % 2 === 0) continue; }' }
        ],
        exercicio: 'Use for para imprimir a tabuada do 7 (7x1 até 7x10). Use while para encontrar o primeiro número divisível por 3 e 5 ao mesmo tempo.',
        codigoInicial: `// Tabuada do 7 com for\nconsole.log("=== Tabuada do 7 ===");\n\n// Encontrar múltiplo com while\nlet num = 1;\nwhile (true) {\n  // Verifique se é divisível por 3 E por 5\n  break;\n  num++;\n}\nconsole.log("Múltiplo de 3 e 5:", num);`,
        codigoSolucao: `// Tabuada do 7 com for\nconsole.log("=== Tabuada do 7 ===");\nfor (let i = 1; i <= 10; i++) {\n  console.log(\`7 x \${i} = \${7 * i}\`);\n}\n\n// Encontrar múltiplo com while\nlet num = 1;\nwhile (true) {\n  if (num % 3 === 0 && num % 5 === 0) {\n    console.log("Múltiplo de 3 e 5:", num);\n    break;\n  }\n  num++;\n}\n\n// Extra: continue\nfor (let i = 1; i <= 10; i++) {\n  if (i % 2 === 0) continue;\n  console.log(\"Ímpar:\", i);\n}`
      },
      {
        dia: 4,
        titulo: 'Loops: for...of e for...in',
        teoria: 'Além dos loops clássicos, JavaScript tem for...of (para iteráveis como arrays e strings) e for...in (para propriedades de objetos).',
        topicos: [
          { titulo: 'For...of com arrays', descricao: 'for (const elemento of array) { } itera sobre os VALORES de um array, string ou qualquer iterável. Exemplo: for (const fruta of frutas) { console.log(fruta); }. É mais legível e seguro que for clássico quando você não precisa do índice. O const no elemento é possível porque cada iteração cria um novo escopo. Não funciona com objetos comuns (que não são iteráveis).', exemplo: 'for (const fruta of frutas) { console.log(fruta); }' },
          { titulo: 'For...of com strings e iteráveis', descricao: 'for...of também funciona com strings: for (const char of "JavaScript") percorre cada caractere. Funciona com Map, Set, NodeList (DOM) e qualquer objeto que implemente Symbol.iterator. Para acessar o índice em for...of, use entries(): for (const [indice, valor] of array.entries()). Isso combina a legibilidade do for...of com acesso ao índice.', exemplo: 'for (const [i, v] of arr.entries()) { console.log(i, v); }' },
          { titulo: 'For...in para objetos', descricao: 'for (const chave in objeto) { } itera sobre as CHAVES (propriedades) de um objeto. Exemplo: for (const prop in usuario) { console.log(prop, usuario[prop]); }. For...in também itera sobre propriedades herdadas da prototype chain — use hasOwnProperty() para filtrar. Não use for...in com arrays (itera sobre índices como strings "0", "1" — e inclui propriedades extras).', exemplo: 'for (const chave in obj) { console.log(chave, obj[chave]); }' },
          { titulo: 'Comparação entre tipos de loop', descricao: 'Use for clássico quando precisar de controle total (índice, condição complexa). Use for...of para iterar valores de arrays/strings (mais legível). Use for...in para objetos. Use while quando não souber o número de iterações. Use do...while quando precisar executar pelo menos uma vez. Métodos como forEach, map, filter são preferíveis para arrays em código moderno (próximas semanas).', exemplo: '[1, 2, 3].forEach(x => console.log(x));' }
        ],
        exercicio: 'Crie um array de frutas. Use for...of para listar cada uma. Use for...in para listar propriedades de um objeto carro (marca, modelo, ano).',
        codigoInicial: `const frutas = ["Maçã", "Banana", "Uva", "Laranja"];\nconst carro = { marca: "Toyota", modelo: "Corolla", ano: 2024 };\n\n// For...of com frutas\n\n// For...in com carro`,
        codigoSolucao: `const frutas = ["Maçã", "Banana", "Uva", "Laranja"];\nconst carro = { marca: "Toyota", modelo: "Corolla", ano: 2024 };\n\nconsole.log("=== Frutas (for...of) ===");\nfor (const fruta of frutas) {\n  console.log(fruta);\n}\n\nconsole.log("\\n=== Frutas com índice ===");\nfor (const [i, fruta] of frutas.entries()) {\n  console.log(\`\${i + 1}. \${fruta}\`);\n}\n\nconsole.log("\\n=== Carro (for...in) ===");\nfor (const prop in carro) {\n  console.log(\`\${prop}: \${carro[prop]}\`);\n}`
      },
      {
        dia: 5,
        titulo: 'Mini-Projeto: Calculadora',
        teoria: 'Vamos construir uma calculadora completa usando condicionais, switch e loops. Este projeto integra todos os conceitos da semana.',
        topicos: [
          { titulo: 'Estrutura da calculadora', descricao: 'Nossa calculadora terá funções para cada operação (somar, subtrair, multiplicar, dividir) e uma função principal que usa switch para selecionar a operação. Usaremos prompt() para entrada do usuário e um loop while para permitir múltiplos cálculos. Esta estrutura é um padrão comum: uma função central que despacha para funções específicas baseado em um parâmetro.', exemplo: 'switch (op) { case "+": return a + b; }' },
          { titulo: 'Validação de entrada', descricao: 'Sempre valide entradas do usuário. Use isNaN() ou Number.isNaN() para verificar se o valor é numérico. Verifique divisão por zero. Use trim() para limpar espaços. Para loops com entrada do usuário, use do-while para garantir que a entrada seja lida pelo menos uma vez. Uma boa experiência do usuário inclui mensagens de erro claras e a opção de sair.', exemplo: 'if (isNaN(a) || b === 0) { console.log("Erro"); }' },
          { titulo: 'Organização do código', descricao: 'Separe responsabilidades: uma função por operação aritmética, uma função para exibir o menu, uma função principal que coordena o fluxo. Use nomes descritivos: somar() ao invés de op1(). Mantenha funções pequenas e com uma única responsabilidade. Use const para funções quando possível. Comente blocos lógicos para facilitar a leitura.', exemplo: 'const somar = (a, b) => a + b;' }
        ],
        exercicio: 'Complete a calculadora com as operações básicas e um loop que permite múltiplos cálculos até o usuário digitar "sair".',
        codigoInicial: `function menu() {\n  return prompt("Digite a operação (+, -, *, /) ou 'sair':");\n}\n\nfunction calcular(op, a, b) {\n  // Use switch para selecionar a operação\n}\n\n// Loop principal\nlet continuar = true;\nwhile (continuar) {\n  // Implemente o fluxo\n}`,
        codigoSolucao: `function menu() {\n  return prompt("Digite a operação (+, -, *, /) ou 'sair':");\n}\n\nfunction calcular(op, a, b) {\n  switch (op) {\n    case "+": return a + b;\n    case "-": return a - b;\n    case "*": return a * b;\n    case "/":\n      if (b === 0) return "Erro: divisão por zero!";\n      return a / b;\n    default: return "Operação inválida!";\n  }\n}\n\nlet continuar = true;\nwhile (continuar) {\n  const op = menu();\n  if (op === "sair") { continuar = false; break; }\n  \n  const a = Number(prompt("Primeiro número:"));\n  const b = Number(prompt("Segundo número:"));\n  \n  if (isNaN(a) || isNaN(b)) {\n    alert("Números inválidos!");\n    continue;\n  }\n  \n  alert(\`Resultado: \${calcular(op, a, b)}\`);\n}`
      }
    ],
    miniProjeto: {
      titulo: 'Calculadora Interativa',
      descricao: 'Calculadora com switch, validação de entrada e loop para repetição',
      requisitos: [
        'Funções para cada operação (somar, subtrair, multiplicar, dividir)',
        'Switch para selecionar operação',
        'Validação de entrada (isNaN, divisão por zero)',
        'Loop while para múltiplos cálculos',
        'Opção de sair digitando "sair"'
      ]
    }
  },
  {
    semana: 3,
    titulo: 'Funções',
    descricao: 'Aprenda a criar e usar funções, desde a declaração básica até closures e higher-order functions',
    instrucoes: 'Funções são o bloco de construção mais importante do JavaScript. Comece entendendo as diferentes formas de declarar funções e depois explore escopo, closures e higher-order functions. Preste atenção especial à diferença entre function declaration e arrow functions, especialmente em relação ao this. Os conceitos de closure e callback são fundamentais para tópicos avançados como React e programação assíncrona. Pratique bastante até se sentir confortável.',
    dias: [
      {
        dia: 1,
        titulo: 'Declaração e Expressão de Funções',
        teoria: 'Funções podem ser declaradas de várias formas: function declaration (hoisted), function expression, e arrow functions. Cada forma tem suas particularidades de escopo, hoisting e this.',
        topicos: [
          { titulo: 'Function declaration', descricao: 'Function declarations são definidas com a palavra-chave function: function somar(a, b) { return a + b; }. Elas sofrem hoisting completo — podem ser chamadas antes da declaração no código. Isso ocorre porque o interpretador JavaScript move todas as declarações para o topo do escopo durante a compilação. Function declarations criam uma variável no escopo atual com o nome da função. São a forma mais tradicional e ainda muito usada.', exemplo: 'function somar(a, b) { return a + b; }' },
          { titulo: 'Function expression', descricao: 'Function expressions atribuem uma função a uma variável: const somar = function(a, b) { return a + b; }. A função em si é anônima (sem nome), mas pode ter nome para recursão. Diferente de declarations, expressions NÃO sofrem hoisting — a variável é hoisted mas fica undefined até a atribuição. Function expressions são úteis para callbacks, IIFEs e quando você quer atribuir funções condicionalmente.', exemplo: 'const somar = function(a, b) { return a + b; };' },
          { titulo: 'Arrow functions', descricao: 'Arrow functions (=>) são uma sintaxe mais concisa: const somar = (a, b) => a + b. Se o corpo tem uma única expressão, o return é implícito (sem { }). Com { } você precisa de return explícito. Com um parâmetro, os parênteses são opcionais: x => x * 2. Arrow functions NÃO têm seu próprio this — elas herdam do escopo pai. Também não têm arguments object e não podem ser usadas como construtoras (new).', exemplo: 'const somar = (a, b) => a + b;' },
          { titulo: 'Parâmetros e argumentos', descricao: 'Funções podem receber parâmetros que são mapeados para argumentos na chamada. Se você passar mais argumentos que parâmetros, os extras são ignorados (mas acessíveis via arguments object ou rest params). Se passar menos, os faltantes são undefined. Parâmetros podem ter valores padrão: function saudar(nome = "Visitante"). O objeto arguments (disponível em functions tradicionais) contém todos os argumentos em array-like.', exemplo: 'function log(nome = "Visitante") { console.log(nome); }' }
        ],
        exercicio: 'Crie três versões de uma função que dobra um número: function declaration, function expression e arrow function. Compare os resultados.',
        codigoInicial: `// Function declaration\nfunction dobrarDeclaracao(n) {\n  return n * 2;\n}\n\n// Function expression\nconst dobrarExpressao = function(n) {\n  return n * 2;\n};\n\n// Arrow function\nconst dobrarArrow = n => n * 2;\n\nconsole.log(dobrarDeclaracao(5));\nconsole.log(dobrarExpressao(5));\nconsole.log(dobrarArrow(5));`,
        codigoSolucao: `// Function declaration (hoisted)\nfunction dobrarDeclaracao(n) {\n  return n * 2;\n}\n\n// Function expression (não hoisted)\nconst dobrarExpressao = function(n) {\n  return n * 2;\n};\n\n// Arrow function (concisa, sem this próprio)\nconst dobrarArrow = n => n * 2;\n\nconsole.log(dobrarDeclaracao(5)); // 10\nconsole.log(dobrarExpressao(5));  // 10\nconsole.log(dobrarArrow(5));      // 10\n\n// Arrow com corpo de bloco\nconst dobrarArrowBloco = n => {\n  const resultado = n * 2;\n  return resultado;\n};`
      },
      {
        dia: 2,
        titulo: 'Parâmetros Avançados',
        teoria: 'JavaScript oferece recursos avançados para parâmetros: valores padrão, rest parameters (...args) e destructuring. Esses recursos tornam as funções mais flexíveis.',
        topicos: [
          { titulo: 'Valores padrão (default params)', descricao: 'Parâmetros podem ter valores padrão que são usados se o argumento for undefined: function cumprimentar(nome = "Visitante", saudacao = "Olá"). O valor padrão é avaliado apenas quando necessário (lazy evaluation). Você pode usar expressões: function rand(max = Math.random() * 10). Valores padrão também podem referenciar parâmetros anteriores: function(a, b = a * 2). Diferente de outros padrões, null não ativa o valor padrão — apenas undefined.', exemplo: 'function soma(a, b = a * 2) { return a + b; }' },
          { titulo: 'Rest parameters (...args)', descricao: 'O rest parameter (...nomeDoArray) captura argumentos extras em um array real: function somarTudo(...numeros) { return numeros.reduce((a, b) => a + b) }. Diferente do objeto arguments, rest params criam um array de verdade com métodos como map, filter, reduce. Só pode haver um rest parameter e ele deve ser o último. Rest params são essenciais para funções que aceitam número variável de argumentos.', exemplo: 'function media(...nums) { return nums.reduce((a,b) => a+b) / nums.length; }' },
          { titulo: 'Destructuring em parâmetros', descricao: 'Você pode desestruturar objetos e arrays diretamente nos parâmetros: function criarUsuario({ nome, idade, email }) { /* usa nome, idade, email diretamente */ }. Isso elimina a necessidade de acessar obj.propriedade dentro da função. Você também pode definir valores padrão na desestruturação: function({ nome = "Anônimo" } = {}). Destructuring em parâmetros é muito usado em React (props) e APIs.', exemplo: 'function criarUsuario({ nome, idade }) { console.log(nome, idade); }' },
          { titulo: 'Objeto arguments', descricao: 'arguments é um objeto array-like disponível em functions tradicionais (não arrow) que contém todos os argumentos passados. Ele tem length e índices numéricos, mas não tem métodos de array como map ou forEach. Para convertê-lo em array: Array.from(arguments) ou [...arguments]. arguments é útil quando você não sabe quantos parâmetros virão, mas rest params são preferíveis em código moderno.', exemplo: 'function args() { console.log(arguments.length); }' }
        ],
        exercicio: 'Crie uma função que aceita múltiplos números com rest params e retorna a média. Crie outra que usa destructuring para extrair dados de um objeto usuário.',
        codigoInicial: `function calcularMedia(...numeros) {\n  // Calcule a média\n}\n\nconsole.log(calcularMedia(10, 8, 9));\nconsole.log(calcularMedia(7, 6, 8, 9, 10));\n\n// Destructuring\nfunction exibirUsuario({ nome, idade, cidade }) {\n  // Exiba os dados\n}\n\nexibirUsuario({ nome: "Ana", idade: 25, cidade: "SP", telefone: "123" });`,
        codigoSolucao: `function calcularMedia(...numeros) {\n  if (numeros.length === 0) return 0;\n  const soma = numeros.reduce((a, b) => a + b);\n  return soma / numeros.length;\n}\n\nconsole.log(calcularMedia(10, 8, 9));      // 9\nconsole.log(calcularMedia(7, 6, 8, 9, 10)); // 8\n\nfunction exibirUsuario({ nome, idade, cidade }) {\n  console.log(\`Nome: \${nome}, Idade: \${idade}, Cidade: \${cidade}\`);\n}\n\nexibirUsuario({ nome: "Ana", idade: 25, cidade: "SP", telefone: "123" });\n// Só extrai nome, idade e cidade — telefone é ignorado`
      },
      {
        dia: 3,
        titulo: 'Escopo, Hoisting e Closure',
        teoria: 'Escopo determina onde variáveis são acessíveis. Hoisting é o comportamento de elevar declarações. Closures são funções que "lembram" o escopo onde foram criadas.',
        topicos: [
          { titulo: 'Escopo global, function e block', descricao: 'Variáveis declaradas fora de qualquer função estão no escopo global (acessíveis em todo o código). Variáveis com var têm escopo de função — visíveis dentro da função inteira. Variáveis com let e const têm escopo de bloco ({}) — visíveis apenas dentro das { } onde foram declaradas. Blocos incluem if, for, while, etc. O escopo de bloco é uma das maiores vantagens de let/const sobre var.', exemplo: 'if (true) { let x = 10; } // x fora da erro' },
          { titulo: 'Hoisting (elevação)', descricao: 'Hoisting é o comportamento do JavaScript de mover declarações para o topo do escopo. Function declarations são hoisted completamente (podem ser usadas antes da declaração). Declarações com var são hoisted mas inicializadas como undefined (acessar antes causa undefined, não error). let e const são hoisted mas ficam na Temporal Dead Zone (TDZ) — acessar antes causa ReferenceError. Sempre declare variáveis no topo do escopo para evitar confusão.', exemplo: 'console.log(x); // undefined\nvar x = 5;' },
          { titulo: 'Closure na prática', descricao: 'Closure é quando uma função "lembra" do escopo onde foi criada, mesmo após esse escopo ter sido executado. Exemplo clássico: function criarContador() { let count = 0; return function() { return ++count; } }. Cada chamada de criarContador() cria um novo closure independente. Closures são usados para: encapsulamento (dados privados), factories de funções, callbacks com estado, e memorização (cache).', exemplo: 'const contador = (() => { let c = 0; return () => ++c; })();' },
          { titulo: 'Closures e loop (problema clássico)', descricao: 'Um problema famoso: for (var i = 0; i < 3; i++) { setTimeout(() => console.log(i), 1000) } imprime 3, 3, 3 (não 0, 1, 2). Isso porque var tem escopo de função e o closure captura a mesma variável i. Soluções: usar let (escopo de bloco) ou um IIFE que captura cada valor: for (var i = 0; i < 3; i++) { (function(j) { setTimeout(() => console.log(j), 1000) })(i) }.', exemplo: 'for (var i = 0; i < 3; i++) { setTimeout(() => console.log(i), 1000); } // 3, 3, 3' }
        ],
        exercicio: 'Crie uma função criarContador que retorna um objeto com métodos incrementar, decrementar e getValor, todos compartilhando o mesmo closure.',
        codigoInicial: `function criarContador(valorInicial = 0) {\n  // Implemente os métodos\n  return {\n    incrementar() {},\n    decrementar() {},\n    getValor() {}\n  };\n}\n\nconst contador = criarContador(10);\ncontador.incrementar();\ncontador.incrementar();\ncontador.decrementar();\nconsole.log(contador.getValor());`,
        codigoSolucao: `function criarContador(valorInicial = 0) {\n  let valor = valorInicial;\n  \n  return {\n    incrementar() { valor++; },\n    decrementar() { valor--; },\n    getValor() { return valor; }\n  };\n}\n\nconst contador = criarContador(10);\ncontador.incrementar();\ncontador.incrementar();\ncontador.decrementar();\nconsole.log(contador.getValor()); // 11\n\n// Cada instância tem seu próprio closure\nconst outro = criarContador(0);\noutro.incrementar();\nconsole.log(outro.getValor());     // 1\nconsole.log(contador.getValor());  // 11 (independente)`
      },
      {
        dia: 4,
        titulo: 'Higher-Order Functions e Callbacks',
        teoria: 'Higher-order functions (HOF) são funções que recebem ou retornam outras funções. Callbacks são funções passadas como argumento para serem executadas depois.',
        topicos: [
          { titulo: 'Funções como valor', descricao: 'Em JavaScript, funções são cidadãs de primeira classe (first-class citizens) — podem ser atribuídas a variáveis, passadas como argumento e retornadas por outras funções. Isso permite programação funcional e composição. Exemplo: const operacoes = { somar: (a, b) => a + b, subtrair: (a, b) => a - b }. Você pode armazenar funções em arrays, objetos e passá-las dinamicamente.', exemplo: 'const ops = { somar: (a, b) => a + b };' },
          { titulo: 'Callbacks', descricao: 'Callback é uma função passada como argumento para outra função, que a executa depois. Exemplo: array.map(x => x * 2) — a arrow function é o callback. Callbacks são usados em: métodos de array (map, filter, forEach), eventos (addEventListener("click", callback)), temporizadores (setTimeout(callback, 1000)), e operações assíncronas. Callbacks podem ser síncronos (executados imediatamente) ou assíncronos (executados depois).', exemplo: '[1, 2, 3].map(x => x * 2); // [2, 4, 6]' },
          { titulo: 'Funções que retornam funções', descricao: 'Uma HOF pode retornar uma nova função, criando factories especializadas. Exemplo: function multiplicador(n) { return function(x) { return x * n } }. const dobrar = multiplicador(2); const triplicar = multiplicador(3). Isso é um closure que "configura" a função. É útil para criar funções com comportamento parcialmente aplicado, validações customizadas e middlewares.', exemplo: 'const mult = (n) => (x) => x * n;\nconst dobrar = mult(2);' },
          { titulo: 'Composição de funções', descricao: 'Composição é combinar funções para criar pipelines: const resultado = funcaoC(funcaoB(funcaoA(dado))). Bibliotecas como lodash têm _.flow() para composição. Em JavaScript moderno, você pode usar reduce: const pipe = (...fns) => (valor) => fns.reduce((acc, fn) => fn(acc), valor). Composição é um conceito central em programação funcional e é muito usada em Redux (middlewares) e dados transformados.', exemplo: 'const pipe = (...fns) => (x) => fns.reduce((v, f) => f(v), x);' }
        ],
        exercicio: 'Crie uma função operacao que recebe um callback e dois números. Depois crie callbacks para somar, subtrair e multiplicar. Crie também uma função multiplicador que retorna uma função.',
        codigoInicial: `function operacao(callback, a, b) {\n  return callback(a, b);\n}\n\n// Crie os callbacks\nconst somar = (a, b) => a + b;\nconst subtrair = (a, b) => a - b;\n\nconsole.log(operacao(somar, 10, 5));\nconsole.log(operacao(subtrair, 10, 5));\n\n// Factory de funções\nfunction multiplicador(n) {\n  return function(x) {\n    return x * n;\n  };\n}`,
        codigoSolucao: `function operacao(callback, a, b) {\n  return callback(a, b);\n}\n\nconst somar = (a, b) => a + b;\nconst subtrair = (a, b) => a - b;\nconst multiplicar = (a, b) => a * b;\n\nconsole.log(operacao(somar, 10, 5));       // 15\nconsole.log(operacao(subtrair, 10, 5));    // 5\nconsole.log(operacao(multiplicar, 10, 5)); // 50\n\n// Factory\nfunction multiplicador(n) {\n  return function(x) {\n    return x * n;\n  };\n}\n\nconst dobrar = multiplicador(2);\nconst triplicar = multiplicador(3);\nconsole.log(dobrar(10));    // 20\nconsole.log(triplicar(10));  // 30\n\n// setTimeout callback\nsetTimeout(() => console.log("Passou 1 segundo"), 1000);`
      },
      {
        dia: 5,
        titulo: 'Recursão e IIFE',
        teoria: 'Recursão é quando uma função chama a si mesma. IIFE (Immediately Invoked Function Expression) é uma função que executa imediatamente após ser definida.',
        topicos: [
          { titulo: 'Recursão básica', descricao: 'Recursão é uma função que chama a si mesma até atingir um caso base. Exemplo: function fatorial(n) { if (n <= 1) return 1; return n * fatorial(n - 1) }. Toda recursão precisa de: caso base (para parar) e passo recursivo (chamada com problema menor). Recursão é mais elegante que loops para problemas como árvores, fractais e algoritmos de busca. O risco é estouro de pilha (stack overflow) com muitas chamadas.', exemplo: 'function fat(n) { return n <= 1 ? 1 : n * fat(n - 1); }' },
          { titulo: 'Recursão vs loops', descricao: 'Tudo que pode ser feito com recursão pode ser feito com loops (e vice-versa). Recursão é mais declarativa e legível para problemas naturalmente recursivos (árvores, JSON, navegação de pastas). Loops são mais eficientes em memória (não criam pilha) e geralmente mais rápidos. Para recursão profunda, considere otimização tail call (se o ambiente suportar). Em JavaScript moderno, prefira recursão quando o código ficar mais claro.', exemplo: 'function fib(n) { return n <= 1 ? n : fib(n-1) + fib(n-2); }' },
          { titulo: 'IIFE (Immediately Invoked Function Expression)', descricao: 'IIFE é uma função que executa no momento da definição: (function() { console.log("Executou!") })(). Os parênteses envolvendo a função a tornam uma expression, e os () finais a invocam. IIFEs criam um escopo isolado — útil antes de let/const existirem para evitar poluição global. Ainda usadas em: módulos (padrão Module Pattern), async IIFEs: (async () => { await fetch(...) })(), e em bibliotecas para isolar código.', exemplo: '(function() { console.log("IIFE"); })();' },
          { titulo: 'Module Pattern com IIFE', descricao: 'O Module Pattern usa IIFE para criar módulos com dados privados e API pública: const modulo = (function() { let privado = "secreto"; return { getPrivado: () => privado } })(). O closure do IIFE protege privado do acesso externo. Embora ES Modules sejam o padrão hoje, o Module Pattern ainda é útil para scripts que não usam bundlers. É a base de bibliotecas como jQuery e Lodash (originalmente).', exemplo: 'const modulo = (() => { let p = "secreto"; return { get: () => p }; })();' }
        ],
        exercicio: 'Implemente fatorial recursivamente e crie um contador de visualizações usando IIFE que mantém o estado privado.',
        codigoInicial: `function fatorial(n) {\n  // Implemente recursivamente\n}\n\nconsole.log("Fatorial de 5:", fatorial(5));\nconsole.log("Fatorial de 0:", fatorial(0));\n\n// Contador com IIFE\nconst contadorVisitas = (function() {\n  // Implemente contagem privada\n})();\n\nconsole.log(contadorVisitas.visitar());\nconsole.log(contadorVisitas.visitar());\nconsole.log(contadorVisitas.getTotal());`,
        codigoSolucao: `function fatorial(n) {\n  if (n <= 1) return 1;\n  return n * fatorial(n - 1);\n}\n\nconsole.log("Fatorial de 5:", fatorial(5)); // 120\nconsole.log("Fatorial de 0:", fatorial(0)); // 1\n\n// Fibonacci recursivo\nfunction fibonacci(n) {\n  if (n <= 1) return n;\n  return fibonacci(n - 1) + fibonacci(n - 2);\n}\n\nconsole.log("Fibonacci 7:", fibonacci(7)); // 13\n\n// Contador com IIFE\nconst contadorVisitas = (function() {\n  let total = 0;\n  return {\n    visitar: () => ++total,\n    getTotal: () => total\n  };\n})();\n\nconsole.log(contadorVisitas.visitar());  // 1\nconsole.log(contadorVisitas.visitar());  // 2\nconsole.log(contadorVisitas.getTotal()); // 2`
      }
    ],
    miniProjeto: {
      titulo: 'Biblioteca de Funções Utilitárias',
      descricao: 'Crie uma biblioteca com funções usando closures, callbacks e HOFs',
      requisitos: [
        'Função memoize com closure para cache',
        'Factory de funções (multiplicador, etc.)',
        'Função pipe/compose para composição',
        'Contador com estado privado via IIFE',
        'Função map personalizada (callback)'
      ]
    }
  },
  {
    semana: 4,
    titulo: 'Arrays',
    descricao: 'Domine arrays e seus métodos: push, pop, map, filter, reduce, split, slice, splice e muito mais',
    instrucoes: 'Esta semana é intensa e prática — os métodos de array são as ferramentas mais usadas no dia a dia do JavaScript. Comece pelos fundamentos (push, pop, shift, unshift) e depois avance para transformação (map, filter, reduce). Pratique o encadeamento de métodos: dados.filter().map().reduce(). O mini-projeto final vai exigir que você combine todos os métodos para processar dados reais. Dedique tempo extra ao reduce — é o mais versátil e também o mais desafiador.',
    dias: [
      {
        dia: 1,
        titulo: 'Fundamentos de Arrays',
        teoria: 'Arrays armazenam coleções ordenadas de dados. JavaScript oferece métodos para adicionar, remover e acessar elementos nas pontas: push, pop, shift, unshift.',
        topicos: [
          { titulo: 'Criação e acesso', descricao: 'Arrays podem ser criados com colchetes: const arr = [1, 2, 3] ou new Array(3). A propriedade length retorna o número de elementos. Acesse elementos por índice: arr[0] (primeiro), arr[arr.length - 1] (último). Índices negativos retornam undefined (diferente de Python/Slice). Você pode definir propriedades em índices: arr[10] = "pulo" — isso cria buracos (sparse arrays) com length = 11. Arrays podem conter qualquer tipo misturado.', exemplo: 'const arr = [1, 2, 3]; arr[0] // 1' },
          { titulo: 'push e pop', descricao: 'push(elemento) adiciona UM ou MAIS elementos ao FINAL do array e retorna o novo length. Exemplo: const nums = [1, 2]; nums.push(3) → [1, 2, 3], retorna 3. pop() remove e retorna o ÚLTIMO elemento: nums.pop() → retorna 3, array fica [1, 2]. pop() em array vazio retorna undefined. push/pop são eficientes O(1) — operam no final. São a base para implementar pilhas (stack): LIFO (Last In, First Out).', exemplo: 'const nums = [1]; nums.push(2); nums.pop() // 2' },
          { titulo: 'shift e unshift', descricao: 'unshift(elemento) adiciona ao INÍCIO do array e retorna o novo length. shift() remove e retorna o PRIMEIRO elemento. Exemplo: const nums = [2, 3]; nums.unshift(1) → [1, 2, 3]; nums.shift() → retorna 1, array fica [2, 3]. Diferente de push/pop, shift/unshift são O(n) — precisam reindexar todos os elementos. Evite shift/unshift em arrays grandes. Use push/pop quando possível, ou considere deque/lista ligada.', exemplo: 'const nums = [2, 3]; nums.unshift(1); nums.shift() // 1' },
          { titulo: 'length e índice negativo', descricao: 'A propriedade length é dinâmica: atribuir length = 0 limpa o array. length = 2 trunca para 2 elementos. Para acessar o último elemento sem saber o tamanho: arr[arr.length - 1]. Para índice negativo como em Python (arr[-1]), não funciona nativamente — use arr.at(-1) (ES2022). arr.at(-1) retorna o último, arr.at(-2) o penúltimo. at() também funciona com strings.', exemplo: '[1, 2, 3].at(-1) // 3' }
        ],
        exercicio: 'Crie um array vazio, adicione frutas com push, remova a primeira e a última, e exiba o array final.',
        codigoInicial: `const frutas = [];\n\n// Adicione frutas\nfrutas.push("Maçã");\nfrutas.push("Banana");\nfrutas.push("Uva");\nfrutas.push("Laranja");\nconsole.log("Após push:", frutas);\n\n// Remova a última\nconst ultima = frutas.pop();\nconsole.log("Removeu:", ultima);\n\n// Remova a primeira\nconst primeira = frutas.shift();\nconsole.log("Removeu:", primeira);\n\nconsole.log("Final:", frutas);\nconsole.log("Tamanho:", frutas.length);\nconsole.log("Último:", frutas[frutas.length - 1]);`,
        codigoSolucao: `const frutas = [];\n\nfrutas.push("Maçã", "Banana");     // push múltiplos\nfrutas.push("Uva", "Laranja");\nconsole.log("Após push:", frutas);  // ["Maçã", "Banana", "Uva", "Laranja"]\n\nconst ultima = frutas.pop();\nconsole.log("Removeu:", ultima);     // "Laranja"\n\nconst primeira = frutas.shift();\nconsole.log("Removeu:", primeira);   // "Maçã"\n\nconsole.log("Final:", frutas);       // ["Banana", "Uva"]\nconsole.log("Tamanho:", frutas.length); // 2\n\n// Último elemento sem pop\nconsole.log("Último:", frutas[frutas.length - 1]); // "Uva"\n\n// Usando at() moderno\nconsole.log("Primeiro:", frutas.at(0));  // "Banana"\nconsole.log("Último:", frutas.at(-1));   // "Uva"`
      },
      {
        dia: 2,
        titulo: 'Inserção, Remoção e Fatiamento',
        teoria: 'splice é o método mais versátil para modificar arrays (adiciona, remove, substitui). slice fatia sem modificar. concat, fill e flat complementam.',
        topicos: [
          { titulo: 'splice — o canivete suíço', descricao: 'splice(índice, quantidadeRemover, ...itensAdicionar) modifica o array original. Exemplos: arr.splice(1, 1) remove 1 elemento no índice 1. arr.splice(1, 0, "novo") adiciona sem remover. arr.splice(1, 1, "substituto") substitui. splice retorna um array com os elementos removidos. É o método mais flexível mas também o mais fácil de usar errado — entenda bem os 3 parâmetros.', exemplo: 'arr.splice(1, 0, "x") // adiciona "x" no índice 1' },
          { titulo: 'slice — fatiamento seguro', descricao: 'slice(início, fim) retorna uma cópia de uma parte do array SEM modificar o original. Aceita índices negativos: arr.slice(-2) retorna os últimos 2. slice() sem argumentos copia o array inteiro (shallow copy). slice(1) copia do índice 1 até o final. slice(1, 3) copia índices 1 e 2 (o fim é exclusivo). slice é usado para clonagem rápida e para obter sub-arrays sem efeitos colaterais.', exemplo: '[1, 2, 3].slice(0, 2) // [1, 2]' },
          { titulo: 'concat e spread', descricao: 'concat() junta arrays: arr1.concat(arr2, arr3) retorna um novo array. O spread operator (...) é mais moderno: [...arr1, ...arr2]. Spread também é usado para clonar arrays: const copia = [...original] (shallow copy). concat aceita valores avulsos: [1].concat(2, 3, [4]) = [1, 2, 3, 4]. Ambos não modificam os originais. Spread é geralmente preferido por ser mais legível e flexível.', exemplo: '[...[1, 2], ...[3, 4]] // [1, 2, 3, 4]' },
          { titulo: 'fill, flat e Array.isArray', descricao: 'fill(valor, início, fim) preenche o array com um valor: new Array(3).fill(0) = [0, 0, 0]. flat(profundidade) achata arrays aninhados: [1, [2, [3]]].flat(2) = [1, 2, 3]. flat() sem argumento achata 1 nível. flatMap é equivalente a map + flat(1) — útil para arrays de arrays. Array.isArray() verifica se um valor é array (typeof [] é "object" — não serve).', exemplo: '[1, [2, [3]]].flat(2) // [1, 2, 3]' }
        ],
        exercicio: 'Use splice para adicionar e remover elementos do meio de um array. Use slice para criar um sub-array. Use concat para juntar dois arrays.',
        codigoInicial: `const letras = ["a", "b", "c", "d", "e"];\n\n// Remova "c" com splice\n\n// Adicione "x" entre "a" e "b"\n\n// Crie sub-array com os 3 primeiros (slice)\n\n// Junte com outro array usando concat\nconst extras = ["f", "g"];`,
        codigoSolucao: `const letras = ["a", "b", "c", "d", "e"];\n\n// Remove "c" (índice 2)\nletras.splice(2, 1);\nconsole.log("Após remover c:", letras); // ["a", "b", "d", "e"]\n\n// Adiciona "x" entre "a" e "b" (índice 1, remover 0)\nletras.splice(1, 0, "x");\nconsole.log("Após adicionar x:", letras); // ["a", "x", "b", "d", "e"]\n\n// Substitui "d" por "z"\nletras.splice(3, 1, "z");\nconsole.log("Após substituir:", letras); // ["a", "x", "b", "z", "e"]\n\n// Sub-array dos 3 primeiros\nconst primeiros = letras.slice(0, 3);\nconsole.log("Primeiros 3:", primeiros); // ["a", "x", "b"]\n\n// Concatena\nconst extras = ["f", "g"];\nconst todos = letras.concat(extras);\nconsole.log("Concatenado:", todos);\n\n// Spread (alternativa moderna)\nconst todosSpread = [...letras, ...extras];`
      },
      {
        dia: 3,
        titulo: 'Transformação: map, filter e reduce',
        teoria: 'map, filter e reduce são os métodos mais poderosos para transformar dados em arrays. Eles formam a base da programação funcional em JavaScript.',
        topicos: [
          { titulo: 'map() — Transformar cada elemento', descricao: 'map(callback) cria um NOVO array com o resultado do callback para cada elemento. O callback recebe (elemento, índice, arrayOriginal). Exemplo: [1, 2, 3].map(x => x * 2) = [2, 4, 6]. map sempre retorna um array do MESMO tamanho. É ideal para: extrair propriedades (usuarios.map(u => u.nome)), converter tipos (strings.map(Number)), formatar dados. map NÃO modifica o original (imutabilidade).', exemplo: '[1, 2, 3].map(x => x * 2) // [2, 4, 6]' },
          { titulo: 'filter() — Selecionar elementos', descricao: 'filter(callback) cria um NOVO array com os elementos que satisfazem a condição (callback retorna true). Exemplo: [1, 2, 3, 4, 5].filter(x => x > 2) = [3, 4, 5]. O callback recebe (elemento, índice, array). filter pode retornar array vazio se nada satisfizer. É ideal para: buscar por condição, remover itens (filter com !), criar sub-conjuntos. Combinado com length, filter conta elementos: arr.filter(x => x > 0).length.', exemplo: '[1, 2, 3, 4].filter(x => x > 2) // [3, 4]' },
          { titulo: 'reduce() — Agregar em um valor', descricao: 'reduce(callback, valorInicial) reduz o array a um ÚNICO valor. O callback recebe (acumulador, elemento, índice, array). Exemplo: [1, 2, 3].reduce((acc, x) => acc + x, 0) = 6. reduce é versátil: somar, multiplicar, contar, agrupar, achatar arrays, criar objetos. Sem valorInicial, acc é o primeiro elemento e a iteração começa do segundo. Sempre forneça valorInicial para evitar erros com array vazio.', exemplo: '[1, 2, 3].reduce((a, b) => a + b, 0) // 6' },
          { titulo: 'Encadeamento de métodos', descricao: 'A verdadeira potência vem de encadear métodos: dados.filter(x => x.ativo).map(x => x.nome).reduce((acc, nome) => acc + ", " + nome, ""). Cada método retorna um novo array, permitindo pipelines de transformação. O encadeamento é mais legível que múltiplas variáveis intermediárias. Cuidado com performance em arrays muito grandes — cada método cria um novo array. Para dados enormes, considere transducers ou loops simples.', exemplo: 'arr.filter(x => x > 1).map(x => x * 2).reduce((a, b) => a + b)' }
        ],
        exercicio: 'Dado um array de produtos (nome, preco, categoria), use filter para selecionar eletrônicos, map para aplicar 10% de desconto, e reduce para calcular o total.',
        codigoInicial: `const produtos = [\n  { nome: "Notebook", preco: 3500, categoria: "eletronico" },\n  { nome: "Camiseta", preco: 50, categoria: "roupa" },\n  { nome: "Mouse", preco: 150, categoria: "eletronico" },\n  { nome: "Calça", preco: 120, categoria: "roupa" },\n  { nome: "Monitor", preco: 1800, categoria: "eletronico" }\n];\n\n// Filtre eletrônicos\n// Aplique 10% de desconto\n// Calcule o total`,
        codigoSolucao: `const produtos = [\n  { nome: "Notebook", preco: 3500, categoria: "eletronico" },\n  { nome: "Camiseta", preco: 50, categoria: "roupa" },\n  { nome: "Mouse", preco: 150, categoria: "eletronico" },\n  { nome: "Calça", preco: 120, categoria: "roupa" },\n  { nome: "Monitor", preco: 1800, categoria: "eletronico" }\n];\n\nconst eletronicosComDesconto = produtos\n  .filter(p => p.categoria === "eletronico")\n  .map(p => ({ ...p, preco: p.preco * 0.9 }));\n\nconsole.log("Eletrônicos com desconto:", eletronicosComDesconto);\n\nconst total = eletronicosComDesconto.reduce((acc, p) => acc + p.preco, 0);\nconsole.log("Total: R$", total.toFixed(2));\n\n// Extra: agrupar por categoria\nconst agrupados = produtos.reduce((acc, p) => {\n  acc[p.categoria] = acc[p.categoria] || [];\n  acc[p.categoria].push(p);\n  return acc;\n}, {});\nconsole.log("Agrupados:", agrupados);`
      },
      {
        dia: 4,
        titulo: 'Busca, Iteração e Ordenação',
        teoria: 'forEach itera sem retornar. find/findIndex buscam elementos. some/every testam condições. sort ordena. includes verifica existência.',
        topicos: [
          { titulo: 'forEach — iterar sem retorno', descricao: 'forEach(callback) executa o callback para cada elemento, mas NÃO retorna nada (undefined). Use forEach quando o objetivo é executar um efeito colateral (imprimir, modificar elementos, enviar dados). Diferente de map, forEach não cria novo array. Não é encadeável (retorna undefined). Para acessar índice: arr.forEach((item, i) => console.log(i, item)). Não use await dentro de forEach — prefira for...of com async.', exemplo: 'arr.forEach(x => console.log(x))' },
          { titulo: 'find, findIndex e includes', descricao: 'find(callback) retorna o PRIMEIRO elemento que satisfaz a condição, ou undefined. findIndex(callback) retorna o índice ou -1. includes(valor) verifica se o valor existe no array (usando ===). Exemplo: usuarios.find(u => u.id === 5). includes não aceita callback — para objetos, use find/some. indexOf(valor) também busca valor mas sem callback. Para arrays de objetos, find é mais adequado.', exemplo: '[1, 2, 3].find(x => x > 1) // 2' },
          { titulo: 'some e every', descricao: 'some(callback) retorna true se PELO MENOS UM elemento satisfaz a condição (short-circuit: para no primeiro true). every(callback) retorna true se TODOS os elementos satisfazem (short-circuit: para no primeiro false). Exemplos: numeros.some(x => x > 10), usuarios.every(u => u.ativo). some em array vazio retorna false, every retorna true (vacuous truth). Úteis para validação e permissões.', exemplo: '[1, 2, 3].some(x => x > 2) // true' },
          { titulo: 'sort — ordenar arrays', descricao: 'sort() converte elementos para string e ordena lexicograficamente — [1, 2, 10, 20].sort() = [1, 10, 2, 20] (erro comum!). Para ordenação numérica: arr.sort((a, b) => a - b) (crescente) ou (b - a) (decrescente). Para strings: arr.sort((a, b) => a.localeCompare(b)). sort MODIFICA o array original — faça uma cópia: [...arr].sort(). Para objetos: arr.sort((a, b) => a.preco - b.preco).', exemplo: '[3, 1, 2].sort((a, b) => a - b) // [1, 2, 3]' }
        ],
        exercicio: 'Dado um array de usuários, use find para achar um por ID, some para verificar se existe maior de idade, filter para menores, sort por nome e idade.',
        codigoInicial: `const usuarios = [\n  { id: 1, nome: "Ana", idade: 25 },\n  { id: 2, nome: "Bruno", idade: 17 },\n  { id: 3, nome: "Carla", idade: 30 },\n  { id: 4, nome: "Daniel", idade: 15 }\n];\n\n// Encontre o usuário com id = 3\n// Verifique se existe algum menor de idade\n// Filtre os maiores de idade\n// Ordene por nome`,
        codigoSolucao: `const usuarios = [\n  { id: 1, nome: "Ana", idade: 25 },\n  { id: 2, nome: "Bruno", idade: 17 },\n  { id: 3, nome: "Carla", idade: 30 },\n  { id: 4, nome: "Daniel", idade: 15 }\n];\n\n// Find\nconst usuario3 = usuarios.find(u => u.id === 3);\nconsole.log("Usuário ID 3:", usuario3); // { id: 3, nome: "Carla", idade: 30 }\n\n// Some\nconst temMenor = usuarios.some(u => u.idade < 18);\nconsole.log("Tem menor:", temMenor); // true\n\n// Every\nconst todosAdultos = usuarios.every(u => u.idade >= 18);\nconsole.log("Todos adultos:", todosAdultos); // false\n\n// Filter\nconst maiores = usuarios.filter(u => u.idade >= 18);\nconsole.log("Maiores:", maiores.map(u => u.nome)); // ["Ana", "Carla"]\n\n// Sort por nome\nconst porNome = [...usuarios].sort((a, b) => a.nome.localeCompare(b.nome));\nconsole.log("Por nome:", porNome.map(u => u.nome));\n\n// Sort por idade\nconst porIdade = [...usuarios].sort((a, b) => a.idade - b.idade);\nconsole.log("Por idade:", porIdade.map(u => \`\${u.nome} (\${u.idade})\`));`
      },
      {
        dia: 5,
        titulo: 'Mini-Projeto: Processador de Dados',
        teoria: 'Combine todos os métodos de array em um pipeline de processamento de dados. Transforme, filtre, agregue e ordene dados como em aplicações reais.',
        topicos: [
          { titulo: 'Pipeline de dados', descricao: 'Um pipeline de dados é uma sequência de transformações: buscar → filtrar → transformar → agregar → apresentar. Cada etapa usa um método de array. Exemplo: fetch("/api/produtos").then(r => r.json()).then(dados => dados.filter(p => p.ativo).map(p => ({...p, precoComDesconto})).sort((a, b) => a.preco - b.preco)). Esse padrão é onipresente em aplicações.', exemplo: 'dados.filter(p => p.ativo).map(p => p.nome).sort()' },
          { titulo: 'split e join em dados', descricao: 'split divide strings em arrays — útil para processar entradas: "nome,email,idade".split(","). join faz o oposto — cria strings a partir de arrays: dados.map(u => u.nome).join(", "). A combinação split → processar → join é um padrão para processamento de texto, CSV, tags, e formatação de saída. split também é usado com regex: texto.split(/\\s+/) para palavras.', exemplo: '"a,b,c".split(",").join("-") // "a-b-c"' },
          { titulo: 'flatMap e reduce avançado', descricao: 'flatMap callback executa map e depois flat(1) automaticamente. Exemplo: palavras.map(p => p.split("")).flat() = palavras.flatMap(p => p.split("")). reduce avançado: agrupar por categoria, criar lookup tables, calcular estatísticas (média, mediana, moda). reduce pode construir objetos, Map, Set, ou qualquer estrutura. Dominar reduce é dominar transformação de dados.', exemplo: 'palavras.flatMap(p => p.split(""))' }
        ],
        exercicio: 'Processe um CSV de vendas: split para parsear, filter para selecionar, map para transformar, reduce para totalizar, sort para ordenar.',
        codigoInicial: `const csv = \`Produto,Preco,Categoria,Quantidade\nNotebook,3500,Eletronico,2\nCamiseta,50,Roupa,5\nMouse,150,Eletronico,10\nCalça,120,Roupa,3\nMonitor,1800,Eletronico,1\`;\n\n// Parse o CSV com split\n// Filtre apenas eletrônicos\n// Calcule o valor total (preco * quantidade) para cada item\n// Calcule o total geral com reduce\n// Ordene por valor total decrescente`,
        codigoSolucao: `const csv = \`Produto,Preco,Categoria,Quantidade\nNotebook,3500,Eletronico,2\nCamiseta,50,Roupa,5\nMouse,150,Eletronico,10\nCalça,120,Roupa,3\nMonitor,1800,Eletronico,1\`;\n\n// Parse CSV\nconst linhas = csv.split("\\n");\nconst cabecalho = linhas[0].split(",");\nconst dados = linhas.slice(1).map(linha => {\n  const cols = linha.split(",");\n  return {\n    produto: cols[0],\n    preco: Number(cols[1]),\n    categoria: cols[2],\n    quantidade: Number(cols[3])\n  };\n});\n\nconsole.log("Dados parseados:", dados);\n\n// Eletrônicos com valor total\nconst eletronicos = dados\n  .filter(item => item.categoria === "Eletronico")\n  .map(item => ({\n    ...item,\n    valorTotal: item.preco * item.quantidade\n  }))\n  .sort((a, b) => b.valorTotal - a.valorTotal);\n\nconsole.log("Eletrônicos ordenados:", eletronicos);\n\n// Total geral\nconst totalGeral = dados.reduce((acc, item) => acc + item.preco * item.quantidade, 0);\nconsole.log("Total geral: R$", totalGeral.toFixed(2));\n\n// Agrupar por categoria\nconst porCategoria = dados.reduce((acc, item) => {\n  acc[item.categoria] = (acc[item.categoria] || 0) + item.preco * item.quantidade;\n  return acc;\n}, {});\nconsole.log("Total por categoria:", porCategoria);`
      }
    ],
    miniProjeto: {
      titulo: 'Processador de Planilha CSV',
      descricao: 'Parseie CSV com split, transforme com map/filter, calcule totais com reduce',
      requisitos: [
        'Parsear CSV usando split e map',
        'Filtrar dados por categoria',
        'Calcular subtotais com map',
        'Totalizar com reduce',
        'Ordenar resultados com sort'
      ]
    }
  },
  {
    semana: 5,
    titulo: 'Objetos',
    descricao: 'Aprenda tudo sobre objetos: criação, propriedades, métodos, this, destructuring e manipulação',
    instrucoes: 'Objetos são a estrutura de dados mais importante do JavaScript. Comece com literais e notação de propriedades, depois explore o comportamento do this, que é uma das maiores fontes de confusão. Os métodos estáticos de Object são essenciais para manipulação de dados. Destructuring e spread vão transformar a forma como você escreve código. Pratique bastante e faça os exercícios com atenção — objetos são a base de JSON, APIs e React.',
    dias: [
      {
        dia: 1,
        titulo: 'Fundamentos de Objetos',
        teoria: 'Objetos em JavaScript são coleções dinâmicas de propriedades (chave-valor). Podem ser criados com literais, new Object() ou funções construtoras.',
        topicos: [
          { titulo: 'Literais de objeto', descricao: 'A forma mais comum: const usuario = { nome: "Ana", idade: 25, "ultimo-acesso": "2024-01-01" }. Chaves são strings (ou Symbols). Se a chave não tem caracteres especiais, as aspas são opcionais. Shorthand: se a variável tem o mesmo nome da chave, { nome, idade } é equivalente a { nome: nome, idade: idade }. Métodos shorthand: { saudar() { return "Olá" } } em vez de { saudar: function() { return "Olá" } }.', exemplo: 'const u = { nome: "Ana", idade: 25 }' },
          { titulo: 'Acesso a propriedades', descricao: 'Duas formas: dot notation (obj.propriedade) e bracket notation (obj["propriedade"]). Dot é mais legível mas não funciona com chaves especiais (com espaços, hífens, ou dinâmicas). Bracket aceita expressões: const chave = "nome"; obj[chave]. Bracket também funciona com números: arr[0]. Para verificar se propriedade existe: "propriedade" in obj ou obj.hasOwnProperty("propriedade"). Acessar propriedade inexistente retorna undefined.', exemplo: 'obj["nome"]; obj.nome' },
          { titulo: 'Adicionar, modificar e deletar', descricao: 'Propriedades podem ser adicionadas ou modificadas com atribuição: obj.novaProp = "valor". Para deletar: delete obj.propriedade (retorna true se bem-sucedido). Propriedades podem ser dinâmicas: const chave = "email"; obj[chave] = "a@b.com". Objetos em JavaScript são mutáveis por padrão. Para congelar: Object.freeze(obj) (impede alterações) ou Object.seal(obj) (impede adições/remoções mas permite modificações).', exemplo: 'obj.nova = "valor"; delete obj.velha' },
          { titulo: 'Iteração sobre objetos', descricao: 'for...in itera sobre as chaves enumeráveis (incluindo herdadas). Object.keys(obj) retorna array de chaves próprias. Object.values(obj) retorna array de valores. Object.entries(obj) retorna array de [chave, valor]. for...of com Object.entries: for (const [chave, valor] of Object.entries(obj)). Esses métodos são preferíveis a for...in por serem mais previsíveis.', exemplo: 'Object.keys(obj) // ["nome", "idade"]' }
        ],
        exercicio: 'Crie um objeto carro com marca, modelo, ano e um método exibirInfo. Adicione uma nova propriedade cor. Delete ano. Itere sobre as chaves.',
        codigoInicial: `const carro = {\n  marca: "Toyota",\n  modelo: "Corolla",\n  ano: 2024,\n  exibirInfo() {\n    // Retorne string com informações\n  }\n};\n\nconsole.log(carro.exibirInfo());\n\n// Adicione cor\n// Delete ano\n// Liste chaves e valores`,
        codigoSolucao: `const carro = {\n  marca: "Toyota",\n  modelo: "Corolla",\n  ano: 2024,\n  exibirInfo() {\n    return \`\${this.marca} \${this.modelo} - \${this.ano}\`;\n  }\n};\n\nconsole.log(carro.exibirInfo()); // "Toyota Corolla - 2024"\n\n// Adicionar\ncarro.cor = "Prata";\ncarro["portas"] = 4;\n\n// Deletar\ndelete carro.ano;\n\nconsole.log("Chaves:", Object.keys(carro));   // ["marca", "modelo", "exibirInfo", "cor", "portas"]\nconsole.log("Valores:", Object.values(carro));\nconsole.log("Entries:", Object.entries(carro));\n\n// Iterar\nfor (const [chave, valor] of Object.entries(carro)) {\n  console.log(\`\${chave}: \${valor}\`);\n}`
      },
      {
        dia: 2,
        titulo: 'this, bind, call e apply',
        teoria: 'this em JavaScript é determinado por COMO a função é chamada, não onde é definida. bind, call e apply controlam explicitamente o valor de this.',
        topicos: [
          { titulo: 'Comportamento do this', descricao: 'Em funções tradicionais, this depende do contexto de chamada: em método de objeto, this é o objeto; em função solta, this é global (window/globalThis) ou undefined em strict mode. Em arrow functions, this é LEXICAL (herda do escopo pai). Em event listeners, this é o elemento. Em construtores (new), this é a nova instância. Esse comportamento dinâmico é fonte de muitos bugs — entender o this é essencial.', exemplo: 'obj.metodo() // this é o obj' },
          { titulo: 'bind() — fixar this', descricao: 'bind(thisArg) cria uma NOVA função com o this permanentemente fixado. Exemplo: const saudar = pessoa.saudar.bind(outraPessoa). bind não executa a função — apenas retorna uma nova com this vinculado. bind também permite fixar argumentos (partial application): const somar5 = somar.bind(null, 5). bind é muito usado em callbacks para preservar o contexto: onClick = this.handleClick.bind(this).', exemplo: 'fn.bind(outroObj)' },
          { titulo: 'call() e apply()', descricao: 'call(thisArg, arg1, arg2, ...) EXECUTA a função com this específico e argumentos individuais. apply(thisArg, [args]) faz o mesmo com array de argumentos. Exemplo: saudar.call(pessoa, "Olá", "Bem-vindo"). apply é útil quando os argumentos já estão em array: Math.max.apply(null, [1, 5, 3]). Em ES6+, apply é menos necessário com spread: Math.max(...[1, 5, 3]). call ainda é usado para herança de construtores.', exemplo: 'fn.call(obj, 1, 2)' },
          { titulo: 'Arrow functions e this', descricao: 'Arrow functions NÃO têm seu próprio this — elas herdam do escopo onde foram definidas (lexical binding). Isso as torna ideais para callbacks: setTimeout(() => { console.log(this.nome) }, 1000) — o this é do escopo pai, não de quem chama setTimeout. Arrow functions também não têm arguments, super ou new.target. Não podem ser usadas como construtoras (new). Métodos de objeto com arrow function NÃO acessam o objeto com this.', exemplo: 'setTimeout(() => this.x++, 1000)' }
        ],
        exercicio: 'Crie um objeto pessoa com nome e um método saudar. Use bind para criar uma nova função com this diferente. Use call para executar com outro objeto.',
        codigoInicial: `const pessoa1 = { nome: "Ana", saudar() { return \`Olá, sou \${this.nome}\`; } };\nconst pessoa2 = { nome: "Bruno" };\n\nconsole.log(pessoa1.saudar());\n\n// Use call para pessoa1.saudar executar com pessoa2\n\n// Use bind para criar função fixada\nconst saudarBruno = pessoa1.saudar.bind(pessoa2);\nconsole.log(saudarBruno());`,
        codigoSolucao: `const pessoa1 = { nome: "Ana", saudar() { return \`Olá, sou \${this.nome}\`; } };\nconst pessoa2 = { nome: "Bruno" };\n\nconsole.log(pessoa1.saudar());           // "Olá, sou Ana"\nconsole.log(pessoa1.saudar.call(pessoa2)); // "Olá, sou Bruno"\nconsole.log(pessoa1.saudar.apply(pessoa2)); // "Olá, sou Bruno"\n\nconst saudarBruno = pessoa1.saudar.bind(pessoa2);\nconsole.log(saudarBruno());              // "Olá, sou Bruno"\n\n// Exemplo: this com arrow function\nconst relogio = {\n  hora: 10,\n  iniciar() {\n    setInterval(() => {\n      this.hora++;\n      console.log(this.hora);\n    }, 1000);\n  }\n};\n// relogio.iniciar(); // this.hora funciona porque arrow herda this do método iniciar`
      },
      {
        dia: 4,
        titulo: 'Destructuring e Spread',
        teoria: 'Destructuring extrai valores de objetos/arrays em variáveis individuais. Spread expande iteráveis em elementos individuais. São recursos modernos essenciais.',
        topicos: [
          { titulo: 'Destructuring de objetos', descricao: 'Extrai propriedades para variáveis: const { nome, idade } = usuario. Renomear: const { nome: nomeCompleto } = usuario. Valores padrão: const { cidade = "Desconhecida" } = usuario. Aninhado: const { endereco: { rua } } = usuario. Destructuring é muito usado em: parâmetros de função, importações, React props, e resposta de APIs. A sintaxe é intuitiva: o lado esquerdo "espelha" a estrutura do objeto.', exemplo: 'const { nome, idade } = usuario' },
          { titulo: 'Destructuring de arrays', descricao: 'Extrai elementos por posição: const [primeiro, segundo] = arr. Pular elementos: const [primeiro, , terceiro] = arr. Rest: const [primeiro, ...resto] = arr. Valores padrão: const [a = 1] = []. Troca de variáveis: [a, b] = [b, a] (sem variável temporária). Destructuring de strings: const [a, b] = "JS" (a = "J", b = "S"). Combinado com split: const [nome, email] = "João,joao@email.com".split(",").', exemplo: 'const [a, b] = [1, 2]' },
          { titulo: 'Spread operator (...)', descricao: 'Spread (...) expande elementos de um iterável. Em arrays: const copia = [...original]; const combinado = [...arr1, ...arr2]; const adicionar = [0, ...arr, 4]. Em objetos: const copiaObj = { ...original }; const merge = { ...obj1, ...obj2 } (propriedades do último sobrescrevem). Spread é SHALLOW — não copia objetos aninhados. É a forma mais comum de clonar arrays e objetos em código moderno.', exemplo: 'const clone = { ...original }' },
          { titulo: 'Rest pattern (...)', descricao: 'Rest coleta elementos residuais em um array ou objeto. Em arrays: const [primeiro, ...resto] = [1, 2, 3, 4] (resto = [2, 3, 4]). Em objetos: const { nome, ...dados } = usuario (dados contém o resto). Em funções: function(...args) { }. Rest sempre deve ser o ÚLTIMO. É diferente de spread: rest COLETA (lado esquerdo da atribuição), spread EXPANDE (lado direito).', exemplo: 'const { nome, ...resto } = obj' }
        ],
        exercicio: 'Use destructuring para extrair dados de um objeto usuário e um array de coordenadas. Use spread para clonar e mesclar objetos.',
        codigoInicial: `const usuario = {\n  nome: "Maria", idade: 30, email: "maria@email.com",\n  endereco: { cidade: "SP", estado: "SP" }\n};\n\nconst coordenadas = [10, 20, 30];\n\n// Destructuring de objeto\n// Destructuring de array\n// Rest em objeto\n// Spread para clonar`,
        codigoSolucao: `const usuario = {\n  nome: "Maria", idade: 30, email: "maria@email.com",\n  endereco: { cidade: "SP", estado: "SP" }\n};\n\nconst coordenadas = [10, 20, 30];\n\n// Destructuring objeto\nconst { nome, idade } = usuario;\nconsole.log(nome, idade); // "Maria" 30\n\n// Destructuring com rename e default\nconst { email: emailUser, telefone = "Não informado" } = usuario;\n\n// Destructuring aninhado\nconst { endereco: { cidade } } = usuario;\nconsole.log("Cidade:", cidade); // "SP"\n\n// Destructuring array\nconst [x, y, z] = coordenadas;\nconsole.log("Coordenadas:", x, y, z); // 10 20 30\n\n// Rest em objeto\nconst { nome: nome2, ...resto } = usuario;\nconsole.log("Resto:", resto); // { idade: 30, email: ..., endereco: ... }\n\n// Spread para clonar e mesclar\nconst clone = { ...usuario, ativo: true };\nconsole.log("Clone:", clone);\n\n// Troca de variáveis\nlet a = 1, b = 2;\n[a, b] = [b, a];\nconsole.log("Trocado:", a, b); // 2 1`
      },
      {
        dia: 5,
        titulo: 'Mini-Projeto: Gerenciador de Usuários',
        teoria: 'Combine todos os conceitos de objetos: criação, manipulação, destructuring, spread e métodos estáticos em um sistema de gerenciamento.',
        topicos: [
          { titulo: 'CRUD com objetos', descricao: 'CRUD (Create, Read, Update, Delete) é o padrão mais comum em aplicações. Create: criar objetos com spread/destructuring. Read: acessar propriedades com destructuring. Update: { ...usuario, nome: "Novo" }. Delete: delete usuario.prop ou criar novo objeto sem a propriedade usando destructuring com rest. Esse padrão imutável (sempre criar novos objetos) é a base do Redux e React.', exemplo: 'const atualizado = { ...usuario, nome: "Novo" }' },
          { titulo: 'Validação de dados', descricao: 'Valide objetos com: hasOwnProperty para verificar campos obrigatórios, typeof para tipos, condicionais para ranges. Use Object.keys para verificar campos extras não permitidos. Destructuring com valores padrão para campos opcionais. Spread para mesclar dados parciais com defaults. A validação robusta previne bugs e é essencial em formulários e APIs.', exemplo: 'if (!obj.nome) return "Nome obrigatório"' }
        ],
        exercicio: 'Crie funções para adicionar, listar, atualizar e deletar usuários em um array. Use destructuring e spread para manipulação imutável.',
        codigoInicial: `let usuarios = [\n  { id: 1, nome: "Ana", email: "ana@email.com" },\n  { id: 2, nome: "Bruno", email: "bruno@email.com" }\n];\n\nfunction adicionarUsuario(usuarios, novo) {\n  // Retorne novo array com usuário adicionado\n}\n\nfunction atualizarUsuario(usuarios, id, novosDados) {\n  // Use spread para atualizar\n}\n\nfunction deletarUsuario(usuarios, id) {\n  // Use filter para remover\n}`,
        codigoSolucao: `let usuarios = [\n  { id: 1, nome: "Ana", email: "ana@email.com" },\n  { id: 2, nome: "Bruno", email: "bruno@email.com" }\n];\n\nfunction adicionarUsuario(usuarios, novo) {\n  const id = Math.max(...usuarios.map(u => u.id), 0) + 1;\n  return [...usuarios, { id, ...novo }];\n}\n\nfunction atualizarUsuario(usuarios, id, novosDados) {\n  return usuarios.map(u =>\n    u.id === id ? { ...u, ...novosDados } : u\n  );\n}\n\nfunction deletarUsuario(usuarios, id) {\n  return usuarios.filter(u => u.id !== id);\n}\n\nusuarios = adicionarUsuario(usuarios, { nome: "Carla", email: "carla@email.com" });\nconsole.log("Adicionou:", usuarios);\n\nusuarios = atualizarUsuario(usuarios, 1, { email: "ana.novo@email.com" });\nconsole.log("Atualizou:", usuarios);\n\nusuarios = deletarUsuario(usuarios, 2);\nconsole.log("Deletou:", usuarios);`
      }
    ],
    miniProjeto: {
      titulo: 'Sistema de Gerenciamento de Usuários',
      descricao: 'CRUD completo com manipulação imutável de objetos',
      requisitos: [
        'Função criar usuário com spread',
        'Função listar usuários formatados',
        'Função atualizar com merge de objetos',
        'Função deletar com filter',
        'Validação de campos obrigatórios'
      ]
    }
  },
  {
    semana: 6,
    titulo: 'DOM e Eventos',
    descricao: 'Aprenda a manipular o DOM (Document Object Model) e gerenciar eventos para criar páginas interativas',
    instrucoes: 'Esta semana conecta o JavaScript ao navegador. Você vai aprender a selecionar, criar e modificar elementos HTML, além de gerenciar eventos do usuário. Pratique bastante no DevTools — a manipulação do DOM é visual e imediata. Entenda a diferença entre innerHTML e textContent, e o modelo de propagação de eventos (captura vs bolha). O mini-projeto final vai integrar tudo em uma aplicação de lista de tarefas interativa.',
    dias: [
      {
        dia: 1,
        titulo: 'Seletores do DOM',
        teoria: 'O DOM (Document Object Model) é uma representação em árvore do HTML. Seletores como getElementById e querySelector permitem acessar elementos.',
        topicos: [
          { titulo: 'getElementById e getElementsByClassName', descricao: 'document.getElementById("id") retorna UM elemento (ou null). É o método mais rápido. getElementsByClassName("classe") retorna uma HTMLCollection VIVA (atualiza automaticamente). getElementsByTagName("div") também retorna coleção viva. HTMLCollection não é array — não tem forEach, map, filter. Converta com Array.from() ou [...colecao]. Prefira querySelector mais flexível.', exemplo: 'document.getElementById("app") // seleciona por ID\ndocument.getElementsByClassName("item") // HTMLCollection viva' },
          { titulo: 'querySelector e querySelectorAll', descricao: 'document.querySelector("seletor") retorna o PRIMEIRO elemento que corresponde ao seletor CSS. querySelectorAll("seletor") retorna uma NodeList ESTÁTICA com todos. Aceitam qualquer seletor CSS: ".classe", "#id", "div > p", "[data-tipo="valor"]", ":nth-child(2)". NodeList tem forEach mas não map/filter — use Array.from() ou [...nodeList]. São os seletores mais flexíveis e recomendados.', exemplo: 'document.querySelector(".classe") // primeiro elemento\ndocument.querySelectorAll("p") // NodeList estática' },
          { titulo: 'Navegação entre elementos', descricao: 'A partir de um elemento, navegue: parentElement (pai), children (filhos HTMLCollection), firstElementChild / lastElementChild, nextElementSibling / previousElementSibling. Para texto (inclui nós de texto): childNodes, firstChild, nextSibling. Para encontrar o ancestral mais próximo com um seletor: element.closest("seletor"). closest é útil para event delegation e navegação.', exemplo: 'element.parentElement // elemento pai\nelement.nextElementSibling // próximo irmão' },
          { titulo: 'Verificação de existência', descricao: 'Sempre verifique se o elemento existe antes de manipular: if (elemento) { elemento.textContent = "..." }. querySelector retorna null se não encontrar. getElementById também. Verifique também se a NodeList não está vazia. Em single-page apps, elementos podem não existir se o componente não foi renderizado. Use optional chaining: elemento?.textContent = "..." ou if (elemento) { ... }.', exemplo: 'if (elemento) { elemento.textContent = "..."; }\nelemento?.textContent // optional chaining' }
        ],
        exercicio: 'No console do navegador, selecione elementos da página atual por ID, classe e seletor CSS. Navegue entre elementos pai e filho.',
        codigoInicial: `// Abra o console em qualquer página e teste:\n\n// Por ID\nconst header = document.getElementById("header");\nconsole.log("Header:", header);\n\n// Por classe\nconst itens = document.querySelectorAll(".item");\nconsole.log("Itens:", itens.length);\n\n// Por seletor CSS\nconst primeiroParagrafo = document.querySelector("p");\nconsole.log("Primeiro p:", primeiroParagrafo?.textContent);`,
        codigoSolucao: `// Exemplo com uma página HTML simples:\n// <div id="app">\n//   <h1>Título</h1>\n//   <p class="texto">Parágrafo 1</p>\n//   <p class="texto">Parágrafo 2</p>\n// </div>\n\nconst app = document.getElementById("app");\nconsole.log("App:", app);\n\nconst titulo = app.querySelector("h1");\nconsole.log("Título:", titulo?.textContent);\n\nconst paragrafos = app.querySelectorAll(".texto");\nconsole.log("Parágrafos:", paragrafos.length);\n\nparagrafos.forEach((p, i) => {\n  console.log(\`P \${i + 1}: \${p.textContent}\`);\n});\n\nconst primeiroFilho = app.firstElementChild;\nconsole.log("Primeiro filho:", primeiroFilho?.tagName);\n\nconst ultimoFilho = app.lastElementChild;\nconsole.log("Último filho:", ultimoFilho?.textContent);`
      },
      {
        dia: 2,
        titulo: 'Manipulação de Elementos',
        teoria: 'Crie, modifique e remova elementos do DOM com createElement, textContent, innerHTML, appendChild e classList.',
        topicos: [
          { titulo: 'createElement e textContent', descricao: 'document.createElement("tag") cria um elemento em memória. element.textContent = "texto" define o texto de forma segura (escapa HTML, prevenindo XSS). textContent é mais rápido e seguro que innerHTML. Para ler: textContent retorna todo o texto visível e oculto. Para adicionar ao DOM: parent.appendChild(elemento), parent.prepend(elemento), parent.append(elemento1, elemento2, "texto").', exemplo: 'const div = document.createElement("div");\ndiv.textContent = "Olá";\ndocument.body.appendChild(div);' },
          { titulo: 'innerHTML e insertAdjacentHTML', descricao: 'element.innerHTML = "<strong>HTML</strong>" interpreta a string como HTML. É poderoso mas perigoso — injeção de HTML pode causar XSS. Use apenas com conteúdo confiável. insertAdjacentHTML(posição, html) insere HTML sem sobrescrever: "beforebegin", "afterbegin", "beforeend", "afterend". insertAdjacentHTML é mais eficiente que innerHTML para adições incrementais.', exemplo: 'element.innerHTML = "<strong>Texto</strong>";\nelement.insertAdjacentHTML("beforeend", "<span>fim</span>");' },
          { titulo: 'classList — gerenciar classes CSS', descricao: 'element.classList.add("classe"), .remove("classe"), .toggle("classe") (adiciona/remove alternadamente), .contains("classe") (verifica). classList é muito mais limpo que className que requer manipulação de string. toggle é útil para dark mode, menus, abas. classList pode receber múltiplas classes: element.classList.add("ativo", "destaque"). classList é suportado em todos os navegadores modernos.', exemplo: 'element.classList.add("ativo");\nelement.classList.toggle("dark");\nelement.classList.contains("visivel");' },
          { titulo: 'Atributos e estilos inline', descricao: 'element.getAttribute("src"), .setAttribute("src", "img.jpg"), .removeAttribute("src"). Para atributos booleanos: element.disabled = true, element.checked. Para data attributes: element.dataset.id = "123" (cria data-id). Estilos inline: element.style.color = "red", element.style.backgroundColor = "#fff" (camelCase para CSS properties com hífen). Para estilos computados: getComputedStyle(element).', exemplo: 'img.setAttribute("src", "foto.jpg");\nelement.style.backgroundColor = "red";' }
        ],
        exercicio: 'Crie uma lista dinâmica: crie elementos li com createElement, adicione texto, classes, e insira no DOM.',
        codigoInicial: `// Crie uma lista <ul> com 3 itens <li>\nconst lista = document.createElement("ul");\nlista.className = "minha-lista";\n\nfor (let i = 1; i <= 3; i++) {\n  const item = document.createElement("li");\n  // Configure texto e classe\n}\n\n// Adicione a lista ao body\ndocument.body.appendChild(lista);`,
        codigoSolucao: `const lista = document.createElement("ul");\nlista.className = "minha-lista";\n\nfor (let i = 1; i <= 3; i++) {\n  const item = document.createElement("li");\n  item.textContent = \`Item \${i}\`;\n  item.classList.add("item-lista");\n  if (i === 1) item.classList.add("primeiro");\n  lista.appendChild(item);\n}\n\ndocument.body.appendChild(lista);\n\n// Modificar depois\nconst primeiro = lista.querySelector("li");\nif (primeiro) {\n  primeiro.textContent = "Primeiro item (modificado)";\n  primeiro.style.color = "blue";\n  primeiro.dataset.index = "0";\n}\n\n// Remover o último\nlista.lastElementChild?.remove();`
      },
      {
        dia: 3,
        titulo: 'Eventos no Navegador',
        teoria: 'Eventos permitem responder a ações do usuário: clique, teclado, mouse, formulário. addEventListener é a forma moderna de registrar handlers.',
        topicos: [
          { titulo: 'addEventListener', descricao: 'element.addEventListener("click", handler) registra uma função para ser chamada quando o evento ocorrer. Vantagens sobre onclick: múltiplos handlers no mesmo evento, controle de propagação (terceiro parâmetro), e remoção com removeEventListener. O handler recebe um objeto Event com propriedades: target (elemento que disparou), currentTarget (elemento do listener), type, preventDefault(), stopPropagation().', exemplo: 'botao.addEventListener("click", () => {\n  console.log("Clicou!");\n});' },
          { titulo: 'Eventos de mouse e teclado', descricao: 'Mouse: click, dblclick, mouseenter/mouseleave (não propagam), mouseover/mouseout (propagam), mousedown/mouseup, mousemove. Teclado: keydown (qualquer tecla), keypress (obsoleto), keyup (tecla solta). KeyboardEvent: key (valor da tecla), code (código físico), ctrlKey, shiftKey, altKey. Exemplo: if (event.key === "Enter") { } para submeter formulário.', exemplo: 'element.addEventListener("keydown", (e) => {\n  if (e.key === "Enter") console.log("Enter!");\n});' },
          { titulo: 'Propagação de eventos', descricao: 'Eventos se propagam em 3 fases: CAPTURA (janela → alvo), ALVO, e BOLHA (alvo → janela). Por padrão, listeners escutam na fase de bolha. event.stopPropagation() interrompe a propagação. event.stopImmediatePropagation() também impede outros listeners no mesmo elemento. event.target é o elemento que originou o evento, event.currentTarget é onde o listener está. A delegação de eventos usa a bolha para escutar em um pai.', exemplo: 'e.stopPropagation(); // interrompe bolha\ne.target // elemento clicado\ne.currentTarget // elemento do listener' },
          { titulo: 'Event delegation (delegação)', descricao: 'Em vez de adicionar listeners a cada item de uma lista, adicione UM listener ao pai e use event.target: lista.addEventListener("click", (e) => { if (e.target.matches("li")) { console.log(e.target.textContent) } }). Vantagens: funciona para elementos adicionados dinamicamente, menos memória, melhor performance. Use matches() ou closest() para identificar o elemento alvo dentro do pai.', exemplo: 'lista.addEventListener("click", (e) => {\n  if (e.target.matches("li")) { /* agir */ }\n});' }
        ],
        exercicio: 'Crie uma lista de tarefas onde clicar em um item o marca como concluído (toggle de classe). Adicione suporte a teclado (Enter para adicionar).',
        codigoInicial: `// HTML de exemplo:\n// <ul id="tasks">\n//   <li>Estudar JS</li>\n//   <li>Praticar DOM</li>\n// </ul>\n\nconst lista = document.getElementById("tasks");\n\n// Delegação: clique no li marca como concluído\nlista.addEventListener("click", (e) => {\n  // Implemente toggle de classe "concluido"\n});\n\n// Adicionar ao pressionar Enter\nconst input = document.getElementById("nova-task");\ninput.addEventListener("keydown", (e) => {\n  if (e.key === "Enter") {\n    // Adicione novo item\n  }\n});`,
        codigoSolucao: `const lista = document.getElementById("tasks");\nconst input = document.getElementById("nova-task");\n\n// Delegação: marca como concluído\nlista.addEventListener("click", (e) => {\n  if (e.target.matches("li")) {\n    e.target.classList.toggle("concluido");\n  }\n});\n\n// Adicionar com Enter\ninput.addEventListener("keydown", (e) => {\n  if (e.key === "Enter" && input.value.trim()) {\n    const item = document.createElement("li");\n    item.textContent = input.value.trim();\n    lista.appendChild(item);\n    input.value = "";\n  }\n});\n\n// Botão de adicionar\nconst btn = document.getElementById("adicionar");\nbtn.addEventListener("click", () => {\n  input.dispatchEvent(new KeyboardEvent("keydown", { key: "Enter" }));\n});`
      },
      {
        dia: 4,
        titulo: 'Formulários e Validação',
        teoria: 'Formulários são a principal forma de entrada de dados. Evento submit, validação com JavaScript, e feedback visual ao usuário.',
        topicos: [
          { titulo: 'Evento submit', descricao: 'form.addEventListener("submit", (e) => { e.preventDefault(); /* processar */ }). preventDefault() evita o recarregamento da página. Acesse campos com form.elements.nome ou form.querySelector("[name=nome]"). O objeto FormData simplifica: new FormData(form) — itera com for (const [name, value] of formData). FormData também funciona com fetch para enviar dados.', exemplo: 'form.addEventListener("submit", (e) => {\n  e.preventDefault();\n  const dados = new FormData(form);\n});' },
          { titulo: 'Validação de campos', descricao: 'Validação comum: campo vazio (trim().length === 0), email (includes("@") && includes(".")), senha com requisitos (length >= 6, /[A-Z]/.test(), /[0-9]/.test()). Feedback visual: classList.add("erro") para borda vermelha, exibir span de erro próximo ao campo. Validação em tempo real: evento input (enquanto digita) ou blur (ao sair do campo). HTML5 validation (required, type="email", pattern) complementa mas não substitui validação JS.', exemplo: 'if (!nome.trim()) { mostrarErro(campo, "Obrigatório"); }\nif (!email.includes("@")) { /* email inválido */ }' },
          { titulo: 'Tipos de campo e valores', descricao: 'input.value (texto), input.checked (checkbox/radio), select.value (option selecionada), textarea.value (texto longo). File input: input.files (FileList). Range: input.value (número). Data: input.value (string yyyy-mm-dd). Para checkbox group: document.querySelectorAll("[name=interesses]:checked"). Para radio: document.querySelector("[name=genero]:checked")?.value.', exemplo: 'input.value // texto\ninput.checked // boolean\nselect.value // opção selecionada' }
        ],
        exercicio: 'Crie um formulário de cadastro com validação: nome (obrigatório), email (formato), senha (6+ caracteres). Exiba erros visuais.',
        codigoInicial: `// <form id="cadastro">\n//   <input name="nome" placeholder="Nome">\n//   <input name="email" placeholder="Email">\n//   <input name="senha" type="password" placeholder="Senha">\n//   <button type="submit">Cadastrar</button>\n// </form>\n\nconst form = document.getElementById("cadastro");\n\nform.addEventListener("submit", (e) => {\n  e.preventDefault();\n  // Valide e exiba erros\n});\n\nfunction mostrarErro(campo, mensagem) {\n  // Adicione classe de erro e mensagem\n}`,
        codigoSolucao: `const form = document.getElementById("cadastro");\n\nform.addEventListener("submit", (e) => {\n  e.preventDefault();\n  const dados = new FormData(form);\n  const nome = dados.get("nome").toString().trim();\n  const email = dados.get("email").toString().trim();\n  const senha = dados.get("senha").toString();\n  \n  let valido = true;\n  \n  if (!nome) { mostrarErro(form.nome, "Nome é obrigatório"); valido = false; }\n  if (!email.includes("@")) { mostrarErro(form.email, "Email inválido"); valido = false; }\n  if (senha.length < 6) { mostrarErro(form.senha, "Mínimo 6 caracteres"); valido = false; }\n  \n  if (valido) console.log("Cadastrado:", { nome, email, senha });\n});\n\nfunction mostrarErro(campo, mensagem) {\n  campo.classList.add("erro");\n  const span = campo.nextElementSibling || document.createElement("span");\n  span.className = "erro-msg";\n  span.textContent = mensagem;\n  if (!campo.nextElementSibling) campo.after(span);\n}`
      },
      {
        dia: 5,
        titulo: 'Mini-Projeto: Lista de Tarefas',
        teoria: 'Construa uma aplicação completa de lista de tarefas usando DOM, eventos, formulários e localStorage para persistência.',
        topicos: [
          { titulo: 'Estrutura do projeto', descricao: 'Organize em funções: adicionarTarefa(), renderizarLista(), toggleConcluido(), removerTarefa(). Separe dados (array de tarefas) de renderização (DOM). Use um estado central: let tarefas = []; function renderizar() { /* limpar e recriar lista */ }. Cada tarefa: { id, texto, concluido }. IDs únicos: Date.now() ou Math.random().toString(36).', exemplo: 'let tarefas = [];\nfunction renderizar() { /* limpar e recriar */ }\nfunction adicionarTarefa(texto) { /* ... */ }' },
          { titulo: 'Persistência com localStorage', descricao: 'Salvar: localStorage.setItem("tarefas", JSON.stringify(tarefas)). Carregar: JSON.parse(localStorage.getItem("tarefas")) || []. Salve após cada operação (adicionar, toggle, remover). localStorage é síncrono e limitado a ~5MB. Para dados estruturados, JSON.stringify/parse é o padrão. Sempre trate dados corrompidos com try/catch.', exemplo: 'localStorage.setItem("chave", JSON.stringify(dados));\nJSON.parse(localStorage.getItem("chave"));' }
        ],
        exercicio: 'Complete a lista de tarefas com adicionar, remover, toggle concluído e persistência em localStorage.',
        codigoInicial: `let tarefas = JSON.parse(localStorage.getItem("tarefas")) || [];\nconst lista = document.getElementById("lista-tarefas");\nconst input = document.getElementById("nova-tarefa");\n\nfunction adicionarTarefa(texto) {\n  // Adicione ao array e salve\n}\n\nfunction renderizar() {\n  // Limpe e recrie a lista\n}\n\nfunction toggleTarefa(id) {\n  // Alterne concluido\n}\n\nfunction removerTarefa(id) {\n  // Filtre e salve\n}\n\nrenderizar();`,
        codigoSolucao: `let tarefas = JSON.parse(localStorage.getItem("tarefas")) || [];\nconst lista = document.getElementById("lista-tarefas");\nconst input = document.getElementById("nova-tarefa");\n\nfunction salvar() {\n  localStorage.setItem("tarefas", JSON.stringify(tarefas));\n  renderizar();\n}\n\nfunction adicionarTarefa(texto) {\n  tarefas.push({ id: Date.now(), texto, concluido: false });\n  salvar();\n}\n\nfunction renderizar() {\n  lista.innerHTML = "";\n  tarefas.forEach(t => {\n    const li = document.createElement("li");\n    li.textContent = t.texto;\n    li.classList.toggle("concluido", t.concluido);\n    li.dataset.id = t.id;\n    \n    const btnRemover = document.createElement("button");\n    btnRemover.textContent = "X";\n    btnRemover.addEventListener("click", (e) => {\n      e.stopPropagation();\n      removerTarefa(t.id);\n    });\n    \n    li.addEventListener("click", () => toggleTarefa(t.id));\n    li.appendChild(btnRemover);\n    lista.appendChild(li);\n  });\n}\n\nfunction toggleTarefa(id) {\n  const tarefa = tarefas.find(t => t.id === id);\n  if (tarefa) {\n    tarefa.concluido = !tarefa.concluido;\n    salvar();\n  }\n}\n\nfunction removerTarefa(id) {\n  tarefas = tarefas.filter(t => t.id !== id);\n  salvar();\n}\n\ninput.addEventListener("keydown", (e) => {\n  if (e.key === "Enter" && input.value.trim()) {\n    adicionarTarefa(input.value.trim());\n    input.value = "";\n  }\n});\n\nrenderizar();`
      }
    ],
    miniProjeto: {
      titulo: 'Lista de Tarefas Interativa',
      descricao: 'Aplicação completa com DOM, eventos, formulários e localStorage',
      requisitos: [
        'Adicionar tarefa ao pressionar Enter',
        'Marcar/desmarcar como concluída (toggle)',
        'Remover tarefa individual',
        'Persistência com localStorage',
        'Feedback visual com classes CSS'
      ]
    }
  },
  {
    semana: 7,
    titulo: 'APIs REST',
    descricao: 'Consuma APIs REST completas com fetch: CRUD, autenticação, tratamento de erros e integração com APIs reais',
    instrucoes: 'Esta semana é prática e voltada ao mundo real. Você vai aprender os fundamentos de REST e HTTP, depois implementar um CRUD completo com fetch, entender autenticação com JWT e tokens, e consumir APIs públicas reais como GitHub e ViaCEP. Cada dia tem um exercício prático com código inicial e solução. No final, você terá construído um cliente CRUD funcional conectado a uma API real. Sem pular etapas — a base de HTTP é essencial.',
    dias: [
      {
        dia: 1,
        titulo: 'Fundamentos de REST e HTTP',
        teoria: 'REST (Representational State Transfer) é o padrão mais usado para comunicação entre sistemas. Entenda os verbos HTTP, status codes e estrutura de uma API REST.',
        topicos: [
          { titulo: 'O que é uma API REST', descricao: 'REST é um estilo de arquitetura onde recursos (usuários, posts, produtos) são acessados via URLs. Cada recurso tem um endpoint único: /api/users, /api/posts/:id. As operações usam verbos HTTP: GET (ler), POST (criar), PUT/PATCH (atualizar), DELETE (remover). Uma API REST é stateless — cada requisição contém toda informação necessária. Respostas geralmente são em JSON. Boas APIs seguem convenções consistentes de nomenclatura e pluralização.', exemplo: 'GET /api/users → lista usuários\nPOST /api/users → cria usuário\nDELETE /api/users/1 → remove' },
          { titulo: 'Verbos HTTP (GET, POST, PUT, DELETE)', descricao: 'GET: recupera dados (nunca modifica). POST: cria um novo recurso (retorna 201 Created + ID). PUT: substitui um recurso inteiro (idempotente). PATCH: atualização parcial. DELETE: remove um recurso (retorna 204 No Content). Exemplo: GET /api/users → lista usuários. POST /api/users → cria. PUT /api/users/1 → substitui. DELETE /api/users/1 → remove. No fetch, defina method: "POST", body, e headers apropriados.', exemplo: 'fetch("/api/users") // GET\nfetch("/api/users", { method: "POST", body: JSON.stringify(dados) })' },
          { titulo: 'Status codes HTTP', descricao: '200 OK (sucesso), 201 Created (recurso criado), 204 No Content (sucesso sem corpo). 400 Bad Request (dados inválidos), 401 Unauthorized (não autenticado), 403 Forbidden (sem permissão), 404 Not Found, 409 Conflict (duplicata). 500 Internal Server Error (erro do servidor). Fetch não rejeita para erros HTTP — você precisa verificar response.ok ou response.status. Sempre trate 401 redirecionando para login e 500 com mensagem amigável.', exemplo: 'if (!resposta.ok) {\n  if (resposta.status === 404) console.log("Não encontrado");\n}' },
          { titulo: 'Headers e Content-Type', descricao: 'Headers transmitem metadados: Content-Type (application/json, multipart/form-data), Accept (formato esperado), Authorization (Bearer token), User-Agent (identificação do cliente). No fetch: headers: { "Content-Type": "application/json" }. Para receber JSON, o header padrão já funciona. Content-Type é essencial no POST/PUT — sem ele, o servidor pode não interpretar o body corretamente. Cache-Control gerencia caching.', exemplo: 'headers: {\n  "Content-Type": "application/json",\n  "Authorization": "Bearer token"\n}' }
        ],
        exercicio: 'Faça um GET para JSONPlaceholder /posts, exiba o título do primeiro post e o status code da resposta.',
        codigoInicial: `async function buscarPosts() {\n  // Faça fetch de https://jsonplaceholder.typicode.com/posts\n  // Verifique se a resposta foi ok\n  // Converta para JSON\n  // Exiba o título do primeiro post e o status\n}\n\nbuscarPosts();`,
        codigoSolucao: `async function buscarPosts() {\n  const resposta = await fetch("https://jsonplaceholder.typicode.com/posts");\n  \n  console.log("Status:", resposta.status, resposta.ok ? "OK" : "ERRO");\n  \n  if (!resposta.ok) {\n    console.error("Erro HTTP:", resposta.status);\n    return;\n  }\n  \n  const posts = await resposta.json();\n  console.log("Total de posts:", posts.length);\n  console.log("\\n--- Primeiro post ---");\n  console.log("Título:", posts[0].title);\n  console.log("Corpo:", posts[0].body);\n}\n\nbuscarPosts();`
      },
      {
        dia: 2,
        titulo: 'CRUD com Fetch API',
        teoria: 'Implemente as quatro operações CRUD (Create, Read, Update, Delete) usando fetch. Cada operação usa um verbo HTTP diferente e requer configurações específicas.',
        topicos: [
          { titulo: 'GET — listar recursos', descricao: 'fetch(url) sem options faz GET. Para listar: fetch("/api/users"). Resposta é array de objetos. Para buscar um: fetch("/api/users/1"). Resposta é um objeto. Sempre verifique response.ok. Use async/await para legibilidade. Em APIs reais, GET pode ter parâmetros: fetch("/api/users?page=1&limit=10&search=ana"). Monte a query string com URLSearchParams: new URLSearchParams({ page: 1, limit: 10 }).', exemplo: 'const resposta = await fetch("/api/users");\nconst usuarios = await resposta.json();' },
          { titulo: 'POST — criar recursos', descricao: 'fetch(url, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(dados) }). O servidor retorna 201 Created com o recurso criado (incluindo o ID gerado). Exemplo: const novo = await fetch("/api/users", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ nome: "Ana", email: "ana@email.com" }) }).then(r => r.json()).', exemplo: 'const resposta = await fetch("/api/users", {\n  method: "POST",\n  body: JSON.stringify({ nome: "Ana" })\n});' },
          { titulo: 'PUT/PATCH — atualizar recursos', descricao: 'PUT substitui o recurso inteiro: fetch("/api/users/1", { method: "PUT", body: JSON.stringify({ nome, email, idade }) }). PATCH atualiza parcialmente: só envia os campos que mudam. PUT é idempotente (mesma chamada = mesmo resultado). PATCH não é necessariamente idempotente. Na prática, muitas APIs aceitam PUT para simplificar. Exemplo: fetch("/api/users/1", { method: "PATCH", body: JSON.stringify({ nome: "Novo Nome" }) }).', exemplo: 'fetch("/api/users/1", {\n  method: "PATCH",\n  body: JSON.stringify({ nome: "Novo" })\n});' },
          { titulo: 'DELETE — remover recursos', descricao: 'fetch("/api/users/1", { method: "DELETE" }). Resposta geralmente é 204 No Content (sem body) ou 200 OK com o recurso removido. DELETE é idempotente — deletar o mesmo recurso duas vezes retorna 404 na segunda. Após deletar, atualize a UI removendo o item da lista. Exemplo: const resposta = await fetch("/api/users/1", { method: "DELETE" }); if (resposta.ok) { lista = lista.filter(u => u.id !== 1) }.', exemplo: 'const resposta = await fetch("/api/users/1", { method: "DELETE" });\nif (resposta.ok) { /* remover da UI */ }' }
        ],
        exercicio: 'Implemente um CRUD completo contra JSONPlaceholder: crie um post, liste todos, atualize o título e depois delete.',
        codigoInicial: `const BASE = "https://jsonplaceholder.typicode.com";\n\nasync function createPost(titulo, corpo) {\n  // POST /posts\n}\n\nasync function listPosts() {\n  // GET /posts\n}\n\nasync function updatePost(id, novoTitulo) {\n  // PATCH /posts/:id\n}\n\nasync function deletePost(id) {\n  // DELETE /posts/:id\n}\n\nasync function main() {\n  const criado = await createPost("Meu Post", "Conteúdo legal");\n  console.log("Criado:", criado);\n  \n  const posts = await listPosts();\n  console.log("Total:", posts.length);\n  \n  await updatePost(criado.id, "Título Atualizado");\n  console.log("Atualizado!");\n  \n  await deletePost(criado.id);\n  console.log("Deletado!");\n}\n\nmain();`,
        codigoSolucao: `const BASE = "https://jsonplaceholder.typicode.com";\n\nasync function request(url, options = {}) {\n  const resposta = await fetch(url, {\n    headers: { "Content-Type": "application/json" },\n    ...options\n  });\n  if (!resposta.ok) throw new Error(\`HTTP \${resposta.status}\`);\n  return resposta.status === 204 ? null : resposta.json();\n}\n\nasync function createPost(titulo, corpo) {\n  return request(BASE + "/posts", {\n    method: "POST",\n    body: JSON.stringify({ title: titulo, body: corpo, userId: 1 })\n  });\n}\n\nasync function listPosts() {\n  return request(BASE + "/posts");\n}\n\nasync function updatePost(id, novoTitulo) {\n  return request(BASE + \`/posts/\${id}\`, {\n    method: "PATCH",\n    body: JSON.stringify({ title: novoTitulo })\n  });\n}\n\nasync function deletePost(id) {\n  return request(BASE + \`/posts/\${id}\`, { method: "DELETE" });\n}\n\nasync function main() {\n  try {\n    const criado = await createPost("Meu Post", "Conteúdo legal");\n    console.log("Criado:", criado.id, "-", criado.title);\n    \n    const posts = await listPosts();\n    console.log("Total de posts:", posts.length);\n    \n    const atualizado = await updatePost(criado.id, "Título Atualizado");\n    console.log("Atualizado:", atualizado.title);\n    \n    await deletePost(criado.id);\n    console.log("Post", criado.id, "deletado com sucesso!");\n  } catch (err) {\n    console.error("Erro na operação:", err.message);\n  }\n}\n\nmain();`
      },
      {
        dia: 3,
        titulo: 'Autenticação e Segurança em APIs',
        teoria: 'APIs frequentemente exigem autenticação. JWT (JSON Web Token) é o padrão mais usado. Entenda como enviar tokens, lidar com CORS e proteger suas credenciais.',
        topicos: [
          { titulo: 'JWT — JSON Web Token', descricao: 'JWT é um token composto por header.payload.signature. O servidor gera o token no login e o cliente envia em toda requisição. Estrutura: header (algoritmo), payload (dados do usuário, expiração), signature (assinatura que valida o token). O token é enviado no header Authorization: Bearer <token>. O servidor verifica a assinatura para garantir que o token não foi alterado. JWTs são stateless — o servidor não precisa armazenar sessão.', exemplo: '// Header: Authorization: Bearer eyJhbGciOiJIUzI1NiIs...\nconst token = localStorage.getItem("token");' },
          { titulo: 'Authorization Header (Bearer Token)', descricao: 'Após o login, armazene o token (localStorage/sessionStorage) e envie em cada requisição: headers: { "Authorization": \`Bearer \${token}\` }. Exemplo: fetch("/api/users", { headers: { "Authorization": \`Bearer \${localStorage.getItem("token")}\` } }). Se o token expirar (401), redirecione para o login. Nunca exponha tokens em URLs ou logs. Use sessionStorage para tokens sensíveis (limpa ao fechar o navegador).', exemplo: 'headers: {\n  "Authorization": \`Bearer \${localStorage.getItem("token")}\`\n}' },
          { titulo: 'CORS — Cross-Origin Resource Sharing', descricao: 'CORS é uma política de segurança do navegador que restringe requisições entre domínios diferentes. Se sua página em localhost:3000 chama api.exemplo.com, o servidor precisa permitir com header: Access-Control-Allow-Origin: *. Para requisições com credenciais: credentials: "include" no fetch. O navegador faz uma requisição OPTIONS (preflight) antes de POST/PUT/DELETE com headers personalizados.', exemplo: '// Servidor precisa enviar:\n// Access-Control-Allow-Origin: *\n// fetch com credentials: "include"' },
          { titulo: 'Boas práticas de segurança', descricao: 'Nunca armazene secrets (chaves de API, senhas) no frontend — elas são visíveis no código fonte. Use variáveis de ambiente (VITE_API_KEY) que são substituídas no build. Para APIs que exigem chave do lado cliente, use restrições de domínio (referer). Sempre valide e sanitize dados no servidor. Use HTTPS (não HTTP). Implemente rate limiting no servidor. Para logout, limpe o token armazenado.', exemplo: '// Use variáveis de ambiente: import.meta.env.VITE_API_KEY\n// Use HTTPS sempre' }
        ],
        exercicio: 'Autentique na GitHub API com um token pessoal (ou simulado) e liste seus repositórios.',
        codigoInicial: `// GitHub API — gere um token em github.com/settings/tokens\nconst TOKEN = "seu_token_aqui"; // ou deixe vazio para simular\n\nasync function listarRepos(usuario) {\n  const headers = {};\n  if (TOKEN && TOKEN !== "seu_token_aqui") {\n    headers["Authorization"] = \`Bearer \${TOKEN}\`;\n  }\n  \n  const resposta = await fetch(\`https://api.github.com/users/\${usuario}/repos\`, { headers });\n  \n  if (!resposta.ok) {\n    console.error("Erro:", resposta.status, resposta.statusText);\n    return [];\n  }\n  \n  return resposta.json();\n}\n\nasync function main() {\n  const repos = await listarRepos("octocat");\n  repos.forEach(r => console.log(\`\${r.name} - \${r.stargazers_count} estrelas\`));\n}\n\nmain();`,
        codigoSolucao: `// GitHub API\nconst TOKEN = "seu_token_aqui";\n\nasync function listarRepos(usuario) {\n  const headers = {};\n  if (TOKEN && TOKEN !== "seu_token_aqui") {\n    headers["Authorization"] = \`Bearer \${TOKEN}\`;\n    console.log("Usando autenticação — mais requests disponíveis");\n  } else {\n    console.log("Sem token — modo não autenticado (rate limit: 60/h)");\n  }\n  \n  const resposta = await fetch(\`https://api.github.com/users/\${usuario}/repos?per_page=5&sort=updated\`, { headers });\n  \n  if (!resposta.ok) {\n    if (resposta.status === 403) {\n      console.error("Rate limit excedido! Tente com token ou aguarde.");\n    } else {\n      console.error(\`Erro HTTP \${resposta.status}: \${resposta.statusText}\`);\n    }\n    return [];\n  }\n  \n  const repos = await resposta.json();\n  console.log(\`\\nRepositórios de \${usuario} (\${repos.length}):\\n\`);\n  repos.forEach((r, i) => {\n    console.log(\`\${i + 1}. \${r.name}\`);\n    console.log(\`   ⭐ \${r.stargazers_count} | 🍴 \${r.forks_count} | \${r.language || "N/A"}\`);\n    console.log(\`   \${r.description || "Sem descrição"}\\n\`);\n  });\n  \n  // Headers de rate limit\n  const remaining = resposta.headers.get("x-ratelimit-remaining");\n  console.log(\"Requests restantes:\", remaining);\n}\n\nlistarRepos("octocat");`
      },
      {
        dia: 4,
        titulo: 'APIs Reais na Prática',
        teoria: 'Combine múltiplas APIs públicas em uma aplicação. Aprenda a lidar com paginação, caching, tratamento consistente de erros e diferentes formatos de resposta.',
        topicos: [
          { titulo: 'ViaCEP — buscar endereço por CEP', descricao: 'ViaCEP (viacep.com.br) é uma API gratuita que retorna endereço a partir do CEP. Exemplo: fetch("https://viacep.com.br/ws/01001000/json/"). Resposta: { cep, logradouro, bairro, localidade, uf }. Sem autenticação necessária. Ideal para formulários de endereço. Trate erros: CEP inválido retorna { erro: true }. Use o evento blur no campo CEP para buscar automaticamente.', exemplo: 'const cep = await fetch("https://viacep.com.br/ws/01001000/json/");\nconst endereco = await cep.json();' },
          { titulo: 'JSONPlaceholder — dados simulados', descricao: 'JSONPlaceholder (jsonplaceholder.typicode.com) é uma API falsa para testes. Recursos: /posts, /comments, /albums, /photos, /todos, /users. Suporta paginação: ?_page=1&_limit=10. Relacionamentos: /users/1/posts, /posts/1/comments. Ideal para prototipação e aprendizado. Os dados são estáticos — POST/PUT/PATCH/DELETE simulam mas não persistem. Ótima para testar CRUD sem infraestrutura.', exemplo: 'fetch("https://jsonplaceholder.typicode.com/posts?_limit=5")\n  .then(r => r.json())\n  .then(console.log);' },
          { titulo: 'Paginação e filtros', descricao: 'APIs reais paginam resultados. Padrões comuns: ?page=1&limit=10 (page-based), ?offset=0&limit=10 (offset-based), ?cursor=abc123 (cursor-based). Headers de paginação: x-total-count (total de itens), Link (links para próxima/anterior página). Exemplo: fetch("/posts?_page=1&_limit=5&title_like=javascript"). Monte parâmetros com URLSearchParams para legibilidade.', exemplo: 'fetch("/posts?_page=1&_limit=10&title_like=js");\n// Headers: x-total-count, Link' },
          { titulo: 'Combinação de múltiplas APIs', descricao: 'Use Promise.all para buscar múltiplos endpoints em paralelo: const [endereco, usuario, posts] = await Promise.all([buscarCep(cep), buscarUsuario(id), buscarPosts(id)]). Trate cada erro individualmente com allSettled para que uma falha não derrube as outras. Crie funções helper para cada API com interface consistente (parâmetros → resposta tratada → erro lançado).', exemplo: 'const [cep, github] = await Promise.all([\n  buscarCep("01001000"),\n  buscarGitHub("octocat")\n]);' }
        ],
        exercicio: 'Busque um CEP no ViaCEP, depois busque um usuário no GitHub, e combine os dados em um dashboard. Use Promise.all para paralelizar.',
        codigoInicial: `const VIA_CEP = "https://viacep.com.br/ws";\nconst GITHUB = "https://api.github.com";\n\nasync function buscarCep(cep) {\n  // GET viacep.com.br/ws/{cep}/json/\n}\n\nasync function buscarGitHub(usuario) {\n  // GET api.github.com/users/{usuario}\n}\n\nasync function main() {\n  const cep = "01001000";\n  const user = "octocat";\n  \n  try {\n    const [endereco, github] = await Promise.all([\n      buscarCep(cep),\n      buscarGitHub(user)\n    ]);\n    \n    console.log("=== Dashboard ===\\n");\n    console.log("Endereço:", endereco);\n    console.log("GitHub:", github);\n  } catch (err) {\n    console.error("Erro:", err);\n  }\n}\n\nmain();`,
        codigoSolucao: `const VIA_CEP = "https://viacep.com.br/ws";\nconst GITHUB = "https://api.github.com";\n\nasync function buscarCep(cep) {\n  const resposta = await fetch(\`\${VIA_CEP}/\${cep}/json/\`);\n  if (!resposta.ok) throw new Error(\`CEP API: HTTP \${resposta.status}\`);\n  const dados = await resposta.json();\n  if (dados.erro) throw new Error("CEP não encontrado");\n  return dados;\n}\n\nasync function buscarGitHub(usuario) {\n  const resposta = await fetch(\`\${GITHUB}/users/\${usuario}\`);\n  if (!resposta.ok) throw new Error(\`GitHub API: HTTP \${resposta.status}\`);\n  return resposta.json();\n}\n\nasync function buscarRepos(usuario) {\n  const resposta = await fetch(\`\${GITHUB}/users/\${usuario}/repos?per_page=3&sort=stars\`);\n  if (!resposta.ok) return [];\n  return resposta.json();\n}\n\nasync function main() {\n  const cep = "01001000";\n  const user = "octocat";\n  \n  try {\n    const [endereco, github, repos] = await Promise.all([\n      buscarCep(cep),\n      buscarGitHub(user),\n      buscarRepos(user)\n    ]);\n    \n    console.log("=== Dashboard de Dados ===\\n");\n    console.log("📍 Endereço:");\n    console.log(\`  \${endereco.logradouro}, \${endereco.bairro}\`);\n    console.log(\`  \${endereco.localidade} - \${endereco.uf}, CEP: \${endereco.cep}\\n\`);\n    \n    console.log("👤 GitHub User:");\n    console.log(\`  Login: \${github.login}\`);\n    console.log(\`  Nome: \${github.name || "N/A"}\`);\n    console.log(\`  Seguidores: \${github.followers} | Seguindo: \${github.following}\`);\n    console.log(\`  Repositórios: \${github.public_repos}\\n\`);\n    \n    console.log("⭐ Top Repos:");\n    repos.forEach(r => console.log(\`  - \${r.name} (\${r.stargazers_count} ⭐)\`));\n  } catch (err) {\n    console.error("Falha ao carregar dashboard:", err.message);\n  }\n}\n\nmain();`
      },
      {
        dia: 5,
        titulo: 'Mini-Projeto: CRUD Client',
        teoria: 'Construa um cliente CRUD completo conectado a uma API real. Organize o código em camadas (API, UI, state) e implemente loading, sucesso e erro para cada operação.',
        topicos: [
          { titulo: 'Arquitetura de cliente API', descricao: 'Separe em camadas: api.js (funções fetch), app.js (lógica de negócio), ui.js (renderização). api.js exporta funções puras que retornam dados: getPosts(), createPost(data), updatePost(id, data), deletePost(id). Cada função lida com serialização/headers e lança erros HTTP. app.js coordena o fluxo: busca → transforma → renderiza. ui.js apenas exibe. Essa separação facilita testes e manutenção.', exemplo: '// api.js\nexport const getPosts = () => fetch("/api/posts").then(r => r.json());\nexport const createPost = (dados) => fetch("/api/posts", { method: "POST", body: JSON.stringify(dados) });' },
          { titulo: 'Estados da UI: loading, success, error', descricao: 'Toda operação assíncrona passa por 3 estados: loading (spinner, botão desabilitado), success (exibir dados, feedback verde), error (mensagem de erro, fallback). Implemente com useState ou um objeto de estado: { loading, error, data }. Exiba loading enquanto busca, dados quando chegam, e erro com botão de retry. Nunca deixe o usuário sem feedback — especialmente em operações de criação/edição.', exemplo: 'const estado = { loading: true, error: null, data: null };\n// loading → spinner\n// error → mensagem\n// data → renderizar' },
          { titulo: 'Refetch e mutações', descricao: 'Após criar/atualizar/deletar, a lista precisa refletir a mudança. Estratégias: 1) Refetch da lista (mais seguro), 2) Atualização otimista (adicionar/remover localmente antes da confirmação). Para refetch: const criarEPostar = async (data) => { await createPost(data); return fetchPosts() }. Para atualização otimista: setLista(prev => [...prev, novoItem]); depois await createPost(data); se falhar, reverta.', exemplo: 'async function criarEListar(dados) {\n  await createPost(dados);\n  return getPosts(); // refetch\n}' }
        ],
        exercicio: 'Monte uma aplicação que lista posts, abre detalhes, cria novo post com formulário, edita o título e deleta. Implemente loading e tratamento de erro em cada operação.',
        codigoInicial: `// api.js\nconst BASE = "https://jsonplaceholder.typicode.com";\n\nasync function api(url, options = {}) {\n  const resposta = await fetch(BASE + url, {\n    headers: { "Content-Type": "application/json" },\n    ...options\n  });\n  if (!resposta.ok) throw new Error(\`HTTP \${resposta.status}\`);\n  return resposta.status === 204 ? null : resposta.json();\n}\n\nexport const getPosts = () => api("/posts?_limit=5");\nexport const getPost = (id) => api(\`/posts/\${id}\`);\nexport const createPost = (data) => api("/posts", { method: "POST", body: JSON.stringify(data) });\nexport const updatePost = (id, data) => api(\`/posts/\${id}\`, { method: "PATCH", body: JSON.stringify(data) });\nexport const deletePost = (id) => api(\`/posts/\${id}\`, { method: "DELETE" });\n\n// app.js\nasync function main() {\n  console.log("=== CRUD Client ===\\n");\n  \n  // 1. Listar posts\n  // 2. Criar um post\n  // 3. Listar novamente (confirmar criação)\n  // 4. Editar o post criado\n  // 5. Deletar o post\n}\n\nmain();`,
        codigoSolucao: `// api.js\nconst BASE = "https://jsonplaceholder.typicode.com";\n\nasync function api(url, options = {}) {\n  const resposta = await fetch(BASE + url, {\n    headers: { "Content-Type": "application/json" },\n    ...options\n  });\n  if (!resposta.ok) throw new Error(\`HTTP \${resposta.status} - \${resposta.statusText}\`);\n  return resposta.status === 204 ? null : resposta.json();\n}\n\nexport const getPosts = () => api("/posts?_limit=5");\nexport const getPost = (id) => api(\`/posts/\${id}\`);\nexport const createPost = (data) => api("/posts", { method: "POST", body: JSON.stringify(data) });\nexport const updatePost = (id, data) => api(\`/posts/\${id}\`, { method: "PATCH", body: JSON.stringify(data) });\nexport const deletePost = (id) => api(\`/posts/\${id}\`, { method: "DELETE" });\n\n// app.js\nasync function withFeedback(operacao, nome) {\n  console.log(\`⏳ \${nome}...\`);\n  try {\n    const resultado = await operacao();\n    console.log(\`✅ \${nome} concluído!\`);\n    return resultado;\n  } catch (err) {\n    console.error(\`❌ Erro ao \${nome.toLowerCase()}:\`, err.message);\n    throw err;\n  }\n}\n\nasync function main() {\n  try {\n    // 1. Listar\n    const posts = await withFeedback(() => getPosts(), "Listar posts");\n    console.log("\\nPosts atuais:");\n    posts.forEach(p => console.log(\`  [\${p.id}] \${p.title}\`));\n    \n    // 2. Criar\n    const novo = await withFeedback(\n      () => createPost({ title: "Novo Post", body: "Criado via fetch!", userId: 1 }),\n      "Criar post"\n    );\n    console.log("  Criado ID:", novo.id);\n    \n    // 3. Listar após criação\n    const aposCriar = await getPosts();\n    console.log("\\nTotal após criação:", aposCriar.length);\n    \n    // 4. Editar\n    const editado = await withFeedback(\n      () => updatePost(novo.id, { title: "Post Editado" }),\n      "Editar post"\n    );\n    console.log("  Novo título:", editado.title);\n    \n    // 5. Deletar\n    await withFeedback(\n      () => deletePost(novo.id),\n      "Deletar post"\n    );\n    console.log("  Post removido com sucesso");\n    \n    console.log("\\n🎉 CRUD completo executado!");\n  } catch (err) {\n    console.error("\\n🔥 Aplicação falhou:", err.message);\n  }\n}\n\nmain();`
      }
    ],
    miniProjeto: {
      titulo: 'Cliente CRUD Completo',
      descricao: 'Aplicação que consome uma API REST com as 4 operações CRUD, tratamento de erros e feedback visual',
      requisitos: [
        'Listagem de recursos com GET',
        'Criação com POST',
        'Atualização com PUT/PATCH',
        'Deleção com DELETE',
        'Tratamento de erros HTTP e de rede',
        'Feedback visual (loading/success/error)',
        'Arquitetura em camadas (api.js / app.js)'
      ]
    }
  },
  {
    semana: 8,
    titulo: 'Assíncrono',
    descricao: 'Domine programação assíncrona: callbacks, Promises, async/await e Fetch API para consumir dados de APIs',
    instrucoes: 'Programação assíncrona é um dos tópicos mais importantes e desafiadores do JavaScript. Comece entendendo o problema dos callbacks e como Promises resolvem, depois aprenda async/await que torna código assíncrono legível como síncrono. Pratique bastante com Fetch API e consumo de APIs reais. O mini-projeto final vai integrar tudo em uma aplicação que busca dados de uma API pública. Não pule etapas — a base de Promises é essencial para entender async/await.',
    dias: [
      {
        dia: 1,
        titulo: 'Callbacks e Callback Hell',
        teoria: 'Callbacks são funções passadas para execução posterior. O padrão callback hell (aninhamento excessivo) motivou a criação de Promises e async/await.',
        topicos: [
          { titulo: 'Callbacks assíncronos', descricao: 'Diferente de callbacks síncronos (como em map/filter), callbacks assíncronos executam DEPOIS que uma operação demorada termina. Exemplos: setTimeout(callback, tempo), fs.readFile(path, callback), addEventListener("click", callback). O JavaScript continua executando código enquanto espera a operação assíncrona — isso é o event loop. O callback é colocado na fila de tarefas e executado quando a pilha principal estiver vazia.', exemplo: 'setTimeout(() => console.log("passou 1s"), 1000)' },
          { titulo: 'Erro com callbacks (error-first)', descricao: 'O padrão error-first é comum em Node.js: callback(err, resultado). Se err não for null, ocorreu um erro: if (err) { console.error(err); return }. Esse padrão manual é propenso a erros — esquecer de tratar o erro ou tratar no lugar errado. Exemplo: fs.readFile("arquivo.txt", (err, data) => { if (err) { /* tratar */ } else { /* usar data */ } }). Callbacks também dificultam o tratamento de erros em cascata.', exemplo: 'fs.readFile("file.txt", (err, data) => { if (err) throw err })' },
          { titulo: 'Callback hell (pirâmide da desgraça)', descricao: 'Callback hell é o aninhamento excessivo de callbacks: operacao1(() => { operacao2(() => { operacao3(() => { /* ... */ }) }) }). Isso cria código difícil de ler, dar manutenção e depurar. Problemas: indentação crescente, tratamento de erro repetido, fluxo confuso, difícil adicionar novas etapas. Soluções: Promises, async/await, ou bibliotecas como async.js. Callback hell motivou a criação das Promises no ES6.', exemplo: 'doA(() => { doB(() => { doC(() => {}) }) })' },
          { titulo: 'setTimeout e setInterval', descricao: 'setTimeout(fn, ms) executa a função após ms milissegundos. Retorna um ID para cancelar com clearTimeout(id). setInterval(fn, ms) executa repetidamente a cada ms. Retorna ID para clearInterval(id). setTimeout(fn, 0) não executa imediatamente — é colocado na fila de tarefas e executa após o código síncrono terminar. setInterval pode acumular chamadas se a execução demorar mais que o intervalo — prefira setTimeout recursivo.', exemplo: 'const id = setInterval(() => console.log("tick"), 1000); clearInterval(id)' }
        ],
        exercicio: 'Use setTimeout para simular uma operação assíncrona. Crie uma sequência de callbacks que leva a callback hell. Depois refatore.',
        codigoInicial: `function buscarUsuario(id, callback) {\n  setTimeout(() => {\n    callback(null, { id, nome: "Ana" });\n  }, 1000);\n}\n\nfunction buscarPedidos(usuarioId, callback) {\n  setTimeout(() => {\n    callback(null, [{ id: 101, total: 200 }]);\n  }, 1000);\n}\n\n// Callback hell\nbuscarUsuario(1, (err, usuario) => {\n  if (err) return console.error(err);\n  console.log("Usuário:", usuario);\n  \n  buscarPedidos(usuario.id, (err, pedidos) => {\n    if (err) return console.error(err);\n    console.log("Pedidos:", pedidos);\n  });\n});`,
        codigoSolucao: `function buscarUsuario(id, callback) {\n  setTimeout(() => {\n    callback(null, { id, nome: "Ana" });\n  }, 1000);\n}\n\nfunction buscarPedidos(usuarioId, callback) {\n  setTimeout(() => {\n    callback(null, [{ id: 101, total: 200 }]);\n  }, 1000);\n}\n\n// Callback hell (cresce rapidamente com mais etapas)\nbuscarUsuario(1, (err, usuario) => {\n  if (err) return console.error(err);\n  console.log("Usuário:", usuario);\n  \n  buscarPedidos(usuario.id, (err, pedidos) => {\n    if (err) return console.error(err);\n    console.log("Pedidos:", pedidos);\n    \n    // Mais aninhamento aqui...\n  });\n});\n\n// Com setTimeout recursivo (evita acúmulo do setInterval)\nlet contador = 0;\nfunction tick() {\n  console.log("Tick", ++contador);\n  if (contador < 5) setTimeout(tick, 1000);\n}\ntick();`
      },
      {
        dia: 2,
        titulo: 'Promises: Criação e Consumo',
        teoria: 'Promise representa um valor que pode estar disponível agora, no futuro ou nunca. then/catch/finally encadeiam operações assíncronas de forma legível.',
        topicos: [
          { titulo: 'Criando Promises', descricao: 'new Promise((resolve, reject) => { /* async */ }). resolve(valor) completa com sucesso, reject(erro) completa com falha. Promise.resolve(valor) cria promise já resolvida. Promise.reject(erro) cria já rejeitada. O executor (callback do constructor) executa IMEDIATAMENTE (síncrono) — o then é que é assíncrono. Uma promise só pode resolver ou rejeitar UMA VEZ — chamadas adicionais são ignoradas. Promise tem 3 estados: pending, fulfilled, rejected.', exemplo: 'new Promise((res) => setTimeout(() => res("ok"), 1000))' },
          { titulo: 'then, catch e finally', descricao: 'promise.then(valor => { }, erro => { }) — o segundo callback opcional trata erro. promise.catch(erro => { }) trata rejeições. promise.finally(() => { }) executa sempre (sucesso ou erro). then retorna uma NOVA promise, permitindo encadeamento: fetch(url).then(r => r.json()).then(data => console.log).catch(err => console.error).finally(() => loading = false). Erros em qualquer then propagam para o catch mais próximo.', exemplo: 'fetch(url).then(r => r.json()).catch(console.error).finally(() => loading = false)' },
          { titulo: 'Encadeamento de Promises', descricao: 'O segredo do encadeamento: cada then retorna uma nova promise. Se o callback retornar um valor, a próxima promise resolve com esse valor. Se retornar uma promise, ela é "achatada" — a próxima then espera essa promise resolver. Isso elimina o callback hell: buscarUsuario(1).then(usuario => buscarPedidos(usuario.id)).then(pedidos => console.log(pedidos)).catch(err => console.error). Cada etapa é plana e independente.', exemplo: 'buscarUser(1).then(u => buscarPedidos(u.id)).then(p => console.log(p))' },
          { titulo: 'Tratamento de erros', descricao: 'Sempre termine uma cadeia de promises com catch() — erros não tratados viram PromiseRejectionEvent (warning no console). catch captura erros de qualquer then anterior. Para tratamento específico: coloque catch após o then que pode falhar. throw dentro de then rejeita a promise. catch pode relançar o erro (throw err) para propagar para o próximo catch. finally não recebe o valor — apenas executa código de limpeza.', exemplo: 'fetch(url).catch(err => console.error("Falhou:", err))' }
        ],
        exercicio: 'Converta as funções com callback do dia anterior para Promises. Encadeie then para sequenciar operações.',
        codigoInicial: `function buscarUsuario(id) {\n  return new Promise((resolve, reject) => {\n    setTimeout(() => {\n      if (id > 0) resolve({ id, nome: "Ana" });\n      else reject(new Error("ID inválido"));\n    }, 1000);\n  });\n}\n\nfunction buscarPedidos(usuarioId) {\n  return new Promise((resolve) => {\n    setTimeout(() => resolve([{ id: 101, total: 200 }]), 1000);\n  });\n}\n\n// Encadeie as operações\nbuscarUsuario(1)\n  .then(usuario => {\n    console.log("Usuário:", usuario);\n    return buscarPedidos(usuario.id);\n  })\n  .then(pedidos => {\n    console.log("Pedidos:", pedidos);\n  })\n  .catch(err => console.error("Erro:", err));`,
        codigoSolucao: `function buscarUsuario(id) {\n  return new Promise((resolve, reject) => {\n    setTimeout(() => {\n      if (id > 0) resolve({ id, nome: "Ana" });\n      else reject(new Error("ID inválido"));\n    }, 1000);\n  });\n}\n\nfunction buscarPedidos(usuarioId) {\n  return new Promise((resolve) => {\n    setTimeout(() => resolve([{ id: 101, total: 200 }]), 1000);\n  });\n}\n\nbuscarUsuario(1)\n  .then(usuario => {\n    console.log("Usuário:", usuario);\n    return buscarPedidos(usuario.id);\n  })\n  .then(pedidos => {\n    console.log("Pedidos:", pedidos);\n    const total = pedidos.reduce((s, p) => s + p.total, 0);\n    console.log("Total: R$", total);\n  })\n  .catch(err => console.error("Erro:", err))\n  .finally(() => console.log("Operação concluída"));\n\n// Teste com erro\nbuscarUsuario(-1)\n  .then(u => console.log(u))\n  .catch(err => console.error("Capturado:", err.message));`
      },
      {
        dia: 3,
        titulo: 'Promise.all, Promise.race e Promise.allSettled',
        teoria: 'Promise.all executa várias promises em paralelo e aguarda TODAS. Promise.race retorna a primeira que resolver. Promise.allSettled aguarda todas (sucesso ou falha).',
        topicos: [
          { titulo: 'Promise.all — paralelo total', descricao: 'Promise.all([promise1, promise2]) retorna uma promise que resolve com array dos resultados. Se QUALQUER promise rejeitar, Promise.all rejeita imediatamente (fail-fast). Exemplo: const [user, posts] = await Promise.all([fetchUser(id), fetchPosts(id)]). Usado para: carregar dados independentes simultaneamente, melhorar performance (requisições paralelas). As promises começam a executar no momento da criação — Promise.all apenas aguarda.', exemplo: 'Promise.all([fetchA(), fetchB()]).then(([a, b]) => console.log(a, b))' },
          { titulo: 'Promise.race — primeira a completar', descricao: 'Promise.race([promises]) retorna a primeira promise que resolver OU rejeitar (a mais rápida). Útil para: timeout (Promise.race([fetch(url), rejectAfter(5000)])), limite de tempo para operações lentas. Cuidado: as outras promises continuam executando (não são canceladas). race é "primeiro a terminar", não "primeiro a resolver". Se a primeira for rejeitada, race rejeita.', exemplo: 'Promise.race([fetch(url), timeout(5000)]).catch(() => console.log("timeout"))' },
          { titulo: 'Promise.allSettled — todas completas', descricao: 'Promise.allSettled([promises]) retorna TODAS as promises após completarem (sucesso ou falha). Resultado: [{ status: "fulfilled", value }, { status: "rejected", reason }]. Não há fail-fast — aguarda todas. Útil para: operações onde você quer saber o resultado individual, não um tudo-ou-nada. Ideal para: validação de múltiplas URLs, processamento em lote onde falhas são aceitáveis.', exemplo: 'Promise.allSettled([ok(), fail()]).then(r => r.forEach(console.log))' },
          { titulo: 'Promise.any — primeira a resolver', descricao: 'Promise.any([promises]) retorna a PRIMEIRA promise que RESOLVER (ignora rejeições). Se todas rejeitarem, retorna AggregateError. Diferente de race que considera rejeição. Útil para: tentar múltiplas fontes de dados e usar a primeira que responder (CDN fallback). É o oposto de Promise.all — "qualquer uma serve". ES2021. Menos suportada que as outras, mas útil em cenários de redundância.', exemplo: 'Promise.any([cdn1, cdn2]).then(r => console.log("usando", r))' }
        ],
        exercicio: 'Simule busca paralela de usuário e posts com Promise.all. Use race para timeout. Use allSettled para operações mistas.',
        codigoInicial: `function delay(ms, valor) {\n  return new Promise(resolve => setTimeout(() => resolve(valor), ms));\n}\n\nfunction falhar(ms) {\n  return new Promise((_, reject) => setTimeout(() => reject(new Error("Falhou")), ms));\n}\n\n// Promise.all\nPromise.all([delay(1000, "A"), delay(500, "B"), delay(800, "C")])\n  .then(console.log);\n\n// Promise.race\nPromise.race([delay(2000, "Lento"), delay(500, "Rápido")])\n  .then(console.log);`,
        codigoSolucao: `function delay(ms, valor) {\n  return new Promise(resolve => setTimeout(() => resolve(valor), ms));\n}\n\nfunction falhar(ms) {\n  return new Promise((_, reject) => setTimeout(() => reject(new Error("Falhou")), ms));\n}\n\n// Promise.all\nPromise.all([delay(1000, "A"), delay(500, "B"), delay(800, "C")])\n  .then(console.log); // ["A", "B", "C"] ~1s (mais lenta)\n\n// Promise.race\nPromise.race([delay(2000, "Lento"), delay(500, "Rápido")])\n  .then(console.log); // "Rápido" (500ms)\n\n// Promise.allSettled\nPromise.allSettled([delay(500, "OK"), falhar(300), delay(700, "OK2")])\n  .then(console.log);\n// [\n//   { status: "fulfilled", value: "OK" },\n//   { status: "rejected", reason: Error(\"Falhou\") },\n//   { status: "fulfilled", value: "OK2" }\n// ]\n\n// Promise.any (ES2021)\nPromise.any([falhar(300), delay(500, "OK"), delay(700, "OK2")])\n  .then(console.log); // "OK" (ignora a rejeição)\n\n// Timeout com race\nconst comTimeout = Promise.race([\n  delay(3000, "Dados carregados"),\n  delay(2000).then(() => { throw new Error("Timeout!") })\n]).catch(err => console.error(err.message));`
      },
      {
        dia: 4,
        titulo: 'Async/Await',
        teoria: 'async/await é syntax sugar sobre Promises. async declara função assíncrona que sempre retorna Promise. await pausa a execução até a promise resolver.',
        topicos: [
          { titulo: 'async function', descricao: 'async function() { } sempre retorna uma Promise. Se a função retorna um valor, a promise resolve com ele. Se lança um erro, a promise rejeita. async pode ser usado com: function declaration, arrow functions, métodos de objeto, class methods. Exemplo: const fetchData = async () => { return "dados" } → fetchData() retorna Promise que resolve com "dados". async/await torna código assíncrono tão legível quanto síncrono.', exemplo: 'async function getData() { return await fetch(url) }' },
          { titulo: 'await — pausar execução', descricao: 'await só pode ser usado dentro de async function. await promise pausa a execução da função (mas não bloqueia o thread — outras funções continuam executando). await Promise.resolve(5) retorna 5. await não pode ser usado no escopo global (exceto em módulos ES2022+). Para usar await fora de async: Promise.resolve().then(async () => { await ... }) ou IIFE async: (async () => { await ... })().', exemplo: 'const data = await fetch(url); console.log(data)' },
          { titulo: 'Try/catch com async/await', descricao: 'Use try/catch para tratar erros em await: try { const data = await fetch(url); } catch (err) { console.error(err) }. Isso substitui o catch de Promises. Para erros específicos, vários catch ou if/else dentro do catch. finally sempre executa. try/catch com async/await é mais legível que .catch() para fluxos complexos. Erro não tratado em async retorna promise rejeitada — use try/catch ou chain com .catch().', exemplo: 'try { const r = await fetch(url) } catch (e) { console.error(e) }' },
          { titulo: 'Paralelo com async/await', descricao: 'await sequencial: const a = await fetchA(); const b = await fetchB() — B espera A terminar. Paralelo: const [a, b] = await Promise.all([fetchA(), fetchB()]) — ambos executam simultaneamente. Não use await em loop forEach (não espera) — use for...of ou Promise.all. Para paralelo com limite: divida em chunks ou use bibliotecas como p-limit. Entender quando serializar vs paralelizar é essencial para performance.', exemplo: 'const [a, b] = await Promise.all([fetchA(), fetchB()])' }
        ],
        exercicio: 'Refatore o código de Promises do dia 2 para async/await. Adicione try/catch. Use Promise.all para paralelizar.',
        codigoInicial: `function delay(ms, valor) {\n  return new Promise(resolve => setTimeout(() => resolve(valor), ms));\n}\n\n// Refatore para async/await\nfunction buscarUsuario(id) {\n  return delay(1000, { id, nome: "Ana" });\n}\n\nfunction buscarPedidos(usuarioId) {\n  return delay(1000, [{ id: 101, total: 200 }]);\n}\n\nasync function carregarDados() {\n  try {\n    const usuario = await buscarUsuario(1);\n    console.log("Usuário:", usuario);\n    const pedidos = await buscarPedidos(usuario.id);\n    console.log("Pedidos:", pedidos);\n  } catch (err) {\n    console.error(err);\n  }\n}\n\ncarregarDados();`,
        codigoSolucao: `function delay(ms, valor) {\n  return new Promise(resolve => setTimeout(() => resolve(valor), ms));\n}\n\nfunction buscarUsuario(id) {\n  return delay(1000, { id, nome: "Ana" });\n}\n\nfunction buscarPedidos(usuarioId) {\n  return delay(1000, [{ id: 101, total: 200 }]);\n}\n\nasync function carregarDados() {\n  try {\n    // Sequencial\n    const usuario = await buscarUsuario(1);\n    console.log("Usuário:", usuario);\n    \n    const pedidos = await buscarPedidos(usuario.id);\n    console.log("Pedidos:", pedidos);\n    \n    return { usuario, pedidos };\n  } catch (err) {\n    console.error("Erro:", err);\n    throw err;\n  }\n}\n\n// Paralelo com Promise.all\nasync function carregarParalelo() {\n  const [usuario, pedidos] = await Promise.all([\n    buscarUsuario(1),\n    buscarPedidos(1)\n  ]);\n  console.log("Paralelo:", { usuario, pedidos });\n}\n\ncarregarDados().then(() => carregarParalelo());\n\n// Loop com async (for...of)\nasync function processarItens(itens) {\n  for (const item of itens) {\n    await delay(500, item);\n    console.log(\"Processado:\", item);\n  }\n}`
      },
      {
        dia: 5,
        titulo: 'Fetch API e Consumo de APIs',
        teoria: 'Fetch API é a forma moderna de fazer requisições HTTP. Combinado com async/await, permite consumir APIs REST de forma limpa e eficiente.',
        topicos: [
          { titulo: 'Fetch básico', descricao: 'fetch(url) retorna uma Promise que resolve com o objeto Response. response.ok (boolean), response.status (200, 404, etc), response.statusText. Para obter o corpo: response.json() (JSON), response.text() (texto), response.blob() (binário). Fetch não rejeita para HTTP errors (404, 500) — só rejeita para erros de rede. Verifique response.ok ou response.status para tratar erros HTTP.', exemplo: 'fetch("https://api.github.com/users/octocat").then(r => r.json())' },
          { titulo: 'Requisições POST e headers', descricao: 'fetch(url, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(dados) }). Outros métodos: PUT, DELETE, PATCH. Headers personalizados: { "Authorization": "Bearer token" }. Para FormData: body: formData (sem setar Content-Type — o browser seta automaticamente com boundary). Para enviar dados URL-encoded: new URLSearchParams({ chave: "valor" }).', exemplo: 'fetch("/api", { method: "POST", body: JSON.stringify(dados) })' },
          { titulo: 'Tratamento de erros e timeouts', descricao: 'Trate erros HTTP: if (!response.ok) { throw new Error(HTTP \${response.status}) }. Trate erros de rede: catch captura falhas de conexão. Para timeout: Promise.race([fetch(url), new Promise((_, reject) => setTimeout(() => reject(new Error("Timeout")), 5000))]). Para cancelamento: AbortController — const controller = new AbortController(); fetch(url, { signal: controller.signal }); controller.abort().', exemplo: 'if (!res.ok) throw new Error(`HTTP ${res.status}`)' },
          { titulo: 'APIs públicas para praticar', descricao: 'APIs gratuitas: JSONPlaceholder (jsonplaceholder.typicode.com — posts, users), PokéAPI (pokeapi.co), GitHub API (api.github.com), ViaCEP (viacep.com.br). Exemplo: fetch("https://api.github.com/users/octocat").then(r => r.json()).then(d => console.log(d.login)). Sempre trate erros de rede e resposta. Use async/await para legibilidade. APIs podem ter rate limiting — respeite os limites.', exemplo: 'fetch("https://jsonplaceholder.typicode.com/posts/1").then(r => r.json())' }
        ],
        exercicio: 'Busque dados da API JSONPlaceholder: /users e /posts. Exiba nome e email dos usuários. Conte posts por usuário com reduce.',
        codigoInicial: `async function buscarUsuarios() {\n  try {\n    const resposta = await fetch("https://jsonplaceholder.typicode.com/users");\n    if (!resposta.ok) throw new Error(\`HTTP \${resposta.status}\`);\n    return await resposta.json();\n  } catch (err) {\n    console.error("Erro:", err);\n    return [];\n  }\n}\n\nasync function buscarPosts() {\n  // Similar\n}\n\nasync function main() {\n  const usuarios = await buscarUsuarios();\n  const posts = await buscarPosts();\n  \n  // Exiba usuários\n  usuarios.forEach(u => console.log(\`\${u.name} - \${u.email}\`));\n  \n  // Conte posts por usuário\n}`,
        codigoSolucao: `async function buscarUsuarios() {\n  const resposta = await fetch("https://jsonplaceholder.typicode.com/users");\n  if (!resposta.ok) throw new Error(\`HTTP \${resposta.status}\`);\n  return await resposta.json();\n}\n\nasync function buscarPosts() {\n  const resposta = await fetch("https://jsonplaceholder.typicode.com/posts");\n  if (!resposta.ok) throw new Error(\`HTTP \${resposta.status}\`);\n  return await resposta.json();\n}\n\nasync function main() {\n  try {\n    const [usuarios, posts] = await Promise.all([buscarUsuarios(), buscarPosts()]);\n    \n    console.log("=== Usuários ===");\n    usuarios.forEach(u => console.log(\`\${u.name} - \${u.email}\`));\n    \n    const postsPorUsuario = posts.reduce((acc, post) => {\n      acc[post.userId] = (acc[post.userId] || 0) + 1;\n      return acc;\n    }, {});\n    \n    console.log("\\n=== Posts por Usuário ===");\n    usuarios.forEach(u => {\n      console.log(\`\${u.name}: \${postsPorUsuario[u.id] || 0} posts\`);\n    });\n  } catch (err) {\n    console.error("Falha ao carregar dados:", err.message);\n  }\n}\n\nmain();`
      }
    ],
    miniProjeto: {
      titulo: 'Consumidor de API',
      descricao: 'Aplicação que busca dados de API pública com fetch, async/await e Promise.all',
      requisitos: [
        'Buscar múltiplos endpoints com Promise.all',
        'Tratar erros HTTP e de rede',
        'Transformar dados com map/filter/reduce',
        'Timeout com Promise.race',
        'Exibir resultados formatados'
      ]
    }
  },
  {
    semana: 9,
    titulo: 'Programação Orientada a Objetos',
    descricao: 'Aprenda POO em JavaScript: constructor functions, protótipos, classes ES6, herança, encapsulamento e composição',
    instrucoes: 'JavaScript tem um sistema de orientação a objetos único baseado em protótipos, diferente de linguagens clássicas como Java ou C++. Esta semana começa com constructor functions (a base) e avança para classes ES6 (o açúcar sintático). Entenda a cadeia de protótipos (prototype chain) — é fundamental para depurar e usar bibliotecas. O mini-projeto final vai integrar classes, herança e composição em um sistema completo.',
    dias: [
      {
        dia: 1,
        titulo: 'Constructor Functions e Prototype',
        teoria: 'Antes das classes ES6, objetos eram criados com constructor functions. O prototype é o mecanismo de herança do JavaScript.',
        topicos: [
          { titulo: 'Constructor functions', descricao: 'function Pessoa(nome) { this.nome = nome } — quando chamada com new, cria um novo objeto, define this como o novo objeto, executa o corpo, e retorna o objeto (a menos que retorne explicitamente outro objeto). Por convenção, constructor functions têm Nome Maiúsculo. new Pessoa("Ana") cria { nome: "Ana" }. Sem new, this seria o objeto global (window/undefined em strict mode) — erro comum.', exemplo: 'function Pessoa(nome) { this.nome = nome }; new Pessoa("Ana")' },
          { titulo: 'O operador new', descricao: 'new executa 4 passos: 1) Cria objeto vazio que herda de Constructor.prototype. 2) Define this como o novo objeto. 3) Executa o constructor. 4) Retorna o objeto (se o constructor não retornar um objeto explicitamente). O novo objeto tem uma ligação interna [[Prototype]] (__proto__) para Constructor.prototype. new.target detecta se a função foi chamada com new (undefined se não).', exemplo: 'const p = new Pessoa("Ana"); console.log(p.nome) // "Ana"' },
          { titulo: 'Propriedades no prototype', descricao: 'Constructor.prototype é um objeto compartilhado por todas as instâncias. Métodos definidos em prototype são compartilhados (não copiados para cada instância): Pessoa.prototype.saudar = function() { return "Olá, " + this.nome }. Isso economiza memória. Propriedades definidas no constructor via this são PRÓPRIAS da instância. Propriedades no prototype são COMPARTILHADAS. É por isso que métodos ficam no prototype, dados no constructor.', exemplo: 'Pessoa.prototype.saudar = function() { return "Olá " + this.nome }' },
          { titulo: 'Cadeia de protótipos (prototype chain)', descricao: 'Quando você acessa obj.prop, o JavaScript procura: 1) Nas propriedades próprias do obj, 2) No obj.__proto__ (Constructor.prototype), 3) No __proto__ do prototype (Object.prototype), 4) null (undefined). Isso é a cadeia de protótipos. Object.prototype está no topo — tem métodos como toString(), hasOwnProperty(), valueOf(). obj.__proto__ é obsoleto — use Object.getPrototypeOf(obj) ou Reflect.getPrototypeOf().', exemplo: 'Object.getPrototypeOf(p) === Pessoa.prototype // true' }
        ],
        exercicio: 'Crie uma constructor function Produto com nome e preço. Adicione métodos no prototype. Crie instâncias e teste a cadeia de protótipos.',
        codigoInicial: `function Produto(nome, preco) {\n  this.nome = nome;\n  this.preco = preco;\n}\n\n// Adicione método no prototype\nProduto.prototype.exibir = function() {\n  return \`\${this.nome} - R$ \${this.preco.toFixed(2)}\`;\n};\n\nProduto.prototype.aplicarDesconto = function(percentual) {\n  this.preco = this.preco * (1 - percentual / 100);\n};\n\nconst p1 = new Produto("Notebook", 3500);\nconst p2 = new Produto("Mouse", 150);\n\nconsole.log(p1.exibir());\nconsole.log(p2.exibir());\n\n// Verifique o protótipo\nconsole.log(Object.getPrototypeOf(p1) === Produto.prototype);`,
        codigoSolucao: `function Produto(nome, preco) {\n  this.nome = nome;\n  this.preco = preco;\n}\n\nProduto.prototype.exibir = function() {\n  return \`\${this.nome} - R$ \${this.preco.toFixed(2)}\`;\n};\n\nProduto.prototype.aplicarDesconto = function(percentual) {\n  this.preco = this.preco * (1 - percentual / 100);\n};\n\nconst p1 = new Produto("Notebook", 3500);\nconst p2 = new Produto("Mouse", 150);\n\np1.aplicarDesconto(10);\nconsole.log(p1.exibir()); // "Notebook - R$ 3150.00"\nconsole.log(p2.exibir()); // "Mouse - R$ 150.00"\n\n// Métodos estão no prototype, não nas instâncias\nconsole.log(p1.hasOwnProperty("exibir")); // false\nconsole.log(Produto.prototype.hasOwnProperty("exibir")); // true\n\n// Cadeia de protótipos\nconsole.log(Object.getPrototypeOf(p1) === Produto.prototype); // true\nconsole.log(Object.getPrototypeOf(Produto.prototype) === Object.prototype); // true\nconsole.log(Object.getPrototypeOf(Object.prototype)); // null`
      },
      {
        dia: 2,
        titulo: 'Classes ES6',
        teoria: 'class é syntax sugar sobre constructor functions e prototype. Oferece sintaxe mais clara: constructor, métodos, getters/setters e propriedades.',
        topicos: [
          { titulo: 'Declaração de classe', descricao: 'class Pessoa { constructor(nome) { this.nome = nome } saudar() { return \`Olá, \${this.nome}\` } } — class não é hoisted (diferente de function). class cria uma função (typeof Pessoa === "function"). Métodos definidos dentro da classe vão para Pessoa.prototype. class não tem vírgulas entre membros. O corpo é executado em strict mode automaticamente. class não pode ser chamada sem new (TypeError).', exemplo: 'class Pessoa { constructor(nome) { this.nome = nome } saudar() { return "Olá " + this.nome } }' },
          { titulo: 'Constructor e propriedades', descricao: 'constructor() é chamado automaticamente com new. Pode receber parâmetros para inicializar a instância. Propriedades são definidas no constructor com this.prop = valor. Propriedades públicas (ES2022): class Pessoa { nome = "Anônimo"; idade = 0 } — são inicializadas antes do constructor. Propriedades privadas (ES2022): #saldo = 0 — só acessível dentro da classe. Métodos públicos/privados: #metodoPrivado() { }.', exemplo: 'class Contador { #count = 0; incrementar() { this.#count++ } }' },
          { titulo: 'Getters e Setters', descricao: 'get propriedade() { return this._prop } — permite acessar como propriedade, não método: obj.propriedade (sem parênteses). set propriedade(valor) { this._prop = valor } — permite validação na atribuição: obj.propriedade = "valor". Getters/setters são úteis para: propriedades computadas (get area() { return this.largura * this.altura }), validação, encapsulamento, compatibilidade com API.', exemplo: 'get area() { return this.largura * this.altura }' },
          { titulo: 'Métodos estáticos', descricao: 'static metodo() { } — método na classe, não na instância: Pessoa.metodo(). Usos: factories (Pessoa.create({...})), utilitários (Pessoa.validarNome()), singletons (Database.getInstance()). Métodos estáticos NÃO têm acesso a this de instância — recebem a classe como this. Herdados por subclasses. Pode ter getters/setters estáticos: static get version() { return "1.0" }.', exemplo: 'static criar(nome) { return new Pessoa(nome) }' }
        ],
        exercicio: 'Converta a constructor function Produto para classe ES6. Adicione getter para precoFormatado, propriedade privada #id, e método estático.',
        codigoInicial: `class Produto {\n  // Propriedades públicas\n  nome;\n  preco;\n  // Propriedade privada\n  #id;\n  \n  constructor(nome, preco) {\n    this.nome = nome;\n    this.preco = preco;\n    this.#id = Math.random().toString(36).slice(2);\n  }\n  \n  exibir() {\n    return \`\${this.nome} - \${this.precoFormatado}\`;\n  }\n  \n  get precoFormatado() {\n    return \`R$ \${this.preco.toFixed(2)}\`;\n  }\n  \n  getId() {\n    return this.#id;\n  }\n  \n  static criar(nome, preco) {\n    return new Produto(nome, preco);\n  }\n}\n\nconst p1 = new Produto("Notebook", 3500);\nconsole.log(p1.exibir());\nconst p2 = Produto.criar("Mouse", 150);\nconsole.log(p2.getId());`,
        codigoSolucao: `class Produto {\n  nome;\n  preco;\n  #id;\n  \n  constructor(nome, preco) {\n    this.nome = nome;\n    this.preco = preco;\n    this.#id = Math.random().toString(36).slice(2);\n  }\n  \n  get precoFormatado() {\n    return \`R$ \${this.preco.toFixed(2)}\`;\n  }\n  \n  set preco(novoPreco) {\n    if (novoPreco < 0) throw new Error(\"Preço não pode ser negativo\");\n    this._preco = novoPreco;\n  }\n  \n  get preco() {\n    return this._preco;\n  }\n  \n  exibir() {\n    return \`\${this.nome} - \${this.precoFormatado} (ID: \${this.#id})\`;\n  }\n  \n  getId() {\n    return this.#id;\n  }\n  \n  static criar(nome, preco) {\n    return new Produto(nome, preco);\n  }\n}\n\nconst p1 = new Produto("Notebook", 3500);\nconsole.log(p1.exibir());\nconsole.log(p1.precoFormatado); // "R$ 3500.00"\n\nconst p2 = Produto.criar("Mouse", 150);\nconsole.log(p2.getId());\n\nconsole.log(p1.#id); // SyntaxError — privado!`
      },
      {
        dia: 3,
        titulo: 'Herança com Classes',
        teoria: 'Herança permite que uma classe derive de outra, reutilizando código. extends estabelece a relação, super chama o constructor/métodos da classe pai.',
        topicos: [
          { titulo: 'extends — herança', descricao: 'class Admin extends Usuario { } — Admin herda todas as propriedades e métodos de Usuario. instanceof verifica: admin instanceof Admin (true), admin instanceof Usuario (true). A cadeia de protótipos: Admin.prototype.__proto__ === Usuario.prototype. extends também funciona com constructor functions tradicionais e objetos com prototype. class não pode extender múltiplas classes (herança simples).', exemplo: 'class Admin extends Usuario {}' },
          { titulo: 'super — classe pai', descricao: 'No constructor da subclasse, super() DEVE ser chamado antes de usar this — é obrigatório se a subclasse tiver constructor próprio. super() chama o constructor da classe pai. super.metodo() chama métodos da classe pai (útil para estender comportamento). Exemplo: constructor(nome, email, cargo) { super(nome, email); this.cargo = cargo }. Se a subclasse não tem constructor, super() é chamado implicitamente.', exemplo: 'constructor(nome, email, cargo) { super(nome, email); this.cargo = cargo }' },
          { titulo: 'Sobrescrita de métodos (override)', descricao: 'Subclasse pode redefinir métodos da classe pai com a mesma assinatura. Dentro do novo método, pode chamar super.metodoOriginal() para estender. Exemplo: class Admin extends Usuario { saudar() { return super.saudar() + " [Admin]" } }. Para garantir que está sobrescrevendo (e não criando novo): TypeScript tem override keyword. Em JS puro, documente com comentários.', exemplo: 'saudar() { return super.saudar() + " [Admin]" }' },
          { titulo: 'instanceof e herança', descricao: 'obj instanceof Classe verifica se Classe.prototype está na cadeia de protótipos de obj. Útil para: type checking, tratamento diferenciado. obj.constructor.name retorna o nome da classe. Cuidado: instanceof não funciona entre realms (iframes diferentes). Para verificar herança: isPrototypeOf: Classe.prototype.isPrototypeOf(obj). Herança em JS é baseada em protótipos, não em classes (como Java) — instanceof reflete isso.', exemplo: 'admin instanceof Admin // true; admin instanceof Usuario // true' }
        ],
        exercicio: 'Crie uma classe base Funcionario e subclasses Desenvolvedor e Gerente. Use super e sobrescrita de métodos.',
        codigoInicial: `class Funcionario {\n  constructor(nome, salario) {\n    this.nome = nome;\n    this.salario = salario;\n  }\n  \n  getInfo() {\n    return \`\${this.nome} - R$ \${this.salario.toFixed(2)}\`;\n  }\n  \n  calcularBonus() {\n    return this.salario * 0.1;\n  }\n}\n\nclass Desenvolvedor extends Funcionario {\n  constructor(nome, salario, linguagem) {\n    super(nome, salario);\n    this.linguagem = linguagem;\n  }\n  \n  getInfo() {\n    return \`\${super.getInfo()} - Dev \${this.linguagem}\`;\n  }\n  \n  calcularBonus() {\n    return super.calcularBonus() + 500;\n  }\n}`,
        codigoSolucao: `class Funcionario {\n  constructor(nome, salario) {\n    this.nome = nome;\n    this.salario = salario;\n  }\n  \n  getInfo() {\n    return \`\${this.nome} - R$ \${this.salario.toFixed(2)}\`;\n  }\n  \n  calcularBonus() {\n    return this.salario * 0.1;\n  }\n}\n\nclass Desenvolvedor extends Funcionario {\n  constructor(nome, salario, linguagem) {\n    super(nome, salario);\n    this.linguagem = linguagem;\n  }\n  \n  getInfo() {\n    return \`\${super.getInfo()} - Dev \${this.linguagem}\`;\n  }\n  \n  calcularBonus() {\n    return super.calcularBonus() + 500;\n  }\n}\n\nclass Gerente extends Funcionario {\n  constructor(nome, salario, equipe) {\n    super(nome, salario);\n    this.equipe = equipe;\n  }\n  \n  calcularBonus() {\n    return super.calcularBonus() + this.equipe * 200;\n  }\n}\n\nconst dev = new Desenvolvedor("Ana", 8000, "JavaScript");\nconst gerente = new Gerente("Carlos", 12000, 5);\n\nconsole.log(dev.getInfo());      // "Ana - R$ 8000.00 - Dev JavaScript"\nconsole.log("Bônus Dev:", dev.calcularBonus());     // 1300\nconsole.log(gerente.getInfo());  // "Carlos - R$ 12000.00"\nconsole.log("Bônus Gerente:", gerente.calcularBonus()); // 2200\n\nconsole.log(dev instanceof Funcionario);  // true\nconsole.log(dev instanceof Desenvolvedor); // true\nconsole.log(dev instanceof Gerente);       // false`
      },
      {
        dia: 4,
        titulo: 'Composição vs Herança',
        teoria: '"Prefira composição sobre herança" é um princípio importante. Mixins e composição permitem reutilizar comportamento sem herança rígida.',
        topicos: [
          { titulo: 'Problemas da herança', descricao: 'Herança pode causar: 1) Acoplamento forte (mudanças na classe base afetam todas as subclasses). 2) Herança profunda difícil de entender. 3) Problema do diamante (herança múltipla — JS não suporta). 4) Força uma hierarquia que nem sempre reflete a realidade. Exemplo: "Pato" extends Animal, mas e se o Pato voa? "Avestruz" também extends Animal mas não voa. Herança força categorizações binárias.', exemplo: 'class Avestruz extends Animal {} // Avestruz não voa, mas herda voar()' },
          { titulo: 'Composição', descricao: 'Composição é "ter" em vez de "ser": um objeto contém outros objetos com comportamentos específicos. Exemplo: function criarPato() { return { nome: "Pato", andar: criarAndar(), voar: criarVoar(), nadar: criarNadar() } }. Cada habilidade é uma função independente. Vantagens: flexível, sem acoplamento, fácil de testar, reutilização granular. Composição é favorecida em padrões como React Hooks.', exemplo: 'const pato = { ...andar, ...voar, ...nadar, nome: "Pato" }' },
          { titulo: 'Mixins em JavaScript', descricao: 'Mixins são objetos com métodos que podem ser copiados para outros objetos. Object.assign(target, mixin) ou spread: { ...target, ...mixin }. Exemplo: const Andavel = { andar() { console.log("Andando") } }; class Pessoa { } Object.assign(Pessoa.prototype, Andavel). Mixins combinam múltiplos comportamentos sem herança. Útil quando vários objetos compartilham habilidades mas não fazem sentido em hierarquia.', exemplo: 'Object.assign(Pessoa.prototype, Andavel, Falante)' },
          { titulo: 'Factory functions', descricao: 'Factory functions criam objetos sem new ou class: function criarUsuario(nome) { return { nome, saudar() { return \`Olá, \${nome}\` } } }. Vantagens: sem this, sem problemas de new, encapsulamento verdadeiro (closures), composição natural. Factories combinam bem com composição: return { ...criarHabilidadeA(), ...criarHabilidadeB(), nome }. É uma alternativa moderna a classes para muitos casos.', exemplo: 'function criarPessoa(nome) { return { nome, saudar: () => "Olá " + nome } }' }
        ],
        exercicio: 'Refatore um sistema de herança para composição usando mixins ou factory functions. Compare a flexibilidade.',
        codigoInicial: `// Herança (antes)\nclass Animal {\n  constructor(nome) { this.nome = nome; }\n}\n\nclass Voador extends Animal {\n  voar() { return \`\${this.nome} voou\`; }\n}\n\nclass Nadador extends Animal {\n  nadar() { return \`\${this.nome} nadou\`; }\n}\n\n// Problema: e se um animal voa E nada? (Pato)\n\n// Composição (depois)\nconst habilidades = {\n  voar() { return \`\${this.nome} voou\`; },\n  nadar() { return \`\${this.nome} nadou\`; },\n  andar() { return \`\${this.nome} andou\`; }\n};`,
        codigoSolucao: `const habilidades = {\n  voar() { return \`\${this.nome} voou\`; },\n  nadar() { return \`\${this.nome} nadou\`; },\n  andar() { return \`\${this.nome} andou\`; }\n};\n\nfunction criarAnimal(nome, ...habils) {\n  const animal = { nome };\n  habils.forEach(h => Object.assign(animal, { [h]: habilidades[h] }));\n  return animal;\n}\n\nconst pato = criarAnimal("Pato", "voar", "nadar", "andar");\nconst cachorro = criarAnimal("Rex", "andar", "nadar");\nconst aguia = criarAnimal("Águia", "voar", "andar");\n\nconsole.log(pato.voar());  // "Pato voou"\nconsole.log(pato.nadar()); // "Pato nadou"\n\n// Factory function com composição\nfunction criarPato(nome) {\n  return {\n    nome,\n    voar: () => \`\${nome} voou\`,\n    nadar: () => \`\${nome} nadou\`,\n    andar: () => \`\${nome} andou\`\n  };\n}\n\nconst pato2 = criarPato("Donald");\nconsole.log(pato2.voar());  // "Donald voou"\nconsole.log(pato2.nadar()); // "Donald nadou"`
      },
      {
        dia: 5,
        titulo: 'Mini-Projeto: Sistema de Funcionários',
        teoria: 'Combine classes, herança, composição e encapsulamento em um sistema completo de gestão de funcionários com cálculo de salários e bônus.',
        topicos: [
          { titulo: 'Arquitetura do sistema', descricao: 'Classe base Funcionario com propriedades comuns (nome, salarioBase, dataContratacao). Subclasses: CLT, PJ, Estagiario, cada uma com regras próprias de cálculo de salário e benefícios. Composição para habilidades (programar, liderar, etc.). Métodos estáticos para utilitários (calcularIR, validarCPF). Privacidade com # para dados sensíveis (CPF, salário real).', exemplo: 'class CLT extends Funcionario { calcularSalario() { return this.salarioBase + this.valeRefeicao } }' },
          { titulo: 'Polimorfismo', descricao: 'Cada subclasse implementa seu próprio calcularSalario(), calcularFerias(), calcularBeneficios(). O código que usa as classes não precisa saber o tipo específico: funcionarios.forEach(f => total += f.calcularSalario()). Isso é polimorfismo — mesmo método, comportamentos diferentes. Para adicionar um novo tipo (PJ, terceirizado), basta criar mais uma subclasse sem modificar o código existente.', exemplo: 'funcionarios.forEach(f => total += f.calcularSalario()) // cada tipo calcula diferente' }
        ],
        exercicio: 'Implemente o sistema com classe base, 2-3 subclasses, método polimórfico calcularSalario, e composição para habilidades.',
        codigoInicial: `class Funcionario {\n  #cpf;\n  constructor(nome, salarioBase, cpf) {\n    this.nome = nome;\n    this.salarioBase = salarioBase;\n    this.#cpf = cpf;\n  }\n  \n  calcularSalario() {\n    return this.salarioBase;\n  }\n  \n  getInfo() {\n    return \`\${this.nome}: R$ \${this.calcularSalario().toFixed(2)}\`;\n  }\n}\n\nclass CLT extends Funcionario {\n  constructor(nome, salarioBase, cpf, valeRefeicao) {\n    super(nome, salarioBase, cpf);\n    this.valeRefeicao = valeRefeicao;\n  }\n  \n  calcularSalario() {\n    return this.salarioBase + this.valeRefeicao;\n  }\n}\n\nclass PJ extends Funcionario {\n  calcularSalario() {\n    return this.salarioBase * 1.1; // +10% de benefício\n  }\n}\n\nconst funcionarios = [\n  new CLT("Ana", 5000, "123.456.789-00", 800),\n  new PJ("Bruno", 8000, "987.654.321-00")\n];\n\nfuncionarios.forEach(f => console.log(f.getInfo()));`,
        codigoSolucao: `class Funcionario {\n  #cpf;\n  constructor(nome, salarioBase, cpf) {\n    this.nome = nome;\n    this.salarioBase = salarioBase;\n    this.#cpf = cpf;\n  }\n  \n  calcularSalario() {\n    return this.salarioBase;\n  }\n  \n  getInfo() {\n    return \`\${this.nome}: R$ \${this.calcularSalario().toFixed(2)}\`;\n  }\n  \n  getCpfMascarado() {\n    return this.#cpf.slice(0, 3) + ".***.***-**";\n  }\n}\n\nclass CLT extends Funcionario {\n  constructor(nome, salarioBase, cpf, valeRefeicao = 800) {\n    super(nome, salarioBase, cpf);\n    this.valeRefeicao = valeRefeicao;\n  }\n  \n  calcularSalario() {\n    return this.salarioBase + this.valeRefeicao;\n  }\n  \n  getInfo() {\n    return \`[CLT] \${super.getInfo()} + VR R$ \${this.valeRefeicao.toFixed(2)}\`;\n  }\n}\n\nclass PJ extends Funcionario {\n  calcularSalario() {\n    return this.salarioBase * 1.1;\n  }\n  \n  getInfo() {\n    return \`[PJ] \${super.getInfo()}\`;\n  }\n}\n\nclass Estagiario extends Funcionario {\n  constructor(nome, salarioBase, cpf) {\n    super(nome, salarioBase, cpf);\n  }\n  \n  calcularSalario() {\n    return this.salarioBase; // sem benefícios\n  }\n}\n\nconst funcionarios = [\n  new CLT("Ana", 5000, "123.456.789-00", 800),\n  new PJ("Bruno", 8000, "987.654.321-00"),\n  new Estagiario("Carla", 2000, "111.222.333-44")\n];\n\nfuncionarios.forEach(f => console.log(f.getInfo()));\n\n// Polimorfismo — cada um calcula de seu jeito\nconst totalFolha = funcionarios.reduce((acc, f) => acc + f.calcularSalario(), 0);\nconsole.log(\"Total folha: R$\", totalFolha.toFixed(2));`
      }
    ],
    miniProjeto: {
      titulo: 'Sistema de Gestão de Funcionários',
      descricao: 'Sistema com classes, herança, polimorfismo, encapsulamento e composição',
      requisitos: [
        'Classe base Funcionario com dados privados (#cpf)',
        'Subclasses CLT, PJ, Estagiario com polimorfismo',
        'Factory function ou mixin para habilidades',
        'Cálculo de salário polimórfico',
        'Relatório com total da folha'
      ]
    }
  },
  {
    semana: 10,
    titulo: 'Projeto Final e Tópicos Avançados',
    descricao: 'Consolide tudo que aprendeu: módulos, error handling, Web APIs, e um projeto final que integra todos os conceitos',
    instrucoes: 'Esta é a semana de consolidação. Os tópicos focam em ferramentas essenciais para o mercado: módulos para organização, error handling para código robusto, Web APIs para aplicações ricas, e um projeto final que integra absolutamente tudo que você aprendeu nas 9 semanas anteriores. Use este projeto final como portfólio. Capriche na organização, tratamento de erros e na experiência do usuário.',
    dias: [
      {
        dia: 1,
        titulo: 'Módulos: import/export',
        teoria: 'Módulos permitem dividir o código em arquivos independentes. export expõe valores, import consome. ES Modules é o padrão moderno.',
        topicos: [
          { titulo: 'Named exports e imports', descricao: 'export const PI = 3.14; export function somar(a, b) { return a + b } — múltiplos exports por arquivo. import { PI, somar } from "./math.js". Para renomear: import { somar as adicionar } from "./math.js". Named exports são explícitos e favorecem tree-shaking (bundlers removem exports não usados). São a forma recomendada para bibliotecas.', exemplo: 'export const PI = 3.14; import { PI } from "./math.js"' },
          { titulo: 'Default exports e imports', descricao: 'export default function() { } — UM default export por módulo. import minhaFuncao from "./modulo.js". Default exports são convenientes para componentes e classes principais. Import pode ter qualquer nome (não precisa corresponder ao export). Controvérsia: alguns preferem apenas named exports por serem mais explícitos. Em projetos grandes, defaults podem causar confusão de nomes.', exemplo: 'export default class Timer {}; import Timer from "./timer.js"' },
          { titulo: 'Re-exporting e barrel files', descricao: 'export { funcao } from "./outro.js" — re-exporta sem importar no arquivo atual. export * from "./utils.js" (exporta tudo exceto default). Barrel files (index.js) agrupam exports: export { ComponenteA } from "./ComponenteA"; export { ComponenteB } from "./ComponenteB"}. Import: import { ComponenteA, ComponenteB } from "./components". Barrel files simplificam imports.', exemplo: 'export { Componente } from "./Componente"' },
          { titulo: 'Módulos vs scripts tradicionais', descricao: 'Módulos (type="module" no HTML) têm escopo próprio (não poluem global), executam em strict mode, suportam import/export, são defer por padrão. Scripts tradicionais (<script> sem type="module") têm escopo global, executam imediatamente, sem import/export. Módulos podem ser usados no navegador e Node.js. No Node.js, use .mjs ou package.json com "type": "module".', exemplo: '<script type="module" src="main.js"></script>' }
        ],
        exercicio: 'Crie 3 módulos: math.js (operações), string.js (formatação), main.js (usa ambos). Use named e default exports.',
        codigoInicial: `// math.js\nexport function somar(a, b) { return a + b; }\nexport function subtrair(a, b) { return a - b; }\nexport const PI = 3.14159;\n\n// string.js\nexport default function capitalize(str) {\n  return str.charAt(0).toUpperCase() + str.slice(1);\n}\n\n// main.js\nimport { somar, subtrair, PI } from "./math.js";\nimport capitalize from "./string.js";\n\nconsole.log(somar(5, 3));\nconsole.log(capitalize("olá mundo"));`,
        codigoSolucao: `// math.js\nexport function somar(a, b) { return a + b; }\nexport function subtrair(a, b) { return a - b; }\nexport function multiplicar(a, b) { return a * b; }\nexport const PI = 3.14159;\n\n// string.js\nexport default function capitalize(str) {\n  return str.charAt(0).toUpperCase() + str.slice(1);\n}\n\nexport function inverter(str) {\n  return str.split("").reverse().join("");\n}\n\n// main.js\nimport { somar, subtrair, multiplicar, PI } from "./math.js";\nimport capitalize, { inverter } from "./string.js";\n\nconsole.log("Soma:", somar(5, 3));          // 8\nconsole.log("PI:", PI);                      // 3.14159\nconsole.log("Capitalize:", capitalize("olá mundo")); // "Olá mundo"\nconsole.log("Inverter:", inverter("JavaScript")); // "tpircSavaJ"\n\n// Import dinâmico (lazy loading)\nconst modulo = await import("./math.js");\nconsole.log(modulo.multiplicar(4, 5)); // 20`
      },
      {
        dia: 2,
        titulo: 'Error Handling: try/catch e Custom Errors',
        teoria: 'Tratamento de erros robusto com try/catch/finally, throw personalizado, e tipos de erro nativos (Error, TypeError, RangeError).',
        topicos: [
          { titulo: 'try/catch/finally', descricao: 'try { /* código que pode lançar erro */ } catch (err) { /* tratar erro */ } finally { /* sempre executa */ }. catch captura qualquer erro lançado no try. finally é opcional e executa sempre (sucesso ou erro) — ideal para limpeza (fechar arquivos, esconder loading). catch pode omitir a variável (ES2022): catch { /* sem acesso ao erro */ } se você não precisa do erro.', exemplo: 'try { JSON.parse(dados) } catch { console.error("JSON inválido") } finally { limpar() }' },
          { titulo: 'throw — lançar erros', descricao: 'throw new Error("Mensagem") — qualquer coisa pode ser lançada: throw "erro", throw 404, throw { code: 500 }. Mas é recomendado usar Error ou subclasses. throw interrompe a execução e desce a pilha até um catch. O objeto Error tem: message (legível), name (tipo), stack (pilha de chamadas — útil para debug). Em produção, não exiba stack para o usuário final.', exemplo: 'if (!nome) throw new Error("Nome obrigatório")' },
          { titulo: 'Tipos de erro nativos', descricao: 'Error — base. TypeError — operação inválida (ex: null.prop). RangeError — valor fora do intervalo (ex: new Array(-1)). ReferenceError — variável não definida. SyntaxError — erro de sintaxe (em eval). URIError — URI malformada (encodeURI/decodeURI). Cada tipo tem name específico: err.name === "TypeError". Use instanceof: err instanceof TypeError === true.', exemplo: 'null.prop // TypeError; new Array(-1) // RangeError' },
          { titulo: 'Custom errors', descricao: 'Crie erros personalizados estendendo Error: class ValidationError extends Error { constructor(message, campo) { super(message); this.name = "ValidationError"; this.campo = campo } }. Depois: if (!nome) throw new ValidationError("Nome obrigatório", "nome"). Catch específico: if (err instanceof ValidationError) { /* tratar validação */ } else { /* outros erros */ }. Custom errors melhoram a semântica do tratamento.', exemplo: 'class ValidationError extends Error { constructor(m) { super(m); this.name = "ValidationError" } }' }
        ],
        exercicio: 'Crie uma função que valida um usuário (nome, email, idade) e lança ValidationError personalizado para cada campo inválido.',
        codigoInicial: `class ValidationError extends Error {\n  constructor(message, campo) {\n    super(message);\n    this.name = "ValidationError";\n    this.campo = campo;\n  }\n}\n\nfunction validarUsuario(dados) {\n  const erros = [];\n  \n  if (!dados.nome?.trim()) {\n    erros.push(new ValidationError("Nome é obrigatório", "nome"));\n  }\n  \n  if (!dados.email?.includes("@")) {\n    erros.push(new ValidationError("Email inválido", "email"));\n  }\n  \n  if (dados.idade < 0 || dados.idade > 150) {\n    erros.push(new ValidationError("Idade inválida", "idade"));\n  }\n  \n  if (erros.length > 0) throw erros;\n  return dados;\n}`,
        codigoSolucao: `class ValidationError extends Error {\n  constructor(message, campo) {\n    super(message);\n    this.name = "ValidationError";\n    this.campo = campo;\n  }\n}\n\nfunction validarUsuario(dados) {\n  const erros = [];\n  \n  if (!dados.nome?.trim()) {\n    erros.push(new ValidationError("Nome é obrigatório", "nome"));\n  }\n  if (!dados.email?.includes("@")) {\n    erros.push(new ValidationError("Email inválido", "email"));\n  }\n  if (dados.idade < 0 || dados.idade > 150) {\n    erros.push(new ValidationError("Idade inválida", "idade"));\n  }\n  \n  if (erros.length > 0) throw erros;\n  return dados;\n}\n\ntry {\n  validarUsuario({ nome: "", email: "invalido", idade: 200 });\n} catch (erros) {\n  if (Array.isArray(erros)) {\n    erros.forEach(err => {\n      if (err instanceof ValidationError) {\n        console.error(\`Campo \${err.campo}: \${err.message}\`);\n      }\n    });\n  }\n}\n\n// Finally example\nasync function buscarDados() {\n  const spinner = mostrarLoading();\n  try {\n    const data = await fetch(url);\n    return await data.json();\n  } catch (err) {\n    console.error("Erro:", err);\n    throw err;\n  } finally {\n    esconderLoading(spinner); // sempre executa\n  }\n}`
      },
      {
        dia: 3,
        titulo: 'Web APIs: Storage, Geolocalização e mais',
        teoria: 'O navegador oferece várias APIs: localStorage/sessionStorage para persistência, Geolocation para localização, Canvas para gráficos, Audio para som.',
        topicos: [
          { titulo: 'localStorage e sessionStorage', descricao: 'localStorage.setItem("chave", "valor") — persistente entre sessões. sessionStorage — dura até fechar a aba. Ambos armazenam apenas STRINGS — use JSON.stringify/parse para objetos. localStorage.getItem("chave") retorna null se não existir. localStorage.removeItem("chave"). localStorage.clear() limpa tudo. Evento storage: window.addEventListener("storage", (e) => { /* outra aba mudou */ }). Limite ~5MB.', exemplo: 'localStorage.setItem("key", JSON.stringify({a:1})); JSON.parse(localStorage.getItem("key"))' },
          { titulo: 'Geolocation API', descricao: 'navigator.geolocation.getCurrentPosition(success, error, options) — obtém posição única. success: { coords: { latitude, longitude, accuracy }, timestamp }. error: { code: 1(permissão negada), 2(indisponível), 3(timeout) }. options: { enableHighAccuracy, timeout, maximumAge }. watchPosition() monitora mudanças. clearWatch() para. Necessita permissão do usuário — sempre trate erro de permissão.', exemplo: 'navigator.geolocation.getCurrentPosition(p => console.log(p.coords.latitude))' },
          { titulo: 'Canvas API', descricao: '<canvas id="meuCanvas" width="400" height="300"></canvas>. const ctx = canvas.getContext("2d"). Desenho: fillRect, strokeRect, fillText, beginPath, moveTo, lineTo, arc, fill, stroke. Cores: fillStyle, strokeStyle. Animações: requestAnimationFrame(loop). Canvas é usado para: gráficos, jogos, visualização de dados, editores de imagem. Para gráficos mais complexos, bibliotecas como Chart.js, D3.js e PixiJS simplificam.', exemplo: 'ctx.fillStyle = "red"; ctx.fillRect(10, 10, 100, 50)' },
          { titulo: 'Notifications e Clipboard', descricao: 'Notification: Notification.requestPermission().then(p => { if (p === "granted") { new Notification("Título", { body: "Texto" }) } }). Clipboard: navigator.clipboard.writeText("texto").write([new ClipboardItem({ "text/plain": blob })]). navigator.clipboard.readText(). Clipboard API requer HTTPS (ou localhost) e permissão. Notifications API requer HTTPS.', exemplo: 'navigator.clipboard.writeText("texto"); new Notification("Título", { body: "msg" })' }
        ],
        exercicio: 'Crie uma página que salva preferências no localStorage, obtém geolocalização, e exibe um alerta se estiver em determinada cidade.',
        codigoInicial: `// Salvar preferências\nconst preferencias = {\n  tema: "escuro",\n  idioma: "pt-BR",\n  notificacoes: true\n};\n\nlocalStorage.setItem("preferencias", JSON.stringify(preferencias));\n\n// Recuperar\nconst salvas = JSON.parse(localStorage.getItem("preferencias") || "{}");\nconsole.log("Preferências:", salvas);\n\n// Geolocalização\nnavigator.geolocation.getCurrentPosition(\n  (pos) => {\n    console.log("Lat:", pos.coords.latitude);\n    console.log("Lon:", pos.coords.longitude);\n  },\n  (err) => {\n    console.error("Erro geo:", err.message);\n  }\n);`,
        codigoSolucao: `// Salvar preferências\nconst preferencias = {\n  tema: "escuro",\n  idioma: "pt-BR",\n  notificacoes: true\n};\n\nlocalStorage.setItem("preferencias", JSON.stringify(preferencias));\n\nconst salvas = JSON.parse(localStorage.getItem("preferencias") || "{}");\nconsole.log("Preferências:", salvas);\n\n// Remover uma\nlocalStorage.removeItem("preferencias");\n\n// Geolocalização\nconst geoOpcoes = {\n  enableHighAccuracy: true,\n  timeout: 10000,\n  maximumAge: 60000\n};\n\nnavigator.geolocation.getCurrentPosition(\n  (pos) => {\n    const { latitude, longitude } = pos.coords;\n    console.log(\`Latitude: \${latitude}, Longitude: \${longitude}\`);\n    console.log(\`Precisão: \${pos.coords.accuracy}m\`);\n  },\n  (err) => {\n    switch(err.code) {\n      case err.PERMISSION_DENIED:\n        console.error("Usuário negou permissão"); break;\n      case err.POSITION_UNAVAILABLE:\n        console.error("Localização indisponível"); break;\n      case err.TIMEOUT:\n        console.error("Tempo excedido"); break;\n    }\n  },\n  geoOpcoes\n);\n\n// Clipboard\nnavigator.clipboard.writeText("Texto copiado!")\n  .then(() => console.log("Copiado!"));\n\n// Notification\nif (Notification.permission === "granted") {\n  new Notification("JS Study", { body: "Hora de estudar!" });\n}`
      },
      {
        dia: 4,
        titulo: 'Projeto Final: Dashboard de Estudos JS',
        teoria: 'Vamos construir o projeto final integrando todos os conceitos: DOM, eventos, localStorage, módulos, async/await, classes e métodos de array.',
        topicos: [
          { titulo: 'Planejamento do projeto', descricao: 'O dashboard terá: 1) Timer de estudos (Pomodoro) com localStorage. 2) Lista de tarefas com CRUD completo. 3) Notas com data e categoria. 4) Relatório semanal com estatísticas. 5) Tema claro/escuro com localStorage. Estrutura: módulos separados (timer.js, tasks.js, notes.js, stats.js, theme.js). Estado central no localStorage. Eventos: addEventListener com delegação.', exemplo: 'localStorage.setItem("js-theme", JSON.stringify({ tema: "escuro" }))' },
          { titulo: 'Integração de conceitos', descricao: 'Arrays e métodos: tarefas.filter().map().reduce() para relatórios. Classes: Timer, Task, Note. Modules: import/export entre arquivos. Async/await: fetch para citações motivacionais. DOM: manipulação dinâmica com createElement. Eventos: delegação na lista de tarefas. localStorage: persistência de todos os dados. Error handling: try/catch em operações críticas.', exemplo: 'tarefas.filter(t => t.concluido).map(t => t.texto).reduce((a, b) => a + ", " + b, "")' }
        ],
        exercicio: 'Inicie o projeto final: configure a estrutura de módulos, implemente o timer Pomodoro com classes e localStorage.',
        codigoInicial: `// timer.js\nexport default class Timer {\n  #duration;\n  #remaining;\n  #intervalId = null;\n  #onTick;\n  #onComplete;\n  \n  constructor(duration, onTick, onComplete) {\n    this.#duration = duration;\n    this.#remaining = duration;\n    this.#onTick = onTick;\n    this.#onComplete = onComplete;\n  }\n  \n  start() {\n    this.#intervalId = setInterval(() => {\n      this.#remaining--;\n      this.#onTick?.(this.#remaining);\n      if (this.#remaining <= 0) {\n        this.stop();\n        this.#onComplete?.();\n      }\n    }, 1000);\n  }\n  \n  stop() {\n    if (this.#intervalId) {\n      clearInterval(this.#intervalId);\n      this.#intervalId = null;\n    }\n  }\n  \n  reset() {\n    this.stop();\n    this.#remaining = this.#duration;\n    this.#onTick?.(this.#remaining);\n  }\n  \n  get remaining() { return this.#remaining; }\n  get isRunning() { return this.#intervalId !== null; }\n}`,
        codigoSolucao: `// timer.js\nexport default class Timer {\n  #duration;\n  #remaining;\n  #intervalId = null;\n  #onTick;\n  #onComplete;\n  \n  constructor(duration, onTick, onComplete) {\n    this.#duration = duration;\n    this.#remaining = duration;\n    this.#onTick = onTick;\n    this.#onComplete = onComplete;\n  }\n  \n  start() {\n    if (this.#intervalId) return;\n    this.#intervalId = setInterval(() => {\n      this.#remaining--;\n      this.#onTick?.(this.#remaining);\n      if (this.#remaining <= 0) {\n        this.stop();\n        this.#onComplete?.();\n      }\n    }, 1000);\n  }\n  \n  stop() {\n    if (this.#intervalId) {\n      clearInterval(this.#intervalId);\n      this.#intervalId = null;\n    }\n  }\n  \n  reset() {\n    this.stop();\n    this.#remaining = this.#duration;\n    this.#onTick?.(this.#remaining);\n  }\n  \n  formatTime() {\n    const min = Math.floor(this.#remaining / 60);\n    const seg = this.#remaining % 60;\n    return \`\${String(min).padStart(2, "0")}:\${String(seg).padStart(2, "0")}\`;\n  }\n  \n  get remaining() { return this.#remaining; }\n  get isRunning() { return this.#intervalId !== null; }\n}\n\n// main.js\nimport Timer from "./timer.js";\n\nconst display = document.getElementById("timer-display");\nconst btnStart = document.getElementById("btn-start");\nconst btnReset = document.getElementById("btn-reset");\n\nconst timer = new Timer(25 * 60,\n  (remaining) => { display.textContent = timer.formatTime(); },\n  () => { alert("Hora de pausar!"); }\n);\n\nbtnStart.addEventListener("click", () => {\n  if (timer.isRunning) timer.stop();\n  else timer.start();\n  btnStart.textContent = timer.isRunning ? "Pausar" : "Iniciar";\n});\n\nbtnReset.addEventListener("click", () => timer.reset());\n\ndisplay.textContent = timer.formatTime();`
      },
      {
        dia: 5,
        titulo: 'Projeto Final: Finalização e Deploy',
        teoria: 'Finalize o dashboard com todos os módulos integrados, testes e prepare para deploy.',
        topicos: [
          { titulo: 'Organização final do código', descricao: 'Estrutura: src/timer.js, src/tasks.js, src/notes.js, src/stats.js, src/theme.js, src/main.js. Cada módulo exporta sua funcionalidade principal. main.js importa todos e coordena a aplicação. Dados: chaves no localStorage (js-timer, js-tasks, js-notes, js-theme). Trate dados corrompidos com try/catch e valores padrão. Consistência: use JSON.stringify/parse para todos os dados.', exemplo: 'import Timer from "./timer.js"; import { gerarRelatorio } from "./stats.js"' },
          { titulo: 'Deploy e próximos passos', descricao: 'Para deploy: GitHub Pages (gratuito), Vercel, Netlify. Apenas arquivos estáticos (HTML, CSS, JS). Para projetos maiores: aprenda React (framework mais usado), Node.js (backend), TypeScript (tipos). Pratique no CodePen, JSFiddle. Contribua para projetos open source. O aprendizado de JavaScript é contínuo — continue construindo projetos!', exemplo: 'npm run build && npx gh-pages -d dist' }
        ],
        exercicio: 'Complete o dashboard com estatísticas semanais (reduce para total de horas, filter para itens concluídos). Publique no GitHub Pages.',
        codigoInicial: `// stats.js\nexport function gerarRelatorio(tarefas, notas, sessoesTimer) {\n  const tarefasConcluidas = tarefas.filter(t => t.concluido).length;\n  const totalTarefas = tarefas.length;\n  const totalHoras = sessoesTimer.reduce((acc, s) => acc + s.minutos, 0) / 60;\n  const notasPorDia = notas.reduce((acc, n) => {\n    const dia = n.data.split("T")[0];\n    acc[dia] = (acc[dia] || 0) + 1;\n    return acc;\n  }, {});\n  \n  return {\n    tarefasConcluidas,\n    totalTarefas,\n    progresso: totalTarefas > 0 ? Math.round(tarefasConcluidas / totalTarefas * 100) : 0,\n    totalHoras: totalHoras.toFixed(1),\n    notasPorDia\n  };\n}`,
        codigoSolucao: `// stats.js\nexport function gerarRelatorio(tarefas, notas, sessoesTimer) {\n  const tarefasConcluidas = tarefas.filter(t => t.concluido).length;\n  const totalTarefas = tarefas.length;\n  const totalHoras = sessoesTimer.reduce((acc, s) => acc + s.minutos, 0) / 60;\n  \n  const notasPorDia = notas.reduce((acc, n) => {\n    const dia = n.data ? n.data.split("T")[0] : "sem-data";\n    acc[dia] = (acc[dia] || 0) + 1;\n    return acc;\n  }, {});\n  \n  const thisWeek = sessoesTimer.filter(s => {\n    const diasAtras = (Date.now() - new Date(s.data).getTime()) / 86400000;\n    return diasAtras <= 7;\n  });\n  \n  return {\n    tarefasConcluidas,\n    totalTarefas,\n    progresso: totalTarefas > 0 ? Math.round(tarefasConcluidas / totalTarefas * 100) : 0,\n    totalHoras: totalHoras.toFixed(1),\n    horasEstaSemana: (thisWeek.reduce((acc, s) => acc + s.minutos, 0) / 60).toFixed(1),\n    notasPorDia\n  };\n}\n\n// Exemplo de uso\nconst tarefas = [\n  { id: 1, texto: "Estudar map", concluido: true },\n  { id: 2, texto: "Praticar reduce", concluido: false }\n];\n\nconst sessoes = [\n  { data: new Date().toISOString(), minutos: 50 },\n  { data: new Date(Date.now() - 86400000).toISOString(), minutos: 45 }\n];\n\nconst relatorio = gerarRelatorio(tarefas, [], sessoes);\nconsole.log("Relatório:", relatorio);\n// { tarefasConcluidas: 1, totalTarefas: 2, progresso: 50, totalHoras: \"1.6\", ... }\n\n// Para salvar relatório em JSON\nlocalStorage.setItem("js-relatorio", JSON.stringify(relatorio));`
      }
    ],
    miniProjeto: {
      titulo: 'Dashboard de Estudos JS',
      descricao: 'Projeto completo integrando módulos, classes, async, DOM, localStorage e métodos de array',
      requisitos: [
        'Timer Pomodoro com classe e eventos',
        'Lista de tarefas com CRUD e persistência',
        'Sistema de notas com categorias',
        'Relatório semanal com map/filter/reduce',
        'Tema claro/escuro com localStorage',
        'Módulos separados (import/export)'
      ]
    }
  }
];


